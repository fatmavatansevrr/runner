# PHASE 10K-GEN.25 — Beginner×3D Core Public HTTP/PostgreSQL Activation

**Parent authority**: `GEN.21` (diagnosed the real, mutable taper-minimum lever), `GEN.23` (implemented the frozen Option-1 taper-specific KEY/EASY/LONG minima triple, real dark-verified representability for missing/positive-observed readiness at every governed Core horizon 8-14 weeks), `GEN.24` (explicit-zero readiness `PRODUCT_INELIGIBLE`, typed rejection), `GEN.20`/`GEN.10` (public-activation phase shape/rigor this report models)
**Phase type**: IMPLEMENTATION (activation) + DEFECT-FAMILY SEARCH + REAL HTTP/POSTGRESQL VERIFICATION
**Execution status**: DONE
**Final classification**: `BEGINNER_3D_CORE_PUBLICLY_ACTIVE_MISSING_AND_POSITIVE_OBSERVED_READINESS`
**Governance commit**: `f3c3677` (implementation + ledger row, backfilled self-referentially per this engagement's established two-commit pattern).

---

## 0. Mandatory startup — completed

`PHASE_LEDGER.md`/`MASTER_ROADMAP.md` read in full. Re-read in full, in order: `PHASE_10K_GEN_21_BEGINNER_3D_CORE_TAPER_MINIMUM_REALIGNMENT.md`, `PHASE_10K_GEN_23_BEGINNER_3D_CORE_TAPER_MINIMUM_IMPLEMENTATION.md`, `PHASE_10K_GEN_24_BEGINNER_3D_EXPLICIT_ZERO_READINESS_PRODUCT_INELIGIBLE.md`, `PHASE_10K_GEN_20_2D_BEGINNER_INTERMEDIATE_CORE_PUBLIC_ACTIVATION.md` (closest precedent — public activation of a dark-verified frequency/level cell, real defects found one layer downstream of dark verification), and `PHASE_10K_GEN_10_ADVANCED_3D_4D_5D_6D_COMBINED_PUBLIC_HTTP_POSTGRESQL_ACTIVATION.md` (original precedent for this activation pattern).

Confirmed the exact representable scope from `GEN.21`-`GEN.24`'s own text: Beginner×3D Core, 8-14 weeks, missing-readiness (12.0km start) and positive-observed readiness (band boundaries 16.0/20.0km) fully representable at every one of the 7 governed horizons (`GEN.23` §5, 21/21 real successes, re-verified zero-delta by `GEN.24` §5); explicit-zero readiness (9.5km, reused from Beginner×4D) is `PRODUCT_INELIGIBLE` at all 7 horizons via `BeginnerThreeDayExplicitZeroReadinessProductIneligibleException` (`GEN.24` §4/§5) — final authority for this identity shape, not reopened here.

`git log -5` (HEAD `2e512d4`), `git fetch && git diff HEAD origin/main` (0/0, in sync), `git status --porcelain` confirmed clean except the pre-existing, unrelated `baseline_tmp`/`plan-catalog/artifacts/audits/ten-k-pilot-domain-decision-audit.{json,md}` modifications predating this session — left untouched throughout, never staged. Next free phase ID confirmed by direct grep of `PHASE_LEDGER.md`: last row is Seq 127 (`GEN.24`), so `GEN.25` is correct — matches the governing prompt's own expectation, verified against repository truth rather than assumed.

## 1. Scope, exactly as governed

Open the real public HTTP/PostgreSQL routing gate for Beginner×3D Core, all horizons **8-14 weeks**, for **missing-readiness and positive-observed-readiness requests only**. Real HTTP `GeneratePreview`, real confirm + fresh reload, Home/Calendar/TrainingDay reads. Explicit-zero readiness must fail closed through the real HTTP response with `GEN.24`'s typed `PRODUCT_INELIGIBLE` rejection — not a silent fallback, not a generic error, not swallowed at the public gate. No widening beyond 8-14 weeks. No attempt to make explicit-zero "work." No new product/numeric authority — this is an activation phase implementing already-approved `GEN.21`/`GEN.23`/`GEN.24` authority only.

## 2. Public gate widening

`V1CatalogPilotIdentityPolicy.cs`:
- Added `ThreeDayBeginnerCandidateKey = "TEN_K__3D__BEGINNER"` (v1) — the real, already-dark-verified `GEN.23`/`GEN.24` candidate, status `VALIDATED` in its catalog document (unchanged; `LocalCatalogAcceptance`'s Development-environment override treats it as effectively `PUBLISHED` for route-decision purposes, the identical mechanism already relied on for `TEN_K__4D__BEGINNER v1`, itself still `VALIDATED`, and for the pilot `TEN_K__4D__INTERMEDIATE v10`, itself still `DRAFT` — confirmed by direct comparison of every combination document's own `metadata.status` field before assuming this needed a catalog change).
- `IsSupportedLevelFrequency`: added `(Beginner, 3)`.
- `ResolveCandidate`: added the `(Beginner, 3) → (ThreeDayBeginnerCandidateKey, ThreeDayBeginnerCandidateVersion)` resolution.
- **Deliberately NOT added** to `IsSupportedPreparationRunwayLevelFrequency` or `IsSupportedPreparationRunwayCandidate` — Beginner×3D Preparation Runway/LongHorizon were never designed or approved by any prior phase (unlike 2D, there is no confirmed-gap-pending-design story here at all; it is simply out of scope), so no public or internal-dark path may treat this candidate as Runway/LongHorizon eligible.

`LongHorizonPublicPlanService.ValidatePilot` — **not touched**. Still only admits `(Intermediate, 4/5/6)` and `(Advanced, 3/4/5/6)`.

**No dedicated horizon-exclusion routing check was added** (unlike `GEN.20`'s 2D-specific 8/9-week check). This is a real, verified difference from 2D's activation shape, not an oversight: `GEN.23` §5 proved every one of the 7 governed horizons (8-14 weeks) representable for missing/positive-observed readiness — there is no narrower non-supported sub-range within 8-14 to gate. The candidate's own `TEN_K_MASTER`-inherited `CoreCycle` bounds (`minimumWeeks=8`, `maximumWeeks=14`, confirmed by direct read of `plan-catalog/catalog/templates/ten-k-master.v11.json`) already constrain `V1LiveCatalogPilotRoutingPolicy.Evaluate`'s own `withinSupportedCycle` check (`CompressedCore`/`PreferredCore`/`ExtendedCore` modes) to exactly 8-14 weeks by construction — 15+ weeks fall outside `RaceHorizonPolicy`'s Core-mode classification entirely and are rejected upstream of any Beginner×3D-specific code, verified real-HTTP in §6.

## 3. Explicit-zero readiness: no new dispatch or exception mapping required

`GEN.24`'s `BeginnerThreeDayExplicitZeroReadinessProductIneligibleException` derives from the shared `CatalogProductIneligibleException` base, which `CatalogPreviewGenerator`'s existing generic `catch (CatalogProductIneligibleException ex)` (and its `DynamicCoreVolumeAndLongRunFailedException` inner-exception variant) already translates to `PlanProductIneligibleException` — mapped by `GlobalExceptionHandler` to HTTP 422 with `productIneligible.Reason` (`"BEGINNER_THREE_DAY_EXPLICIT_ZERO_READINESS_NOT_ELIGIBLE"`) echoed verbatim as the error code. This is the exact "every future candidate cell's ineligibility exception is picked up automatically" behavior `GEN.24` §4.1 itself documented. **This phase adds zero exception-mapping or dispatch code** — activation alone is sufficient to make the already-implemented rejection reachable and observable via real HTTP, verified in §6.

## 4. Recurring-defect-family search — public-routing/gate layer specifically

Per this phase's own explicit instruction: `GEN.24` came back clean (the first phase in this chain to find no new hardcode instance), but that clean result was earned entirely inside the session-prescription/volume-planning layer (`CatalogVolumeAndLongRunPlanner`, `V1ThreeDaySessionVolumeAllocationPolicy`, `CatalogFinalPrescribedPlanValidator` — all already exercised by `GEN.23`'s own real dark pipeline). The public-routing/gate layer itself (`V1CatalogPilotIdentityPolicy`, `LivePlanPreviewRouting.cs`, `CatalogPreviewGenerator.cs`, `CatalogPublicPreviewMaterializer.cs`, `GlobalExceptionHandler.cs`, `CatalogPlanConfirmationService.cs`) had never been exercised by any Beginner×3D-specific request before this phase — this is exactly the "one layer downstream of anything previously dark-verified" shape `GEN.20` found two real production defects in for 2D.

Searched every file in that layer for `DaysPerWeek ==`/`Level ==`/hardcoded-Level-default patterns before writing any code:

- `LivePlanPreviewRouting.cs`: two Level/DaysPerWeek-scoped short-circuits exist (`cycleLength is 8 or 9 && DaysPerWeek == 2 && (Beginner || Intermediate)` — 2D-specific, `GEN.20`; `cycleLength == 8 && Level == Intermediate && DaysPerWeek == V1CatalogPilotIdentityPolicy.DaysPerWeek(==4) && RecentWeeklyVolumeKm == 0` — Intermediate×4D-specific, `GEN.4E`). Both are exact-typed combination matches; neither can misfire for `(Beginner, 3)` by construction (confirmed by direct reading, not assumption) — no generalization needed, no new instance found.
- `CatalogPreviewGenerator.cs`: grepped for `RunningBackground`/`Level ==`/`"NEW"`/`"BEGINNER"` — zero matches. The catalog-live preview generation path is Level/DaysPerWeek-agnostic by construction; dispatch happens entirely through the already-resolved `PlanCatalogCandidateSummary` and downstream typed policies, not through any Level-blind literal in this file.
- `CatalogPublicPreviewMaterializer.cs`: grepped for `DaysPerWeek ==`/`DaysPerWeek is` — zero matches. No structural-count-proxy hardcode of the exact shape `GEN.20` §4.2 found in `CatalogFinalPrescribedPlanValidator` (a different file, already fixed by `GEN.23` for Beginner×3D specifically).
- `CatalogPlanConfirmationService.cs`: one `snapshot.NormalizedInput.Level ?? RunningBackground.Intermediate` null-coalescing default at `BuildCatalogTrainingPlan` (line 577) — inspected directly. This is not a new risk specific to Beginner×3D: it only fires if `NormalizedInput.Level` is itself null, an upstream request-normalization concern identical for every already-public Beginner candidate (2D, 4D); not a fresh hardcode instance introduced or newly reachable by this phase.
- `GlobalExceptionHandler.cs`: confirmed the generic `PlanProductIneligibleException productIneligible => (422, productIneligible.Reason)` mapping (§3) requires no per-exception-type edit — verified by grep, no `BeginnerThreeDay`-specific case needed or added.

**No new instance of the recurring hardcode-assumption defect family was found in the public-routing/gate layer.** Unlike `GEN.20`, this activation required no production-code defect fix — the identity-widening change (§2) alone, combined with `GEN.23`/`GEN.24`'s already-complete downstream implementation, is sufficient. This was verified empirically (§6), not assumed from the clean search alone.

## 5. Not new authority

The only production-code change in this phase (`V1CatalogPilotIdentityPolicy.cs`, §2) widens an existing, already-frozen allow-list mechanism to a real, already-approved candidate identity (`GEN.21`/`GEN.23`/`GEN.24`'s own frozen authority) — no new numeric constant, product rule, or eligibility decision. No `DOMAIN_DECISION_REQUIRED` STOP condition was reached anywhere in this phase.

## 6. Real HTTP/PostgreSQL verification (`Gen25BeginnerThreeDayCorePublicActivationTests`, 45 tests, all real HTTP + real PostgreSQL)

- **Missing readiness, 8-14 weeks** (`CoreHorizons_EightThroughFourteenWeeks_PublicPreviewSucceeds_MissingReadiness`, 7 cases): real `GeneratePreview` success, `TEN_K__3D__BEGINNER` template ID confirmed, exactly 3 sessions/week confirmed for every week.
- **Positive-observed readiness, both band boundaries (16.0km/20.0km), 8-14 weeks** (`CoreHorizons_EightThroughFourteenWeeks_PublicPreviewSucceeds_PositiveObservedReadiness`, 14 cases): real preview success at every horizon for both boundary profiles.
- **`Full8To14Matrix_MissingAndBothPositiveObservedProfiles_AllTwentyOneSucceed`**: the full 21-cell matrix (3 readiness profiles × 7 horizons), real HTTP, asserts all 21 succeed — matching `GEN.23`'s own 21/21 dark-verified result now proven live over real HTTP/PostgreSQL, not merely the dark pipeline.
- **Explicit-zero fail-closed, every horizon** (`ExplicitZero_AllGovernedHorizons_FailsClosed_WithGen24TypedRejection_NotSilentFallbackNotGenericError`, 7 cases): real HTTP 422, body contains `BEGINNER_THREE_DAY_EXPLICIT_ZERO_READINESS_NOT_ELIGIBLE`, confirmed absent: `INTERNAL_ERROR`, the sibling `BEGINNER_THREE_DAY_CORE_TAPER_VOLUME_BELOW_MINIMUM_FULL_LAYOUT` exception (`GEN.23`'s own distinct taper-ineligibility type — confirms no silent misrouting between the two typed rejections), and any successful-plan template-ID marker.
- **`Full8To14Matrix_ExplicitZero_ExactlySevenTypedRejections_ZeroSuccesses`**: the full 7-horizon explicit-zero matrix, real HTTP, asserts exactly 0 successes and exactly 7 typed rejections — the uniform-rejection shape `GEN.24` §5 established, now proven live.
- **Confirm + fresh-PostgreSQL-reload** (`Confirm_ReloadsFromFreshPostgres_WithCorrectLevelAndDaysPerWeek`): asserts the reloaded persisted `TrainingPlan.Level == Beginner`, `DaysPerWeek=3`, `CatalogCandidateKey == TEN_K__3D__BEGINNER`, 12 real persisted weeks, 36 real persisted days with the correct 12/12/12 `KEY_SESSION`/`EASY_SUPPORT`/`LONG_RUN` role partition.
- **Home / Calendar / TrainingDay reads** (`ConfirmedPlan_HomeCalendarAndTrainingDayReads_AllSucceed`): real HTTP `active/home` (containing the real plan ID), `active/calendar`, and a real `training-days/{id}` detail read against a confirmed, persisted Beginner×3D plan.
- **Unsupported-neighbor closure** — see §7 for the full breakdown; all real HTTP requests, none code-inspection-only.

45/45 pass.

## 7. Unsupported-neighbor closure results

All real HTTP, per this phase's own non-negotiable requirement (never code-inspection-only):

- **Explicit-zero fail-closed with the real typed rejection surfacing through the real HTTP response** (§6) — the headline requirement of this phase's own governing prompt, verified at all 7 governed horizons, confirmed distinct from both the generic `INTERNAL_ERROR` shape and `GEN.23`'s own sibling taper-ineligibility exception.
- **Beginner×4D zero-delta, including its own explicit-zero handling** (`BeginnerFourDay_ExplicitZeroHandling_IsCompletelyUnaffected_ZeroDelta`, 2 cases): real HTTP, 8-week explicit-zero still rejects with the unchanged `BEGINNER_FOUR_DAY_CORE_TAPER_VOLUME_BELOW_MINIMUM_FULL_LAYOUT` (confirmed absent: the new Beginner×3D-specific reason code), 13-week explicit-zero still succeeds at the unchanged 9.5km default. 4D's own 9.5km explicit-zero default and 9-week eligibility boundary are untouched by this phase — confirmed by construction (no file under `V1BeginnerFourDayMissingReadinessStartingVolumePolicy.cs`/`V1BeginnerFourDayVolumeEligibilityPolicy.cs`/`V1FourDaySessionVolumeAllocationPolicy.cs` touched) and now also by real HTTP.
- **Beginner×2D zero-delta** (`ExistingPubliclyActiveFrequencies_ZeroDelta`): real HTTP preview success, unchanged.
- **Beginner×5D/6D/7D remain unreachable** (`UnsupportedNeighborFrequencies_BeginnerFiveSixSevenDay_RemainClosed`, 3 cases): real HTTP failure, confirmed absent: `TEN_K__3D__BEGINNER` and `INTERNAL_ERROR` — `GEN.22`'s `PRODUCT_NON_SUPPORT` reaffirmation and `GEN.6`'s original closure are both unaffected; no code path relevant to 5D/6D/7D was touched.
- **Intermediate zero-delta, every supported frequency** (`ExistingPubliclyActiveFrequencies_ZeroDelta`): real HTTP preview success for Intermediate 2D/3D/4D/5D/6D, all unchanged.
- **Advanced 3D-6D zero-delta** (`ExistingPubliclyActiveFrequencies_ZeroDelta`, plus the dedicated `AdvancedThreeDay_RemainsPubliclyActive_ZeroDelta`): real HTTP preview success for all four Advanced frequencies, including Advanced×3D specifically (the identity nearest this phase's own widened cell, sharing `DaysPerWeek==3` but a different `Level`) — confirmed unaffected by the new `(Beginner,3)` allow-list entry.
- **No public path reaches Preparation Runway or LongHorizon for Beginner×3D**:
  - `RunwayHorizon_BeginnerThreeDay_FailsClosed_NeverReachesRunwayOrLongHorizon` (2 cases, 15/20 weeks): real HTTP 422, body contains `PLAN_HORIZON_COMPOSITION_REQUIRED` (the same pre-existing, unmodified typed exception every other frequency without Runway product authority already returns), confirmed absent: `TEN_K__3D__BEGINNER` and `INTERNAL_ERROR`. This holds by construction (`IsSupportedPreparationRunwayLevelFrequency` was deliberately not widened, §2) and is now also confirmed by real HTTP.
  - `LongHorizonEndpoint_BeginnerThreeDay_FailsClosed_NeverReachesLongHorizon` (3 cases, 21/32/52 weeks): real HTTP failure against the dedicated LongHorizon endpoint, body contains `LONG_HORIZON_PILOT_UNSUPPORTED` (the pre-existing, unmodified `ValidatePilot` rejection), confirmed absent: `TEN_K__3D__BEGINNER`, `INTERNAL_ERROR`, and `rolling_long_horizon` (the LongHorizon success marker).
- **Unsupported neighbor level at 3D remains closed** (`ExperiencedThreeDay_RemainsClosed_NoFallbackToBeginnerThreeDayIdentity`): real HTTP failure, confirmed absent: `TEN_K__3D__BEGINNER` and `INTERNAL_ERROR` — Experienced×3D was never designed and remains unreachable through the widened gate.

No indication was found during this activation that Beginner×3D's absence of Preparation Runway/LongHorizon causes any *silent or confusing* public-facing failure mode — every out-of-8-14-week request returns one of two pre-existing, already-correctly-worded typed rejections (`PLAN_HORIZON_COMPOSITION_REQUIRED` for 15-20wk, `LONG_HORIZON_PILOT_UNSUPPORTED` for 21+wk), identical in shape to what every other Runway/LongHorizon-unsupported frequency already returns.

## 8. Regression

`Gen25BeginnerThreeDayCorePublicActivationTests` run alone first (45/45 pass, `gen25_new_tests.trx`). Full `RunningApp.IntegrationTests` regression then run alone (confirmed zero `dotnet.exe`/build-server processes via `tasklist` immediately before launch, `dotnet build-server shutdown` used first to clear lingering MSBuild/VBCSCompiler nodes from prior invocations in this session): **4232 total, 4223 passed, 9 failed** on the first full run — the 3 known pre-existing baseline failures plus **6 newly-obsolete pre-activation assertions**, the exact "prior activation phase's own neighbor-remains-closed assertion is now legitimately false" shape `GEN.10` §6 already established a correction discipline for. Each was individually classified `OBSOLETE_PRE_ACTIVATION_ASSERTION` (never `POSSIBLE_REAL_REGRESSION`) by reading the failing assertion and confirming the newly-`True`/removed cell is exactly `(Beginner, 3)`, the one cell this phase's own gate widening (§2) made legitimately reachable — not a broader, unexplained loosening:

1. `Gen24BeginnerThreeDayExplicitZeroIneligibilityTests.PublicGate_RemainsClosed_BeginnerThreeDayNotWidened` — asserted the gate stays closed; renamed `PublicGate_WidenedByGen25_BeginnerThreeDayNowPubliclyRoutable`, assertion flipped to `True`.
2. `Gen4DBeginnerFourDayCoreTests.Candidate_IsPublic_AndOnlyBeginnerFourDayWasWidened` — the same test file `GEN.10` §6 already corrected once for Advanced×4D; its `(Beginner,3) → False` assertion flipped to `True`, doc comment extended.
3. `RunningBackgroundV2Tests.BeginnerLevel_ReachesItsOwnPilotMapping_AtFourDaysOnly` — renamed `AtFourAndThreeDays`, added a second positive resolution check confirming Beginner×3D resolves to its own distinct `TEN_K__3D__BEGINNER` candidate (never Intermediate's or 4D's).
4. `Gen3BThreeDayPublicActivationTests.WrongCombination_NeverNearestMatches` theory — removed the now-obsolete `("beginner", 3)` `InlineData` row; the two other rows (`("advanced", 3)`, `("intermediate", 5)`) are unaffected, still-genuinely-unsupported combinations.
5. `Gen4EBeginnerFourDayPublicActivationTests.WrongCombination_NeverNearestMatches` theory — same correction, `("beginner", 3)` row removed; `("advanced", 4)`/`("beginner", 5)` rows unaffected.
6. `Gen23BeginnerThreeDayCoreTests.Candidate_RemainsInternallyGated_NotPubliclyRoutable` — renamed `Candidate_IsNowPubliclyRoutable_AsOfGen25`, assertion flipped to `True`; the underlying catalog document's own `metadata.status` is confirmed unchanged (still `"VALIDATED"`) — activation required no catalog-status edit (§2).

After these 6 corrections (net test-case delta: `-2`, since the two theory rows were removed rather than flipped), a clean full regression was re-run, alone, zero dotnet/build-server processes confirmed via `tasklist` before launch:

```
RunningApp.IntegrationTests: 4230 total, 4227 passed, 3 failed, 0 skipped (gen25_full_regression_final.trx, ~36 minutes)
```

The 3 failures are the identical, already-named pre-existing baseline failures this engagement has carried forward unchanged since `GEN.17`/`GEN.18`/`GEN.20`/`GEN.23`/`GEN.24` (`Gen4EBeginnerFourDayPublicActivationTests.ExplicitZeroAtOrAboveBreakEven_Generates` at weeks:13 and weeks:14, `Sw09ExplicitZeroReadinessEndToEndTests.Sw09Request_ExplicitZeroReadiness_GeneratesFullPreview_UsingExplicitZeroPolicy_NotDefaultOrMissingSubstitution`) — identical test names, identical `InternalServerError`-vs-`OK` failure shape, confirmed by direct read of the TRX stack traces (not console-summary-only). **Total reconciles exactly**: `4187` (`GEN.24`'s own confirmed-clean baseline) `+ 45` this phase's new `Gen25BeginnerThreeDayCorePublicActivationTests` tests `- 2` (the two now-removed obsolete `InlineData` rows, §8 items 4-5) `= 4230`. **Zero new regressions.** `PlanCatalog.Tests`: unaffected by construction (no catalog document added, edited, or removed by this phase — the only production change is `V1CatalogPilotIdentityPolicy.cs`, an application-layer allow-list, §2). `dotnet build RunningApp.sln -c Debug`: 0 errors throughout (only the same pre-existing warning set).

## 9. Explicit constraints — confirmed respected

- **Activated range is exactly 8-14 weeks, not wider**: confirmed by `Full8To14Matrix_MissingAndBothPositiveObservedProfiles_AllTwentyOneSucceed` (all 21 cells across the governed range succeed) and by `RunwayHorizon_BeginnerThreeDay_FailsClosed_NeverReachesRunwayOrLongHorizon`/`LongHorizonEndpoint_BeginnerThreeDay_FailsClosed_NeverReachesLongHorizon` (15+ weeks fail closed). The boundary is enforced entirely by the candidate's own `TEN_K_MASTER`-inherited `CoreCycle` bounds (§2) — no separate dedicated check was needed or added, unlike `GEN.20`'s 2D-specific 8/9-week exclusion.
- **Explicit-zero was not made to "work" by any mechanism**: `GEN.24`'s `BeginnerThreeDayExplicitZeroReadinessProductIneligibleException` is unmodified by this phase; the starting-volume policy gap it declined to fix (`GEN.23` §5/§9, `GEN.24` §11's filed backlog item) remains open and unaddressed here.
- **No new product/numeric authority**: the sole production-code change (§2) is an allow-list widening to an already-approved identity; no new number, rule, or eligibility decision anywhere.
- **Zero-delta for every already-`PUBLICLY_ACTIVE` frequency**: confirmed both by full regression (§8) and by dedicated real-HTTP zero-delta assertions (§7) for Beginner×2D/4D, Intermediate×2D/3D/4D/5D/6D, and Advanced×3D/4D/5D/6D.

## 10. Governance

`PHASE_LEDGER.md` row appended (`GEN.25`). `MASTER_ROADMAP.md`'s Beginner axis support-matrix cell and axis-status paragraph updated to record: Core 8-14 weeks `PUBLICLY_ACTIVE` for Beginner×3D, missing-readiness and positive-observed-readiness only; explicit-zero readiness remains `PRODUCT_INELIGIBLE` (`GEN.24`, now real-HTTP-verified fail-closed); Preparation Runway/LongHorizon remain **not implemented or designed** for Beginner×3D — distinct from 2D's "confirmed architecture gap pending a dedicated design phase" story, this is simply out of scope, never approved. Two-commit self-referential SHA-backfill pattern followed (implementation+ledger commit with a `PENDING` placeholder, then a small backfill commit recording this row's own real SHA). Push-gate discipline applied after both commits.

**This closes `BEGINNER_3D_CORE_PUBLICLY_ACTIVE_MISSING_AND_POSITIVE_OBSERVED_READINESS`.** Beginner×3D Core is now a real, public, HTTP/PostgreSQL-verified capability across exactly weeks 8-14, for missing-readiness and positive-observed-readiness requests. Explicit-zero readiness remains formally, permanently `PRODUCT_INELIGIBLE` at every horizon (`GEN.24`) — this phase makes that rejection observable over real HTTP, it does not reopen or attempt to resolve the underlying starting-volume-policy gap `GEN.23`/`GEN.24` disclosed and left open. Preparation Runway (15-20wk) and LongHorizon (21-52wk) remain **not implemented** for Beginner×3D — never designed, out of scope. Next: not selected by this phase — the disclosed Beginner×3D zero-readiness on-ramp backlog item (`GEN.24` §11) and any future Beginner×3D Runway/LongHorizon design remain unscheduled.
