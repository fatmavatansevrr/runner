# Archived Architecture Documents

These documents are preserved for comparison and audit. None is authoritative merely because its filename contains `Reconciled` or `Canonical`.

Use `docs/canonical/CURRENT_ARCHITECTURE.md` to designate an approved source after validation.
