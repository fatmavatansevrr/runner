# Domain Wave 3 D8/D12 Evidence Review

D8 and D12 remain non-blocking, but their evidence strength is now recorded as `EXPLICIT_PRODUCT_DEFAULT` rather than `CANONICAL_CONFIRMED`.

- D8 `FARTLEK v4 $.components`: `WARM_UP, MAIN_SET, RECOVERY, COOL_DOWN`
- D12 `THRESHOLD_TEMPO v4 $.components`: `WARM_UP, MAIN_SET, COOL_DOWN`

The Golden Fixture supports the general workout shapes, but it does not directly canonize the reusable catalog component vocabulary or ordering. Values were not changed, and no blocker was recreated.
