# Activation Readiness Risks — Living Aggregator

Non-blocking-but-activation-relevant risk notes that must be reviewed before publishing or activating `TEN_K__4D__INTERMEDIATE v8` or any descendant candidate, regardless of blocker/decision count.

Current aggregate: 73 risks, 18 `OPEN`, 55 `CLOSED`.

## Visibility pre-check (Wave 7 / D4 pass)

Before implementing D4, a check was performed for whether **TD-D3-001 / AUD-406** was already visible from the activation-readiness / activation-plan / blocker-resolution / release-readiness reporting path. Searched:

- `domain-wave2-activation-plan.{json,md}` — predates D3, no reference.
- `domain-wave3-activation-plan.{json,md}` — predates D3, no reference.
- `domain-wave5-d2-implementation.json`'s `activationReadiness` section — predates the D3 follow-up pass, no reference.
- `production-readiness-error-contract-audit.{json,md}` — a historical, v4-specific error-contract analysis, not a living per-candidate checklist; no reference.

**Result: not visible.** This file was created as the fix — a dedicated, wave-agnostic aggregator seeded with `TD-D3-001`, cross-referenced to its original source (`domain-d3-followup.{json,md}`, audit entry `AUD-406`). Neither `domain-d3-followup.*` nor `AUD-406` nor any Wave 5/D2 report was modified — **D3 is not reopened.**

## Risks

| ID | Statement | Blocking? | Applies to | Status |
|---|---|---|---|---|
| `TD-LONG-HORIZON-POSTGRESQL-CONSTRAINT-EXCEPTION-ROLLBACK-001` | **Real PostgreSQL constraint exceptions roll back the complete Long-Horizon unit of work (Phase 4L.2F-A).** The configured Npgsql provider now produces and verifies real 23505 duplicate activation-idempotency, executable-session and plan-scoped Core-context failures; real 23503 plan-ownership failures during block and retry; and an honestly reachable 23502 required `IdempotencyKey` failure. Every case stages the invalid entity only after valid production mutations are tracked and inside the same existing single `SaveChangesAsync`, then disposes the failed context, proves canonical raw row and `xmin` equality from a fresh connection, reconstructs through production services with integrity validation, and performs corrected replay once. V1 remains Active and unsuperseded after failed refresh; no V2, partial activation, session, block or retry survives. No Long-Horizon check constraint exists, so no 23514 test or convenience migration was fabricated. The internal optional mutation seam defaults to NoOp and has no DI/API/configuration exposure. The Phase 4L.2F parent closes at complete mutation-category coverage; Phase 4L.2B remains open for Phase 4L.2G's remaining concurrency/idempotency/commit-ambiguity matrix. | No | `TEN_K__4D__INTERMEDIATE v10` dark Long-Horizon persistence only | **CLOSED** |
| `TD-LONG-HORIZON-TRANSACTIONAL-FAILURE-INJECTION-ROLLBACK-MATRIX-001` | **Transactional failure-injection and rollback matrix closed at complete mutation-category coverage (Phase 4L.2F-A append-only update).** Phase 4L.2F established the single-`SaveChangesAsync` transaction boundary, production-inert failpoints, representative pre-commit rollback and post-commit acknowledgement recovery. Phase 4L.2F-A now closes its exact residual gap with real configured PostgreSQL: 23505 duplicate activation/session/Core-context ownership, 23503 block/retry foreign-key ownership and reachable 23502 required-value failures all roll back every earlier tracked mutation. Canonical raw state, row counts and `xmin` equal the pre-state; a fresh context reconstructs and passes integrity validation; corrected replay commits once. Exhaustive per-line failpoints are not required because distinct pre-commit stages share the same single EF transaction; all distinct mutation categories and commit boundaries are covered. No Long-Horizon check constraint exists, so no 23514 test was fabricated. The separate Phase 4L.2B parent stays open for Phase 4L.2G concurrency/idempotency/commit-ambiguity work. See `PHASE4L_2F_A_POSTGRESQL_CONSTRAINT_EXCEPTION_AND_ROLLBACK_COMPLETION.md`. | No | `TEN_K__4D__INTERMEDIATE v10` specifically | **CLOSED** |
| `TD-LONG-HORIZON-FUTURE-ONLY-CORE-CONTEXT-REFRESH-001` | **Future-only Core context refresh capability implemented (Phase 4L.2E).** Closes one of the two remaining blockers on the parent `TD-LONG-HORIZON-MIXED-CORE-REFRESH-POSTGRESQL-COMPLETION-MATRIX-001`: a real, eligibility-gated V1→V2 Core context refresh — `LongHorizonCoreRefreshEligibility` rejects refresh during GE/Runway, with no active context, with no future Pending Core week, with a same-or-earlier AsOfDate, or with unchanged evidence (a new nullable `EvidenceFingerprint` column, one additive migration). Eligible refreshes reuse the existing, unmodified real condition-resolution/Core-generation/persistence chain (`LongHorizonRollingRestartContinuationService.ContinueJitCompositionAsync`) rather than duplicating it — the underlying V1→V2 supersession mechanism already existed in the repository but never populated `SupersededByContextId` (now fixed, a narrow proven-necessary correction). **Proven against real PostgreSQL** (horizon 25, Core=14-25): historical weeks 17-20 remain byte-identical after refresh and restart; the new context correctly owns future weeks starting at 21; both V1 (Superseded, linked via `SupersededByContextId`) and V2 (Active) reconstruct from a fresh connection; normal rolling continuation (unmodified, no refresh-specific selector) uses V2 through to terminal completion; a stale-version replay is rejected without duplicating the context; one focused concurrency race has exactly one winner. Two scope narrowings disclosed honestly: refresh uses the existing combined refresh-plus-activation authority rather than a separate context-only persistence path (per the phase's own explicit fallback), and idempotency is proven via version-staleness rejection rather than a literal identical-result replay. The full 13-item corruption matrix and the remaining Phase 4L.2G concurrency scenarios were not attempted. Zero numeric/calendar/direction/Runway/target-lock formula changes. Full LongHorizon suite (769 tests) re-confirmed passing. See `PHASE4L_2E_FUTURE_ONLY_CORE_CONTEXT_REFRESH_CAPABILITY.md`. | No | `TEN_K__4D__INTERMEDIATE v10` specifically | **CLOSED** |
| `TD-LONG-HORIZON-GE-RUNWAY-PARTIAL-BOUNDARY-HANDOFF-001` | **GE-to-Runway partial-boundary checkpoint handoff root-cause resolved (Phase 4L.2D) — the reported production defect was actually a test-harness bug.** Real-Postgres reproduction of 25/26/27-week horizons showed a second continuation call throwing `LongHorizonCheckpointDecisionInvalidException: Next GE week 5 must be NumericPending`, which Phase 4L.2B/4L.2C assumed was a production checkpoint-runtime defect. Direct, non-speculative investigation (isolated calls to the checkpoint runtime, direct calls to the composition orchestrator, stage-by-stage tracing) proved otherwise: the partial terminal GE window (week 5 alone, for horizon 25) is and always was a valid checkpoint window, correctly returning `NextGeWindowActivated`, and the handoff to real JIT composition (via `GeCheckpointCompletedWithoutGeWindowBecauseRunwayBoundaryReached`/`reachesGeBoundary` routing) already worked correctly. The **actual root cause**: `LongHorizonRunwayCoreRestartFixture.AdvanceOneWindowAsync` (a shared PostgreSQL test fixture used since Phase 4L.2A) hardcoded `RaceDate = StartDate + 21*7 days` regardless of the plan's real `TotalWeeks`, making the race appear 4-6 weeks too soon for 25/26/27-week horizons. This caused the real, unmodified Core-generation/condition-resolution pipeline to reject the first GE+Runway mixed-window composition and silently block it; since block persistence (by design) doesn't move the aggregate window pointer, the SECOND checkpoint call recomputed the same boundary and correctly rejected the now-Blocked week 5 — a downstream symptom, not the defect itself. **Fix**: one line, computing `RaceDate` from `state.StructuralRoadmap.TotalWeeks` instead of a hardcoded constant — zero production `RunningApp.Application` code changed. **Proven against real PostgreSQL** for all three horizons (25/26/27), with a restart between every call: the natural mixed shape (GE remainder + greedy-capped Runway weeks) persists, reconstructs, and the Runway continuation advances correctly with no regeneration. No new production contract/validator was built for Parts 10-11's suggested `LongHorizonGeToRunwayBoundaryHandoff`/validator, since direct evidence proved no such gap existed — building one would have been unnecessary abstraction over an already-correct code path. Full existing 757-test LongHorizon suite re-confirmed passing. See `PHASE4L_2D_GE_RUNWAY_PARTIAL_BOUNDARY_CHECKPOINT_HANDOFF_ROOT_CAUSE_RESOLUTION.md`. | No | `TEN_K__4D__INTERMEDIATE v10` specifically | **CLOSED** |
| `TD-LONG-HORIZON-RUNWAY-CONTINUATION-WINDOW-ADVANCEMENT-001` | **Runway continuation window advancement root-cause resolved (Phase 4L.2C).** Root cause proven, not merely hypothesized: `LongHorizonLockedCoreWeekOneTarget.LockedForActivatedRunwayWeekRange` is a `(int, int)` ValueTuple, which exposes its data as public fields (`Item1`/`Item2`), not properties — `System.Text.Json`'s default `JsonSerializerOptions` only serializes/deserializes properties, so the tuple silently round-tripped as `(0,0)` instead of the real persisted value on every restart, tripping `ImmutablePreparationRunwayPrescriptionValidator`'s reuse-scope check on every second continuation call and masked behind the generic `JitSegmentTransitionInfeasible` reason code. **Fix**: a shared `JsonSerializerOptions { IncludeFields = true }` applied symmetrically to every serialize call in `LongHorizonRollingActivationPersistenceAdapter` and every matching deserialize call in `LongHorizonRollingStateReconstructionService` — no numeric/calendar/direction/evidence/target-lock/slice formula touched, no regeneration workaround. A new fail-closed `LongHorizonRunwayContinuationAdvancementValidator` now guards the invariant ("a committed continuation must advance the durable lifecycle boundary") in the real continuation path, not only in tests. **Proven against real PostgreSQL**: three-call progression from GE entry through two Runway continuations into the Runway→Core boundary, each call opening a brand-new `AppDbContext`; the strengthened Phase 4L.2A continuation test now asserts genuine advancement (distinct ranges, no re-activated weeks, exactly one activation record per window, durable pointer matches) instead of identity reuse alone; replay idempotency and fail-closed corruption behavior both re-confirmed. **Re-diagnosed but NOT fixed**: the Phase 4L.2B GE→Runway mixed-window finding (25/26/27-week horizons) fails with a *different* error (`LongHorizonCheckpointDecisionInvalidException: Next GE week 5 must be NumericPending`, thrown in GE checkpoint validation before Runway composition is reached) — confirmed to be an independent, still-open defect, not the same root cause. Full existing 751-test LongHorizon suite re-confirmed passing. See `PHASE4L_2C_RUNWAY_CONTINUATION_WINDOW_ADVANCEMENT_ROOT_CAUSE_RESOLUTION.md`. | No | `TEN_K__4D__INTERMEDIATE v10` specifically | **CLOSED** |
| `TD-LONG-HORIZON-MIXED-CORE-REFRESH-POSTGRESQL-COMPLETION-MATRIX-001` | **Mixed-window/Core-refresh/failure-injection/concurrency completion matrix — CLOSED after Phase 4L.2G real-PostgreSQL concurrency, replay and commit-ambiguity completion.** Per the phase's own explicit governance instruction ("may be CLOSED only if all four remaining capability groups close... if any group remains incomplete, keep this TD OPEN"), this historical row is now superseded by the Phase 4L.2G closure: all of (1) mixed-window restart, (2) Core-only/future-refresh restart, (3) the failure-injection/rollback matrix, or (4) the full concurrency/idempotency matrix close this phase. The phase's real contribution is a genuine finding, not a broad new passing-test matrix: real Postgres testing revealed that Phase 4L.2A's "Runway continuation restart" claim proved prescription/target-lock **identity reuse** but never verified the activation **window actually advances** on a second continuation call — adding that assertion showed it currently does not, a defect invisible until 4L.2A's own fix (making `ExistingRunwayPrescription` real instead of always-null) removed the masking effect of forced full regeneration on every call. Real GE→Runway mixed-window restart at the naturally-reachable 25/26/27-week horizons was also attempted and did not activate as expected (`SegmentsCovered` stayed `[GeneralEndurance]` only); the test file was removed rather than left failing or fabricated as passing, since the root cause could not be isolated within this phase's time budget and is very plausibly the same underlying issue as the continuation defect. No production code was changed; the full existing 46-test Long-Horizon persistence suite continues to pass, confirming no regression. See `PHASE4L_2B_MIXED_WINDOW_CORE_REFRESH_FAILURE_INJECTION_AND_CONCURRENCY_COMPLETION_MATRIX.md`. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **CLOSED** |
| `TD-LONG-HORIZON-RUNWAY-CORE-POSTGRESQL-RESTART-RECOVERY-MATRIX-001` | **Runway/Core PostgreSQL restart, recovery and transaction boundary matrix — narrow, honest scope (Phase 4L.2A).** Closes Phase 4L.2's "smoke-tested only" disclosure for pure first-Runway-entry, Runway continuation, Runway blocked/retry, and no-regeneration paths — every test runs against the real configured PostgreSQL database with a brand-new `AppDbContext`/connection per operation, never in-memory. Two real defects were found and fixed via this phase's own real-database testing: (1) the Runway target lock, full prescription, and calendar projection were **not being persisted with full fidelity at all** — `LongHorizonRunwayState` gained two bounded jsonb columns storing the real internal objects verbatim, and reconstruction now correctly restores them (previously always null after restart); (2) `LongHorizonCoreContextRecord`'s primary key used Phase 4K.8C's own cross-plan-deterministic `CoreContextId` directly, which **collided across different plans** with similar composition inputs — fixed by scoping the persisted row identity per-plan (`StableGuid(planId+coreContextId)`). **16 new tests, all passing**, proving: target lock/prescription/calendar reconstruct with full fidelity; continuation reuses the exact same lock/prescription (never regenerated); a real safety block during first Runway entry survives restart with zero false-activated sessions; retry-then-normal-activation succeeds after restart; three independent reconstructions produce byte-identical values; reconstruction never invokes `RuntimeConditionResolutionService`/`TenKPreparationRunwayDarkOrchestrator`/`PreparationRunwayNumericMaterializer`; tampered/corrupted rows fail closed; one real concurrency race has exactly one winner. **Explicitly does NOT close**: pure GE+Runway mixed-window restart (1+3/2+2/3+1), Core-only/future-refresh restart, the full 20-point transaction failure-injection matrix, the full concurrency matrix, canonical fingerprint hashing, and terminal-completion restart — all honestly disclosed as open rather than claimed complete. See `PHASE4L_2A_RUNWAY_CORE_POSTGRESQL_RESTART_RECOVERY_AND_TRANSACTION_BOUNDARY_MATRIX.md`. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **CLOSED** |
| `TD-LONG-HORIZON-ROLLING-PERSISTENCE-RESTART-SAFETY-001` | **Long-Horizon rolling persistence and restart-safe state contract implemented, dark (Phase 4L.2).** Eight new tables (`LongHorizonRollingPlanState` and children — deliberately NOT `TrainingPlans`/`TrainingDays` rows, since no `GoalType.LongHorizon` enum value exists and reusing `TrainingDay` would risk dark session leakage into live Home/Calendar/completion surfaces) durably store structural weeks, executable sessions, activation-window/checkpoint/block-retry audit records, and immutable Runway/Core ownership (bounded, versioned jsonb snapshots — Decision A, never regenerated on restart, since their generation is evidence-fed and byte-identical re-derivation isn't guaranteed). The GE structural skeleton (evidence-independent) is instead deterministically **regenerated** on reconstruction via the same unmodified `LongHorizonStructuralMaterializer`. Every one of **30 new tests runs against the real configured PostgreSQL database** (never EF InMemory), each "restart" proven by opening a brand-new `AppDbContext`/connection per operation — including two independent sequential restarts continuing real GE checkpoint activation, Postgres `xmin`-backed optimistic concurrency correctly rejecting a stale writer, deterministic idempotency keys preventing duplicate writes on replay, and fail-closed corruption detection (missing/duplicate week rows, Activated-without-sessions, unknown persistence version) with no auto-repair. Direct Blocked→Activated has no code path at all; retry requires a strictly later checkpoint date and a changed evidence fingerprint, both rejected otherwise and proven against real Postgres. Runway/Core JIT composition shares the same persistence adapter but was smoke-tested, not exhaustively restart-matrix-tested — disclosed, not claimed complete. Migration adds only new tables (zero `AddColumn`/`AlterColumn` against any existing table); applied locally. Zero endpoint, DI registration, background job, or Flutter file references any new type. See `PHASE4L_2_LONG_HORIZON_ROLLING_PERSISTENCE_AND_RESTART_SAFE_STATE_CONTRACT.md`. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **CLOSED** |
| `TD-LONG-HORIZON-PUBLIC-PREVIEW-CONTRACT-READINESS-001` | **Long-Horizon public preview contract approved, dark (Phase 4L.1).** `LongHorizonPublicPreviewMapper` maps real Phase 4K.9 dark lifecycle snapshots into a public-safe contract: a complete structural roadmap for all 21–52 weeks, the currently activated numeric window (at most 4 weeks, full session/date/volume detail from the real, unmodified `ActivatedNumericWeek`/Phase 4K.8D calendar projection), and explicit non-leaking `Pending`/`Blocked` rows for every future week — `LongHorizonStructuralRoadmapWeekContract` has no field capable of carrying a numeric/session value, so future-week leakage is structurally impossible, not just avoided by convention (proven by dedicated reflection-based leakage-guard tests). Direct repository inspection (not assumption) resolved every readiness question required by the phase: **confirmation is `NotReadyForConfirmation`** (the existing confirm flow requires a complete, fully-dated static schedule and throws `CatalogPreviewNotPersistableException` otherwise); **persistence requires new schema** (`TrainingWeek`/`TrainingDay` carry no `Pending`/`Blocked`/`Available` status field today); **restart/resume has nothing to reconstruct from** (no durable rolling-lifecycle state exists); the recommended API strategy is a **dedicated Long-Horizon response discriminator**, since `GeneratePreviewResponse`/`PreviewWeekDto`/`PreviewDayDto` are fully non-nullable and cannot represent a pending week without a breaking change. This phase's own real-snapshot testing found and fixed two genuine mapper bugs (the Phase 4K.9 harness leaves `CurrentWindow.Status` stale after a later checkpoint blocks; "currently executable"/"currently blocked" must instead be derived from `LifecycleStates`/`ActivatedWeeks` and the audit-event trail respectively) before landing at **37/37 new tests passing**. Zero endpoint, DI registration, confirmation code, migration, or Flutter file touched. See `PHASE4L_1_LONG_HORIZON_PUBLIC_PREVIEW_CONTRACT_AND_DARK_TO_PUBLIC_READINESS_REVIEW.md`. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **CLOSED** |
| `TD-LONG-HORIZON-FULL-DARK-LIFECYCLE-VALIDATION-001` | **Full 21-52 rolling dark lifecycle validation, explicit retry and boundary matrix (Phase 4K.9).** An internal supplied-evidence harness repeatedly invokes the existing Phase 4K.6 initial runtime, Phase 4K.7 checkpoint runtime and Phase 4K.8C composition chain (including inherited 4K.8 activation and 4K.8D calendar alignment). All 21-52 horizons complete without full-upfront execution; 20/53 reject. Explicit `NumericActivationBlocked -> NumericPending` retry requires a later checkpoint, changed evidence and new deterministic decision identity; direct Blocked-to-Activated remains illegal. Natural GE/Runway/Core boundary shapes, both profiles, Growth/Maintenance, three supported load levels, real pace-source paths, immutable Runway lock/prescription/calendar, future-only Core refresh, accumulated session dates, deterministic replay and derived loop safety are validated. Dark only: no persistence, preview, API, DTO, DI, background-job or Flutter wiring. | No | `TEN_K__4D__INTERMEDIATE v10` Long-Horizon rolling 21-52-week lifecycle only | **CLOSED** |
| `TD-LONG-HORIZON-ACTIVATED-SESSION-CALENDAR-PROJECTION-001` | **Real session-level calendar projection aligned with activated numeric weeks (Phase 4K.8D).** For selected executable Runway/Core weeks, `RealCompositionResult.CalendarComposition` is now the sole session-date authority. A mapping-only adapter preserves global week, structural slot ordinal, role, workout key/version, exact date/weekday, prescription/slice/target/context identity and provenance; it invokes no calendar composer or `WeekStartDate`. `ActivatedNumericWeek.CalendarDates` remains the separate structural week boundary, while each selected session reference receives the exact authoritative `AssignedDate`. The original full Runway calendar projection is immutable and reused across continuation slices; only selected Core weeks receive executable dates. Final alignment validates one-to-one identity, four distinct sessions, preferred/long-run day, structural bounds and mixed chronology before atomic success. Failure returns zero activated projection with one existing typed reason and an internal diagnostic. Dark only: no persistence, public DTO/API/DI/Flutter wiring. | No | `TEN_K__4D__INTERMEDIATE v10` Long-Horizon rolling Runway/Core only | **CLOSED** |
| `TD-LONG-HORIZON-JIT-REAL-CORE-CONDITION-CALENDAR-COMPOSITION-001` | **Real Core target, runtime condition and session-level calendar composition adapter implemented (Phase 4K.8C).** `LongHorizonRollingJitCompositionOrchestrator` (dark, unwired) closes Phase 4K.8's own disclosed gap: the Core Week-1 target, available Core weeks, and resolved condition results Phase 4K.8's request previously accepted as caller-fabricated inputs are now produced internally, for real, via the real unmodified `RuntimeConditionResolutionService` (all four production resolvers, real `CoreCycle`) and the real, unmodified `TenKPreparationRunwayDarkOrchestrator` reached through its own existing production factory (`TenKPreparationRunwayDarkOrchestratorFactory.Create`) — the same factory the live 15–20 week preview path already uses. `LongHorizonRollingCoreGenerationInputAdapter` maps checkpoint-validated rolling evidence into the exact legacy generation-request shape the unchanged Core pipeline requires (Phase 4K.5A's approved mapping). The Core Week-1 target is re-extracted via the exact unchanged `PreparationRunwayCoreWeekOneTargetAdapter`; bounded Core-week selection reads real `CatalogPrescribedSession` entries (real date + real distance together) directly from the real generated result. Core generation is skipped entirely for pure mid-Runway continuation windows (reusing the caller's existing locked target/prescription), never rerun needlessly. The composed real inputs are then handed unchanged to Phase 4K.8's own `ILongHorizonRollingJitActivationRuntime` — window selection, atomicity, the direction guard, target-lock validation, and bounded Runway slice selection all remain exclusively Phase 4K.8's own, never duplicated or redesigned. One shared try/catch gives atomic all-or-nothing composition-plus-activation. Honest disclosure: real per-session calendar dates ARE computed and available as provenance on the result (`RealCompositionResult.CalendarComposition`), but Phase 4K.8's own final `ActivatedNumericWeek.CalendarDates` field still uses the simpler, unmodified week-level `LongHorizonCalendarAssigner` assignment — this phase does not redesign that Phase 4K.8 authority. **18 new tests, all passing**, exercising the real on-disk catalog, real resolvers, and real Core generator end-to-end (never a fabricated shortcut). See `PHASE4K_8C_REAL_CORE_TARGET_RUNTIME_CONDITION_AND_SESSION_CALENDAR_COMPOSITION_ADAPTER.md`. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **CLOSED** |
| `TD-LONG-HORIZON-RUNWAY-CORE-JIT-RUNTIME-001` | **Runway and Core JIT numeric runtime and mixed-segment window activation implemented (Phase 4K.8).** `LongHorizonRollingJitActivationRuntime` (dark, unwired) resolves and atomically activates the next rolling window whenever it involves Preparation Runway and/or Core. Window selection: first contiguous run of `NumericPending` weeks, capped at 4; a pure GE-only window is out of this runtime's scope (Phase 4K.7's own) and blocks. Weekly/long-run authority is the caller's own `ValidatedSustainableLoad` (Phase 4K.5A) — onboarding and planned GE exit are never read. The runtime does not itself invoke the Core generator or condition-resolution service — like the real, unmodified `TenKPreparationRunwayCoreGenerationRequest`, it accepts the caller's own already-produced Core Week-1 target, available Core weeks, and resolved condition results as inputs, then locks/bounds/selects from them (disclosed explicitly). The Phase 4K.8B direction guard, immutable full Runway prescription (materialized exactly once via the unchanged `PreparationRunwayNumericMaterializer`), and bounded-slice factory are reused verbatim — no numeric formula, no downward interpolation. Four typed outcomes (`GeRunwayMixedWindowActivated`/`RunwayWindowActivated`/`RunwayCoreMixedWindowActivated`/`CoreWindowActivated`) plus `JitWindowBlocked`, all atomic: any validator failure anywhere yields zero newly activated weeks with prior history unchanged. Calendar continuity reuses the existing `LongHorizonCalendarAssigner.WeekStartDate` primitive rather than wiring the heavier DI-dependent Runway calendar composer — an explicitly disclosed simplification. Deterministic SHA-256 identities throughout, no random GUID/clock. **41 new tests, all passing**, including a genuine multi-window chain proving a Runway→Core mixed window arises naturally from a non-multiple-of-four Runway remainder. Full backend and plan-catalog suite results in the phase document. Full 21–52 lifecycle validation/retry (Phase 4K.9), persistence, public preview, API, and Flutter remain unimplemented. See `PHASE4K_8_RUNWAY_AND_CORE_JIT_NUMERIC_RUNTIME_AND_MIXED_SEGMENT_WINDOW_ACTIVATION.md`. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **CLOSED** |
| `TD-LONG-HORIZON-RUNWAY-BOUNDED-PRESCRIPTION-CONTRACTS-001` | **Preparation Runway direction guard and bounded prescription production contracts implemented (Phase 4K.8B).** Dark, unwired C# contracts in `RollingActivation/PreparationRunway/` (9 files) implement everything Phase 4K.8A required: `PreparationRunwayDirectionRelation`/`Comparer`/`Policy`/`Guard` (weekly/long-run below-or-equal-target conditionally supported, above-target unsupported, mapped to the existing `JIT_SEGMENT_TRANSITION_INFEASIBLE` reason, never a new one); `ImmutablePreparationRunwayPrescription<TKey>` wrapping the unchanged `PreparationRunwayNumericMaterializer` output with a deterministic SHA-256 identity (reusing the existing `LongHorizonRollingCheckpointRuntime.StableGuid` convention); `PreparationRunwayPrescriptionWeekReference<TKey>` that reads values directly from the real production week object, never recomputing them; a one-target-lock-per-full-range rule with a mid-Runway refresh guard; `BoundedPreparationRunwayPrescriptionSlice<TKey>` (1–4 weeks) produced only by a factory that structurally cannot invoke the numeric materializer and runs an exact-equivalence validator on every slice it builds; and a `PreSpecificTransition` terminal-stage invariant. 70 new tests, all passing, built from real `PreparationRunwayNumericMaterializer` results via the existing test fixtures — never a hand-built shortcut. Phase 4K.8's JIT orchestration/runtime remains entirely absent. See `PHASE4K_8B_PREPARATION_RUNWAY_DIRECTION_GUARD_AND_BOUNDED_PRESCRIPTION_CONTRACT_IMPLEMENTATION.md`. | No | `TEN_K__4D__INTERMEDIATE v10` Long-Horizon rolling Runway | **CLOSED** |
| `TD-LONG-HORIZON-RUNWAY-DOWNWARD-CONSOLIDATION-AUTHORITY-001` | **Runway direction authority reconciled with production (Phase 4K.8A).** The earlier 4K.4 claim that existing interpolation supports both directions is superseded: production rejects weekly start above Core target and every negative weekly change. Approved authority is upward-or-flat only, subject to all existing validators. Raw long-run evidence is separately normalized by the existing 0.30–0.36 preferred band, 0.40 cap and 0.5 km rounding; effective below/equal may proceed when validators pass, while effective above target fails closed. No downward percentage, Core-target lift, Taper/recovery reuse or formula change is approved. Existing `JIT_SEGMENT_TRANSITION_INFEASIBLE` is sufficient for future orchestration. | No | `TEN_K__4D__INTERMEDIATE v10` Long-Horizon rolling Runway | **CLOSED** |
| `TD-LONG-HORIZON-BOUNDED-RUNWAY-MATERIALIZATION-001` | **Full immutable Runway prescription approved as the single numeric authority with exact bounded exposure (Phase 4K.8A).** The unchanged 3–8-week materializer runs once at JIT entry against one immutable Core Week-1 target covering the complete Runway range. Rolling windows expose only exact 1–4-week references from that full prescription; local/global numbering and full interpolation coordinates never reset. `PreSpecificTransition` remains only the full terminal Runway week. Future computed weeks are internal/non-executable, create no `TrainingDay`, persistence, public DTO value or checkpoint evidence. No target refresh occurs mid-Runway; later non-overlapping Core-only refresh remains allowed. Phase 4K.8B must implement the narrow contract before the 4K.8 JIT runtime. | No | `TEN_K__4D__INTERMEDIATE v10` Long-Horizon rolling Runway | **CLOSED** |
| `TD-LONG-HORIZON-CHECKPOINT-NEXT-WINDOW-RUNTIME-001` | **Dark Checkpoint Evidence Aggregation, State Evaluation and Atomic Next GE Window Runtime Implemented (Phase 4K.7).** Internal `ILongHorizonRollingCheckpointRuntime.EvaluateAndActivateNextGeWindowAsync` aggregates only the most recently activated window's supplied real `TrainingDay` outcomes, derives completed-history weekly/long-run validated load, evaluates the approved safety-first Growth/Maintenance/Blocked table, and activates at most one contiguous 1-4-week GE window. Growth reuses existing GE progression; Maintenance uses the validated anchor with the existing 0.85 recovery and existing allocation/long-run rules. Identity/versioning and activation are deterministic and atomic. At the Runway boundary it returns a non-error handoff and invokes no Runway, Core, JIT context or target lock. The service remains dark, unpersisted, absent from public preview/API/DI/Flutter. See `PHASE4K_7_CHECKPOINT_EVIDENCE_AGGREGATION_STATE_EVALUATION_AND_NEXT_WINDOW_ACTIVATION_RUNTIME.md`. | No | `TEN_K__4D__INTERMEDIATE v10`; Race/exact-10K/Intermediate/4D/21-52 only | **CLOSED** |
| `TD-LONG-HORIZON-ROLLING-GE-INITIAL-RUNTIME-001` | **Dark Rolling General Endurance Initial Numeric Runtime Implemented (Phase 4K.6).** Internal `ILongHorizonRollingInitialActivationRuntime.BuildInitialActivationAsync` reuses the existing complete structural materializer and bounded GE numeric/session/calendar authorities. It activates only GE weeks `1..min(4, GE weeks)` from typed `InitialOnboardingActivation` evidence; every later GE, Runway, and Core week remains `NumericPending`. Partial 21/22/23-week horizons activate 1/2/3 GE weeks without Runway borrowing or synthetic recovery; real Week 4 retains the existing 0.85 recovery. Activation is atomic, deterministic, validator-gated, dark, unpersisted, and absent from API/public preview/DI/Flutter. Checkpoint aggregation, later-window activation, and Runway/Core JIT remain deferred. See `PHASE4K_6_ROLLING_GENERAL_ENDURANCE_NUMERIC_RUNTIME_AND_INITIAL_WINDOW_IMPLEMENTATION.md`. | No | `TEN_K__4D__INTERMEDIATE v10` specifically; no generalization is claimed | **CLOSED** |
| `TD-D3-001` | Before publishing/activating `TEN_K__4D__INTERMEDIATE v8` or any descendant, Process B/runtime mapping must be explicitly verified against `RUNTIME_CONDITION_VALUES_V1 v2` canonical values (`PACE_SOURCE_IN`, `TIME_ADEQUACY_IN`, `CORE_ENTRY_READINESS_IN`) and confirmed to no longer expect/emit the old v1 strings. | No | `v8` onward (inherited by `v9`, `v10`) | OPEN |
| `TD-WAVE5-001` | No automated cross-check exists between `ProgressionModifier.AllowGoalPaceRehearsal` and whether the reachable `WorkoutProgression` actually contains a goal-pace-rehearsal stage/candidate. As of D13, `TEN_K__4D__INTERMEDIATE v10` contains a real, reachable `GOAL_PACE_TEN_K v2` candidate gated only by `GOAL_FEASIBILITY_IN` — never by `AllowGoalPaceRehearsal`. Currently consistent (flag=true, candidate exists); confirm this remains true before activation, or add the recommended `TemplateCombinationValidator` cross-check if the flag is ever changed. | No | `v7` onward (inherited by `v8`, `v9`, `v10`) | OPEN (revisited in D13, not closed) |
| `TD-BACKEND-001` | Backend (`runner/backend`) has **zero integration** with plan-catalog — no code path loads any bundle/document, and the seeded database contains only 3 templates (all 5K/Beginner). The generation engine is explicitly a documented "Phase 1 placeholder" with "no real generation logic." A 10K/Intermediate/4-day request would silently fall back to an unrelated 5K/Beginner template today. **Supersedes/subsumes TD-D3-001 and TD-WAVE5-001** as the more fundamental blocker. | No | Any version (backend-side gap) | OPEN |
| `TD-REGISTRY-001` | **CORE_ENTRY_READINESS registry/fixture vocabulary mismatch.** The runtime condition registry defines `CORE_ENTRY_READINESS_IN` values as `READY`/`CAUTION`/`NOT_READY`, but the golden fixture decision trace contains `readiness="STANDARD"`. Also observed in earlier D3 follow-up work; independently rediscovered during Phase 4A Corrigendum. Classification: `REGISTRY_FIXTURE_CONFLICT`. Severity: `BLOCKS_RESOLVER_IMPLEMENTATION`. Affected areas: `RUNTIME_CONDITION_VALUES_V1 v2`, golden fixture decision trace, `CORE_ENTRY_READINESS_IN` resolver, future runtime generation. Required resolution: decide whether `STANDARD` is a legacy fixture-only value mapping to `READY`, or update the registry to include `STANDARD`, or update the fixture to use `READY`/`CAUTION`/`NOT_READY` — do not implement resolver behavior until resolved. | No | Any version (registry/fixture-level gap) | OPEN |
| `TD-PACESOURCE-001` | **PACE_SOURCE_IN ESTIMATED path is registry-valid but never emitted.** `PACE_SOURCE_IN.ESTIMATED` is a valid registry value, but `PaceSourceResolver` (Phase 4D.2) deliberately never emits it — no approved estimation methodology exists in-repo for deriving pace source from training-volume evidence alone. Users with `recentWeeklyVolumeKm`/`recentLongestRunKm`/`recentRunsPerWeek` but no recent race and no target time currently resolve to `NONE`. Classification: `DEFERRED_REGISTRY_VALUE_NOT_EMITTED`. Severity: `ACTIVATION_RISK`. Required resolution: approve and implement a V1 estimation methodology, or explicitly decide V1 will not use `ESTIMATED`, or confirm product/UX accepts `NONE` for this population — do not implement an unapproved method to close this risk. **Implementation note (Phase 4D.5.1, cross-risk finding):** if `ESTIMATED` is later implemented, or `PaceSourceResolver`'s output priority (`RECENT_RACE > TARGET_TIME > NONE`) changes for any other reason, re-verify `GoalFeasibilityResolver`'s `PACE_SOURCE_IN=NONE`-with-`targetFinishTimeSeconds`-present branch — it is currently unreachable through real `ResolveAll` orchestration (`PaceSourceResolver` already emits `TARGET_TIME` instead of `NONE` whenever a target time exists), a behavioral dependency, not a type-system guarantee. See `PHASE4D_5_1_PACESOURCE_GOALFEASIBILITY_CROSS_RISK_NOTE.md`. | No | Any candidate consuming `PACE_SOURCE_IN` | OPEN |
| `TD-PACESOURCE-002` | **AsOfDate preview/confirm lifecycle semantics not wired.** `PaceSourceResolver` computes recency metadata relative to `RuntimeResolverContext.AsOfDate` (an explicit evaluation-date field, never the wall clock), but no code in the live preview/confirm lifecycle populates it yet. Classification: `UNWIRED_CONTEXT_FIELD_LIFECYCLE_UNDECIDED`. Severity: `ACTIVATION_RISK`. Required resolution: decide whether confirm reuses the preview's `AsOfDate` or recomputes it independently, then wire that decision in when the resolver is eventually connected to generation — do not silently default to wall-clock time at confirm. **Implementation note (Phase 4E.1.1, governance clarification pass):** Phase 4E.1 computes `AsOfDate` once per preview and freezes it into `CatalogPreviewSnapshot.AsOfDate` — the preview-side half of the contract. None of this TD's three closure criteria are satisfied, since they are all about **confirm** (reuse-vs-recompute decision + wiring), and Phase 4E.1 deliberately does not wire catalog generation into confirm at all. Intended closure path: Phase 4E.2, which must read the frozen `AsOfDate` from the stored snapshot and make (and wire) an explicit reuse-vs-recompute decision. May remain OPEN even after Phase 4E.2 if that phase reuses the snapshot mechanically without an explicit, recorded product decision. **Addendum (Phase 4E.2 acceptance-finalization pass — appended, status NOT changed):** `CatalogPlanConfirmationService.BuildPlan` now reads `StartedAt` from `snapshot.AsOfDate` and never recomputes it from wall clock (`ConfirmedAtUtc` stays strictly separate/technical-only). **Resolved**: preview freezes `AsOfDate`; confirm code, where wired, does not recompute it; confirm does not rerun resolver orchestration; an immutable preview/confirm snapshot-consistency boundary exists in code. **Unresolved** (and outside this TD's own tracked scope): persistable stage-to-week schedule generation (this `BuildPlan` code path is unreachable in production — the step-11 persistability guard rejects every real snapshot first, so the reuse decision has never been exercised against real traffic); calendar-day alignment; production concurrent-confirmation safety (tracked separately as a Blocking Defect in `PHASE4E_2_CLAUDE_SAFETY_AUDIT.md` §15); public catalog activation. **Still NOT closed** — consistent with `TD-CORE-READINESS-001`'s own "implemented but not yet exercised against live traffic" precedent. See `PHASE4E_2_FINAL_ACCEPTANCE_REPORT.md`. | No | Any candidate consuming `PACE_SOURCE_IN` recency metadata | OPEN |
| `TD-CORE-READINESS-001` | **CORE_ENTRY_READINESS_IN thresholds are not approved** *(RESOLVED IN PART — see resolution note)*. Registry values (`READY`/`CAUTION`/`NOT_READY`) exist. Originally, no approved mapping from `recentWeeklyVolumeKm`/`recentLongestRunKm`/`recentRunsPerWeek` to those three values existed anywhere in the repo. Classification: `UNAPPROVED_THRESHOLD_MAPPING`. Severity: `ACTIVATION_RISK`. **Resolution note (Phase 4D.3.1):** product owner explicitly approved V1 thresholds as an internal canonical decision (`EV-008`): `READY` = weekly≥15 AND longest≥6; `NOT_READY` = weekly<8 OR longest<4 (checked first); `CAUTION` otherwise, including partial-evidence cases; both missing → `NOT_READY` only in a `GoalType=Race` context, else `NotEvaluated`. `recentRunsPerWeek` remains metadata-only, never a hard gate. `CoreEntryReadinessResolver` now implements this logic; `STANDARD` is still never emitted. **Not mechanically closed** (consistent with this file's convention — see `TD-WAVE5-001`'s own "revisited, not closed" precedent): the residual risk is that the resolver, though now implementing real owner-approved logic, remains unwired from live generation and untested against real traffic. Re-review before any future generation-wiring. | No | Any candidate consuming `CORE_ENTRY_READINESS_IN` | OPEN |
| `TD-EASY-WORKOUT-REGISTRY-001` | **Tier-1 Golden Fixture references `EASY_SHAKEOUT`, but no validated, versioned `EASY_SHAKEOUT` workout definition exists in the resolved v10 catalog bundle.** `EASY_WITH_STRIDES` shares the same divergence pattern (named only in architecture-spec example vocabulary and governance prose, never present in the fixture itself). Classification: `ACTIVATION_READINESS / CATALOG_FIXTURE_DIVERGENCE`. **Closure note (Phase 4F.6B):** closed via the third `requiredResolution` option — explicit reclassification, not renaming the fixture or adding the workout definition. Direct inspection of the fixture's `EASY_SHAKEOUT` occurrence (`W12_D3`) shows `raceEveShakeout=true` and `raceEveDistanceCapKm=4` — fields with no `EASY_STANDARD` equivalent at all, evidencing a specialized race-eve shakeout-run semantic, not a stylistic variant of a generic easy run. Renaming it to `EASY_STANDARD` would therefore be factually incorrect, not merely conservative. This mirrors the repository's own already-accepted precedent for `LONG_RUN_PROGRESSION` (`domain-wave1-schema-necessity-audit.md`: "a distinct, non-substitutable workout key, not evidence for `LONG_RUN_STANDARD` itself"). `EASY_SHAKEOUT` is now formally reclassified as illustrative of a distinct, richer, not-yet-built V1 workout concept — never evidence against the accepted `EASY_SUPPORT -> EASY_STANDARD` binding (D-C07). The fixture itself was **not** modified. `EASY_WITH_STRIDES` remains a separate, still-open (non-blocking) vocabulary question. | No | Any `TEN_K__4D__INTERMEDIATE` version (fixture/catalog-vocabulary-level gap) | **CLOSED** |
| `TD-GOAL-FEASIBILITY-001` | **`GoalFeasibilityResolver`'s `PACE_SOURCE_IN=TARGET_TIME` branch collapsed every target finish time into one `NotEvaluated`/`PACE_SOURCE_TARGET_TIME_NO_INDEPENDENT_EVIDENCE` outcome (→ HTTP 422), with no way to distinguish the product's own "go with average" canonical planning reference from an unverified user-typed performance claim.** Exposed by acceptance test SW-02 (a fully valid 10K/Intermediate/4-day/12-week product-average request rejected). Classification: `CONTRACT_GAP_BLOCKS_VALID_REQUEST`. **Closure note (Phase 4D.4.1):** a new required `target_finish_time_source` field (`product_average`/`user_defined`) was added to the public Race request contract and threaded through `ResolverInputSnapshot`. `GoalFeasibilityResolver` now returns `Evaluated`/`CHALLENGING`/`PACE_SOURCE_TARGET_TIME_PRODUCT_AVERAGE_ACCEPTED` for `product_average` (validated against `CanonicalTargetFinishTimePolicy` — a mismatched value is rejected with HTTP 400 before this resolver ever runs), while `user_defined` (and legacy/unset callers) keep the exact prior `NotEvaluated`/`PACE_SOURCE_TARGET_TIME_NO_INDEPENDENT_EVIDENCE` behavior unchanged. See `PHASE4D_4_1_PRODUCT_AVERAGE_TARGET_TIME_GOAL_FEASIBILITY_CLASSIFICATION.md`. | No | Any candidate consuming `GOAL_FEASIBILITY_IN` with a Race target time | **CLOSED** |
| `TD-FOUNDATION-COMPRESSION-001` | **Foundation Compression Readiness Gate Is Undefined.** For the current exact-8-week `TEN_K__4D__INTERMEDIATE` core (the only 8-week allocation the catalog's own minimums permit: `F2/B3/RS2/T1`), Foundation does not compress to 1 week — the active catalog minimum for `FOUNDATION` is 2, so this is not applicable to today's pilot (`N8_STATUS: NOT_APPLICABLE_DUE_TO_CATALOG_MINIMUM_2`). This does **not** resolve the general rule for future horizons or candidates where Foundation=1 may become structurally possible (`GENERAL_STATUS: OPEN_FOR_FUTURE_HORIZONS_OR_CANDIDATES_ALLOWING_FOUNDATION_1`): no repository artifact defines which `CORE_ENTRY_READINESS_IN` outcomes (`READY`/`CAUTION`/`NOT_READY`) would permit Foundation=1. The runtime already resolves `CORE_ENTRY_READINESS_IN` for every catalog-routed request and its result is already in scope at the phase-allocation call site, so the open issue is decision/contract ownership, not absence of the signal. Classification: `DECISION_REQUIRED`. Severity: `ACTIVATION_RISK`. Required resolution: before any future allocation permits Foundation=1, explicitly define whether `READY`/`CAUTION`/`NOT_READY` each permit it, which layer owns the decision, how the phase allocator consumes `CORE_ENTRY_READINESS_IN` without duplicating resolver evaluation, and the typed fail-closed result when Foundation=1 is structurally required but the runner is not eligible. See `PHASE4G_3A_EIGHT_WEEK_CORE_ALLOCATION_AUDIT.md` section 5a. **Closure note (Phase 4G.3B.9 — reachability proof, not a coaching threshold):** closed via structural/mechanical reachability proof, mirroring `TD-VOLUME-CAP-UNENFORCED-001`'s closure pattern — not by inventing a `READY`/`CAUTION`/`NOT_READY`-to-Foundation-weeks mapping, which was NOT invented and remains genuinely open scientifically. Re-verified this pass, not assumed: (1) `ReadinessEligibilityVerifierTests.Verify_RealFeasibleRange_OutcomeIsPass_FoundationNeverBelowCatalogMinimum`, re-run, exhaustively confirms `Pass`/zero violations for `targetWeeks` 8–14 against the real allocator and candidate; Foundation's exact allocated weeks per target (2,2,2,2,3,4,4) never reach 1. (2) A repository-wide grep for every `CoreEntryReadiness`/`CORE_ENTRY_READINESS` reference across all four backend production projects (16 files) found none that feed phase week-count allocation — `CatalogPhaseAllocationResolver.Resolve` (the sole phase allocator) takes no readiness parameter in either overload, and its own file contains zero readiness reference outside doc comments explicitly disclaiming one; `CatalogPlanConfirmationService`'s own doc comment explicitly lists `CoreEntryReadiness` among resolvers it must never call; `ProgressionStageAllocator`'s own `CoreEntryReadinessResolver.ConditionTypeValue` reference is a separate, downstream, workout-stage-eligibility allocator operating after weeks are already fixed — confirmed unrelated to phase week-count allocation. (3) `PhaseConstraintVerifier` (re-run) reports `Fail` unconditionally for any `AllocatedWeeks < MinimumWeeks` with no override path, and the allocator itself structurally cannot produce a below-minimum allocation — its compression loop's own `canAdjust` predicate makes it impossible, and any target below the sum of all minimums is rejected as infeasible before any distribution runs. Real current minimums re-verified against `ten-k-master.v6.json` (correcting an unverified `BUILD=2` assumption in this pass's own task prompt): `FOUNDATION=2, BUILD=3, RACE_SPECIFIC=2, TAPER=1`. Must be reopened if: (1) the minimum supported horizon drops below 8 weeks for any candidate, (2) phase minimums become soft/overridable, or (3) `CORE_ENTRY_READINESS_IN` begins influencing phase allocation in any future phase. A brief literature check found general supporting principle (base phases should not compress without limit; readiness affects safe compression) but no directly transferable numeric threshold — noted for context, not used as the closure basis. | No | Any `TEN_K__4D__INTERMEDIATE` version or descendant candidate whose future horizon-aware allocation would require `FOUNDATION` below its currently-active catalog minimum of 2 weeks — not applicable to the current exact-12-week pilot or the current exact-8-week audit finding | **CLOSED** |
| `TD-TESTFLAKE-001` | **Transient reset-endpoint HTTP 500s observed once during Phase 4G.3B.1 validation, not reproduced since.** Ten relational/reset-endpoint tests failed with HTTP 500 or a preview-row-count mismatch in one captured run: `ResetEndpointRelationalScenarioTests.ResetEndpoint_AllEightRequiredScenarios_SucceedWithoutFkViolationOrStaleState`; `Sw02ProductAverageEndToEndTests.Sw02Request_ReturnsHttp200_With12Weeks_4SessionsPerWeek_48TotalSessions_NoRestRows`; `Sw09ExplicitZeroReadinessEndToEndTests.Sw09Request_ExplicitZeroReadiness_IsNotRejectedByTransportValidation`; `Sw12LongHorizonFailClosedEndToEndTests.VerifiedRegressionCase_NoLegacyFallback_NoPreviewOrPlanPersistence`; `Sw12LongHorizonFailClosedEndToEndTests.InRangeBoundaryHorizon_Returns422_PlanCoreHorizonUnsupported_NotHttp200(weeks: 8)`; the same test with `weeks: 14`; and `Sw12LongHorizonFailClosedEndToEndTests.UnsupportedLongHorizon_Returns422_PlanHorizonCompositionRequired` with `weeks: 16`, `24`, `15`, and `20`. They did not reproduce in two subsequent clean full-suite runs. Root cause is unknown. They are not attributable to Phase 4G.3B.1's `PlanCatalogBundleLoader.cs` or `PlanCatalogCandidateSummary.cs` changes, which are unrelated to the reset endpoint and relational test infrastructure. Classification: `UNVERIFIED_TRANSIENT_FAILURE`. Severity: `NON_BLOCKING_OBSERVATION`. Required resolution: if this pattern reoccurs, investigate shared PostgreSQL test-database state/reset-ordering before assuming it is safe to ignore again. | No | Repository-wide relational validation infrastructure; not candidate-version-specific | OPEN |
| `TD-ALLOCATION-PRIORITY-001` | **Phase compression and extension priorities remain unconfirmed catalog-authoring placeholders.** `TEN_K_MASTER v6` carries priorities `1/2/3/4`, but AUD-008 classifies the combined field as `PLACEHOLDER_UNCONFIRMED`: the ordering was invented and has no canonical product/coaching source; the 12-week golden fixture exercises neither compression nor extension. Phase 4G.3B.2 transports and mechanically applies the authored values without claiming approval. `AllocationOrderCorrectnessVerifier` reports `DECISION_REQUIRED` for priority-dependent feasible non-preferred allocations, except the order-independent 8- and 14-week fully-exhausted boundaries. Classification: `CATALOG_AUTHORING_DECISION_REQUIRED`. Severity: `BLOCKS_NON_PREFERRED_ALLOCATION_SAFETY_APPROVAL`. Required resolution: explicitly confirm or replace compression and extension priorities independently through the versioned catalog-authoring process and update AUD-008 before treating priority-dependent allocations as verified safe. **Closure note (Phase 4G.3B.8/4G.3B.8.1 — Extension Priority Decision Audit and independent coaching-literature evidence application):** Phase 4G.3B.8's decision audit re-derived from `TEN_K_MASTER v6`'s real phase bounds that only `targetWeeks=13` is genuinely extension-order-dependent (`RACE_SPECIFIC` has zero extension headroom; `targetWeeks=14` is a fully-exhausted, order-independent boundary regardless of priority, re-verified unchanged by this pass). That audit found reversed extension order structurally impossible and left Foundation-first extension without independent evidence. This pass supplies that evidence: independent coaching-literature sources (Joe Friel and Stephen Seiler, via Roadman Cycling, corroborated directionally by Triathlete.com and COROS) converge on build-phase duration being comparatively fixed while base/foundation-phase duration is elastic and absorbs available extra time — supporting Foundation-first extension priority on independently evidenced grounds, not by default inheritance from compression order. Evidence classification: `EVIDENCE_INFORMED` (coaching-system consensus, not peer-reviewed RCT literature) — not upgraded to `EVIDENCE_BACKED`. No catalog data change was required: `ten-k-master.v6.json`'s `compressionPriority`/`extensionPriority` were already `1/2/3/4` for both fields; this decision confirms those existing values as deliberate and now-evidenced rather than an unconfirmed placeholder, recorded here rather than as a new inline catalog field (no rationale-annotation field exists in `plan-template.schema.json`; this repository's established convention records this kind of decision in governance documentation, not inline catalog data — see `TD-VOLUME-CAP-UNENFORCED-001`'s own closure). `AllocationOrderCorrectnessVerifier.Verify`'s logic was **not** modified (out of scope for this pass): it still reports `DecisionRequired` for `targetWeeks=13` unconditionally, because its current design only ever produces `Pass` for structurally order-independent allocations and has no mechanism to consume a resolved-but-still-order-dependent decision — a known, unaddressed gap for a future pass. The existing test `Resolve_InRangeNonPreferredTargets_MatchExactPriorityOrderedAllocation` already independently confirms, unchanged, that the real allocator's output at `targetWeeks=13` is `FOUNDATION=4/BUILD=4/RACE_SPECIFIC=4` (Foundation extended first) and at `targetWeeks=14` is `FOUNDATION=4/BUILD=5/RACE_SPECIFIC=4` — Foundation-first was already the allocator's actual behavior; this closure confirms it as evidenced, not as a behavior change. Scoped to `TEN_K_MASTER v6`'s current phase bounds and the `TEN_K__4D__INTERMEDIATE` candidate specifically; does not generalize to a future candidate/catalog revision with different bounds. | No | `TEN_K__4D__INTERMEDIATE v10` and descendants retaining `TEN_K_MASTER v6`; revisit independently for other templates | **CLOSED** |
| `TD-NOTEVALUATED-FALLBACK-001` | **NotEvaluated Resolver Results Can Route to Fallback Stages Without Confirmed Product Approval — scope corrected by real E2E characterization (Phase 4G.3B.6.1).** `ProgressionStageAllocator.IsStageEligible` computes `satisfied = result.Status == Evaluated && result.OutputValue is not null && condition.AllowedValues.Contains(result.OutputValue)`; a `NotEvaluated` result and an `Evaluated` result whose output isn't in the stage's allowed values both make `satisfied=false`, and `ResolveEligibility` walks `FallbackStageKey` identically either way — this mechanism still exists unchanged. Discovered during Phase 4G.3B's `StageReachabilityVerifier` work (which deliberately delegates all stage-selection logic to the real `ProgressionStageAllocator` rather than reimplementing it). Structurally the same category of gap as AUD-008 / `TD-ALLOCATION-PRIORITY-001`. **Scope correction (Phase 4G.3B.6 / 4G.3B.6.1):** Phase 4G.3B.6 (source audit) correctly established that `GOAL_FEASIBILITY_IN` can resolve `NotEvaluated`/`PACE_SOURCE_TARGET_TIME_NO_INDEPENDENT_EVIDENCE` for a real, currently-reachable live-12-week-pilot input (`TargetFinishTimeSource=UserDefined`, no complete `RecentRace`), but inferred from the allocator's logic in isolation that this would silently reach `CURRENT_FITNESS_SPECIFIC_REHEARSAL`. Phase 4G.3B.6.1 corrected that downstream inference via a real HTTP end-to-end test through `POST /api/v1/plans/generate-preview/race` (real Api host + real Postgres): `CatalogPreviewGenerator.ApplyNotEvaluatedGovernancePolicy` classifies this reason as `Unsupported` and throws `RuntimeConditionUnsupportedException` — observed result is **HTTP 422 `RUNTIME_CONDITION_UNSUPPORTED`**, with no stage allocation, fallback stage, workout binding, dated schedule, or persistence, confirmed **before** `ProgressionStageAllocator`/stage scheduling is ever reached. Silent runtime fallback is therefore **not currently reachable** for this production trigger — the resolver-reachability finding stands; the downstream silent-fallback prediction is disproved. The comparable `ProductAverage` control is unaffected (`Evaluated`/`CHALLENGING`, HTTP 200, 12 weeks/48 sessions, `fallback_used=false`). **Real, corrected impact:** a real subset of the already-live 12-week `TEN_K__4D__INTERMEDIATE` pilot (custom target time, no qualifying recent race) is hard-blocked from generating any plan at all today, with an undifferentiated 422 and no actionable guidance — not merely a future 8–14-week activation concern, and not a silent quality downgrade as originally described. Separately, the offline `GoalPaceReachabilityVerifier` still unconditionally evaluates a synthetic universal `NotEvaluated` check on every call, which is why `RaceCoreSupportRegistry` (Phase 4G.3B.5) still reports `DecisionRequired` for every real 8–14-week target including the live 12-week pilot — that offline finding remains valid on its own terms and is not disproved by this runtime characterization. Classification: `DECISION_REQUIRED`. Severity: `ACTIVATION_RISK`. Required resolution: (1) product/UX decision on whether the current hard-block is intended behavior and what actionable guidance should be shown; (2) decision on whether the offline verifier should distinguish runtime-rejected `NotEvaluated` reasons from stage-reachable fallback outcomes; (3) independently, whether any `NotEvaluated` reason that DOES reach the allocator uncontested should keep silently falling back or instead block/flag the week — do not change `ProgressionStageAllocator`, `CatalogPreviewGenerator`'s governance policy, or `NotEvaluatedReasonClassifier` without an explicit decision recorded first. **Semantics clarification addendum (Phase 4G.3B.6.4 / 4G.3B.6.5):** runtime safety for this risk's originally identified trigger (`PACE_SOURCE_TARGET_TIME_NO_INDEPENDENT_EVIDENCE`) is confirmed — the request fails closed with HTTP 422 before stage scheduling (Phase 4G.3B.6.1). `GoalPaceReachabilityVerifier`'s `DecisionRequired` classification (propagating to `RaceCoreSupportRegistry` for all 7 target week counts, including the live 12-week pilot) reflects an unproven universal theoretical catalog-contract completeness claim, not an observed runtime safety gap — now documented in `PHASE4G_3B_6_4_GOALPACE_VERIFIER_RUNTIME_REACHABILITY_SEMANTICS_AUDIT.md` and reflected in the verifier/orchestrator/registry documentation. Additionally: the currently-shipped Flutter client does not yet call the real `generatePreview()` endpoint (a mock shortcut is active), so no real user is exposed to either the ProductAverage or UserDefined path today — recorded as current-impact context, not resolution. Option C (splitting universal-completeness from runtime-reachable-safety as two distinct verifier outputs) remains explicitly deferred until a real consumer of that distinction exists — see `PHASE4G_3B_5_SUPPORT_REGISTRY.md`'s "Future Option C trigger conditions" section. This TD remains OPEN; open decisions 1–3 from the Phase 4G.3B.6.2 correction remain unresolved and unchanged by this addendum. **Phase 4G.5G generalization confirmation:** the new dark `DynamicCoreSessionPrescriptionOrchestrator` re-exercised the downstream fallback mechanism across 7 target week counts (8–14) and 5 pace-evidence categories (including NotEvaluated and Evaluated/`UNSUPPORTED` `GOAL_FEASIBILITY_IN`) — 35 combinations, real unmodified `ProgressionStageAllocator`/`CatalogSessionPrescriptionPlanner`. Confirmed in every case: `GOAL_PACE_TEN_K` never appears when goal feasibility is `NotEvaluated`/`UNSUPPORTED` — the existing allocator fallback already routes away from it before the prescription layer is reached, so `CatalogSessionPrescriptionPlanner`'s own fail-closed guard is never exercised because the unsafe combination it guards never arises. Confirming evidence only; this TD's own open decisions are unchanged, and this dark orchestrator does not itself alter the live pilot's confirmed HTTP 422 behavior. See `PHASE4G_5G_DYNAMIC_CORE_PACE_PRESCRIPTION.md`. | No | Any candidate whose workout progression contains a conditional stage with a `FallbackStageKey` and consumes a resolver capable of returning `NotEvaluated` — not specific to a single candidate version. Confirmed reachable today against the live 12-week `TEN_K__4D__INTERMEDIATE` pilot for the UserDefined/no-RecentRace combination (Phase 4G.3B.6.1); not limited to future 8–14-week activation. | OPEN |
| `TD-VOLUME-CAP-UNENFORCED-001` | **HardMaxWeeklyIncreaseRatio and AbsoluteWeeklyIncrementCapKm Are Documented Policy Fields Never Enforced by the Real Weekly-Volume Curve.** `CatalogVolumeAndLongRunPlanner.BuildWeeklyPlan` generates every non-taper week via linear interpolation to a transition-count-scaled reachable peak (`ResolvePeak`). `VolumeSafetyPolicy.HardMaxWeeklyIncreaseRatio`/`AbsoluteWeeklyIncrementCapKm` exist as typed policy fields and are threaded into `ReachablePeakDecision`, but only as decision-trace provenance metadata — neither is ever read back to clamp or reject an actual week-to-week transition anywhere in the real curve-generation code. `VolumeProgressionVerifier` independently confirmed, for the real `TEN_K__4D__INTERMEDIATE` candidate across all 7 feasible target week counts (8–14), that every generated non-taper transition currently stays within both bounds anyway — but this is a property of this candidate's specific starting-volume/peak interpolation shape, not a guarantee actively enforced by the planner for any future candidate, catalog revision, or target week count. If a future candidate/catalog combination produced a starting volume and peak whose linear interpolation would exceed either bound, nothing in the current code path would catch or prevent it — the two fields would remain silently decorative. Architecturally different from `TD-FOUNDATION-COMPRESSION-001`/`TD-ALLOCATION-PRIORITY-001`/`TD-NOTEVALUATED-FALLBACK-001` (which document undecided product questions about behavior that does execute): this documents a safety mechanism that exists in code as a typed contract but is not wired to enforce anything today — closer to a silent gap than an open decision. Classification: `DECISION_REQUIRED`. Severity: `ACTIVATION_RISK` (see this phase's report for why no more precise existing severity value was found, and a proposed-but-not-applied new value). Required resolution: a product/engineering decision on whether these two fields should become an actively-enforced per-transition cap inside `CatalogVolumeAndLongRunPlanner.BuildWeeklyPlan`, or be explicitly reclassified in `VolumeSafetyPolicy`'s own documentation as informational/provenance-only — resolved, or explicitly accepted as informational-only with documented rationale, before this planner is trusted beyond the already-verified current 8–14-week `TEN_K_MASTER v6` range; do not silently start enforcing or silently continue treating these fields as decorative without this decision being recorded first. **Closure note (Phase 4G.3B.7/4G.3B.7.1 — Volume-Cap Enforcement Decision Audit and Option B application):** Phase 4G.3B.7's decision audit proved algebraically that for `TEN_K_MASTER v6`'s current constants, the per-transition volume-increase ratio required to reach the catalog peak within the fixed non-taper transition count is independent of starting volume and target week count, and stays structurally below both `HardMaxWeeklyIncreaseRatio` and `AbsoluteWeeklyIncrementCapKm` by construction — not by coincidence of the specific values tested. Enforcing these caps as active per-transition clamps (Option A) was evaluated and found to require redesigning the reachable-peak model with zero benefit to current behavior, since the caps are structurally unreachable today. Resolution: `HardMaxWeeklyIncreaseRatio`/`AbsoluteWeeklyIncrementCapKm` are now explicitly documented as informational/provenance-only fields (Option B) in `VolumeSafetyPolicy.cs`, `PHASE4G_3B_0_VOLUME_SAFETY_POLICY_GOVERNANCE_NOTE.md`, and `VolumeProgressionVerifier.cs` — not active runtime safety gates. No planner algorithm, numeric value, or verifier check logic was changed. This closure is scoped to `TEN_K_MASTER v6`'s current constants — see `PHASE4G_3B_7_VOLUME_CAP_ENFORCEMENT_DECISION_AUDIT.md` for the exact conditions (a future candidate/catalog revision with different starting-volume/peak-volume/core-cycle constants) that would require re-running this analysis before treating a new candidate's curves as safe under the same assumption. | No | Any candidate whose weekly-volume curve is generated by `CatalogVolumeAndLongRunPlanner` — not specific to a single candidate version; verified safe only for the current `TEN_K__4D__INTERMEDIATE` 8–14-week range | **CLOSED** |
| `TD-RACEDATE-CHECK-NOT-HORIZON-AGNOSTIC-001` | **Live Race-Date Alignment Guard Combines an Exact-12-Week Check with Date Tolerance, Contrary to the Prior Horizon-Agnostic Characterization.** `CatalogPreviewGenerator`'s live `CatalogRaceDateAlignmentInvalidException` guard (in `BuildDarkInternalDatedSkeleton`) threw if `datedSkeleton.Weeks.Count != RaceHorizonPolicy.ExactStandaloneCoreSupportedWeeks` (currently 12) **OR** `daysBeforeRace < 0` **OR** `daysBeforeRace > 7`, where `daysBeforeRace = RaceDate.DayNumber - datedSkeleton.EndDate.DayNumber`. The date component alone accepts a final session when `0 <= RaceDate - FinalSessionDate <= 7` days. The standalone `RaceDateAlignmentVerifier` independently confirmed, via its own test suite against the real generated 8–14-week `TEN_K_MASTER v6` schedules, that this date-tolerance rule succeeds on its own for every one of those week counts (`FinalSessionDate = RaceDate - 1` in every case) — but the combined live guard would nevertheless reject every one of those schedules purely on the `Weeks.Count` clause, even with perfectly aligned dates. `PHASE4G_3A_EIGHT_WEEK_CORE_ALLOCATION_AUDIT.md` section 15 characterized the complete live invariant as horizon-agnostic ("ready for Phase 4G.3B as-is... no code change needed") — that characterization examined only the date-comparison formula and did not account for the `Weeks.Count != 12` clause joined to it in the same live condition, so it was inaccurate/incomplete as a claim about the complete guard. Classification: `DECISION_REQUIRED`. Severity: `ACTIVATION_RISK`. **Closure note (Phase 4G.3B.4a — Separate Horizon-Support Enforcement from Race-Date Alignment Validation):** resolved via requiredResolution option (A). A production call-path audit first proved the upstream guarantee: the only week count that can ever reach this guard in production is exactly 12 — every other classification (`BelowMinimum`, `CoreLengthRecognizedButNotImplemented`, `CompositionRequired`) is independently rejected upstream by both `PlanServices.GeneratePreviewAsync`'s horizon guard and `LivePlanPreviewRouting`'s route decision before catalog generation is reached. With that proven, the `Weeks.Count != ExactStandaloneCoreSupportedWeeks` clause (and its supporting `expectedWeekCount` local) was removed from the live guard, which now validates date-tolerance alignment only. Standalone week-count support remains owned exclusively by `RaceHorizonPolicy`/the preview-routing layer, unchanged: 8–11 and 13–14 weeks still return HTTP 422 `PLAN_CORE_HORIZON_UNSUPPORTED`, 15+ weeks still return HTTP 422 `PLAN_HORIZON_COMPOSITION_REQUIRED`, and 12 weeks still returns HTTP 200 with unchanged, semantically-verified public behavior. No new horizon was enabled. New focused tests (`Phase4G3B4AHorizonGateAlignmentGuardSeparationTests`) prove the narrowed guard still rejects genuine date misalignment, that a real correctly-aligned non-12-week (8-week) internal skeleton is no longer rejected solely for its week count (internal-ownership-only — the public 8-week route remains rejected by `RaceHorizonPolicy`), and that the guard's own source block no longer references `Weeks.Count` comparisons, `ExactStandaloneCoreSupportedWeeks`, or any exact-12 constant. The standalone `RaceDateAlignmentVerifier` was not modified and remains dark (zero production call sites, no DI registration, not invoked by the refactored guard). The section 15 correction addendum in `PHASE4G_3A_EIGHT_WEEK_CORE_ALLOCATION_AUDIT.md` is preserved unchanged. See `PHASE4G_3B_4A_HORIZON_GATE_ALIGNMENT_GUARD_SEPARATION.md`. | No | Any candidate/request path reaching `CatalogPreviewGenerator.BuildDarkInternalDatedSkeleton`'s race-date-alignment guard — not specific to a single candidate version | **CLOSED** |
| `TD-RUNWAY-VALIDATOR-EXHAUSTIVENESS-001` | **Preparation Runway Validators Only Enforce CompositionType/RunwayDays Consistency for NotApplicable Status.** `CROSS_PHASE_4G_4A_TO_4G_4B_V_AND_BACKEND_BASELINE_INDEPENDENT_AUDIT.md` independently found, via a source-level adversarial trace performed in that pass (not by re-running Phase 4G.4B's own test suite), that `PreparationRunwayValidators` only checks `CompositionType`-vs-`RunwayDays` internal consistency when `Status == NotApplicable`: `PreparationRunwayPlanningResultValidator`'s cross-check at that status is the only place any validator compares `RacePlanCompositionMetadata.CompositionType` against `RunwayDays`/`FullRunwayWeeks`. A `PreparationRunwayPlanningResult` with `Status == DecisionRequired`, `Unsupported`, or `InvalidInput` could therefore carry self-contradictory composition metadata — for example a `CompositionType` of `StandaloneCore` paired with a nonzero `RunwayDays` value, or `PreparationRunwayPlusCore` paired with `RunwayDays == 0` — without any validator catching it. Currently non-blocking: `PreparationRunwayContracts`/`PreparationRunwayValidators` are entirely dark and unwired to any live path, per Phase 4G.4B/4G.4B.V's own confirmed scope (zero production call sites outside the dedicated contract folder, no DI registration, no endpoint, no public DTO). This becomes a real risk the moment a future phase (4G.4C or later) begins constructing `PreparationRunwayPlanningResult` instances from real allocator logic and consumes their `Status`/metadata without independently re-validating consistency. Classification: `DECISION_REQUIRED`. Severity: `ACTIVATION_RISK`. Required resolution: before Phase 4G.4C (or whichever phase first implements a runway allocator producing real `PreparationRunwayPlanningResult` instances) is considered complete, extend `PreparationRunwayValidators` to enforce `CompositionType`/`RunwayDays` consistency across ALL statuses, not only `NotApplicable` — or explicitly document, with reasoning, why the other statuses are exempt if that turns out to be intentional; cross-reference `CROSS_PHASE_4G_4A_TO_4G_4B_V_AND_BACKEND_BASELINE_INDEPENDENT_AUDIT.md` section 6 when this risk is next revisited; do not silently start enforcing full-status consistency, or silently continue treating the `NotApplicable`-only check as sufficient, without this decision being explicitly recorded first. **Closure note (Phase 4G.4B.1):** `PreparationRunwayContextValidator.Validate` now enforces a general, status-independent `CompositionType`/`RunwayDays` consistency rule — `CompositionType==PreparationRunwayPlusCore` requires `RunwayDays > 0`, and any other `CompositionType` requires `RunwayDays == 0` — matching the exact binary convention every existing test helper already used to construct consistent inputs. Because `RacePlanCompositionMetadataValidator.Validate` is called unconditionally by `PreparationRunwayPlanningResultValidator.Validate` for every `Status`, this single check now applies identically to `Planned`, `DecisionRequired`, `Unsupported`, `InvalidInput`, and `NotApplicable` alike — closing the gap without status-specific duplication. The pre-existing `NotApplicable`-specific check was left untouched and re-confirmed still passing. New tests reproduce the audit's own literal adversarial example (`CompositionType=StandaloneCore` with `RunwayDays=7`, wrapped in a `DecisionRequired` result) plus the same mismatch under `Unsupported`/`InvalidInput`, plus the reverse-direction mismatch (`PreparationRunwayPlusCore` with `RunwayDays==0`) — all now correctly rejected. No allocator, materializer, composer, or live-path code was touched. | No | Any future candidate whose plan generation would use Preparation Runway composition (currently only theoretical 15+ week horizons under `PLAN_HORIZON_COMPOSITION_REQUIRED`); not applicable to any currently-supported horizon or the current exact-12-week pilot, which never constructs these types | **CLOSED** |
| `TD-COREHORIZON-ALLOCATOR-UNWIRED-001` | **CoreHorizonDecision/CoreHorizonMode Is Not Mechanically Consumed by CatalogPhaseAllocationResolver.** A repo-wide grep for `CoreHorizonDecision` and `CoreHorizonMode` usage outside `CoreHorizonClassifier.cs`'s own file found zero production references, including inside `CatalogPhaseAllocation.cs` (the real `CatalogPhaseAllocationResolver.Resolve` phase-week-count allocator). The only non-classifier reference anywhere in the repository is a single dark-reachability test in `PreparationRunwayContractsTests.cs` (`DarkReachability_ClassifierVocabularyIsAllowedWithoutContractConsumption`), which exists to prove the classifier's `PreparationRunwayPlusCore` enum vocabulary is not contract consumption — it is not a production integration point. `CatalogPhaseAllocationResolver.Resolve(candidate, targetWeekCount)` is unchanged since Phase 4G.3B.2 and derives its own `AllocationMode` (`Compression`/`Preferred`/`Extension`) purely from `targetWeekCount` minus the sum of preferred phase weeks — a week-count-only concept that does not read, invoke, or duplicate `CoreHorizonClassifier`'s day-accurate `CoreHorizonMode` classification (`Unsupported`/`ReadinessOnly`/`CompressedCore`/`PreferredCore`/`ExtendedCore`/`PreparationRunwayPlusCore`/`InvalidInput`) in any way. The two components are independent and currently non-contradictory — neither silently duplicates or overrides the other's decision — but no orchestrator or call path currently connects the classifier's output to the allocator's input. This was confirmed during Phase 4G.5C's validation of Phase 4G.5A/4G.5B. Classification: `UNWIRED_COMPONENT_INTEGRATION_UNDECIDED`. Severity: `NON_BLOCKING_OBSERVATION`. **Closure (Phase 4G.5J/4G.5K):** the explicit upstream-gate architecture now makes `CoreHorizonDecision` the day-accurate eligibility authority and supplies `AvailableFullWeeks` as the allocator target; `CatalogPhaseAllocationResolver` owns phase distribution only, and `CatalogPreviewGenerator` is the single live composition point. Partial days are never rounded into an extra allocated week. Reopen if another live caller derives the target independently or allocation begins interpreting partial days. See `PHASE4G_5C_THIRTEEN_WEEK_EXTENSION_DECISION.md` section 5 and `PHASE4G_5C_ADDENDUM_COREHORIZON_WIRING_GAP_TD.md`. | No | Any candidate whose future generation pipeline would need to combine day-accurate horizon classification with phase-week-count allocation — not specific to any single catalog candidate version | CLOSED |
| `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` | **21-52 Week Staged Single-Plan Architecture (General Endurance → Preparation Runway → Race Core) Is Approved Direction, Not Implemented Capability.** For the current `TEN_K__4D__INTERMEDIATE v10` pilot, 21-52 available full weeks are approved to become, in a future phase, a single user-visible Race `TrainingPlan` whose internal segment provenance rolls through `GENERAL_ENDURANCE` → `PREPARATION_RUNWAY` → `RACE_CORE`, materialized incrementally rather than frozen at initial confirmation, with a time-triggered (not evidence-blocking) transition into the race-preparation window. This is recorded as approved architectural direction (`STAGED_SINGLE_PLAN`) and an approved-but-unproven direction for reusing the existing Habit-style rolling planner (`ROLLING_GENERAL_ENDURANCE` / `HABIT_STYLE_PLANNER_REUSE`), not as implemented or activated capability. No General Endurance objective semantics, workout-role policy, volume/long-run progression, deload/maintenance policy, or rolling-materialization mechanism exists anywhere in the repository today. Classification: `RESEARCH_REQUIRED`. Severity: `ACTIVATION_RISK`. Required resolution: product/coaching research and an explicit decision on General Endurance's own objective and workout-role policy before any allocator/contract/schema work begins; explicit decisions on rolling-materialization slice length/idempotency/duplicate-week prevention, segment-provenance persistence shape, and transition orchestration (retry/failure, missing-evidence handling via the separately-approved missing-evidence policy) before any migration is authored; do not implement a Preparation Runway allocator, General Endurance contracts/services, Habit-planner reuse, staged materialization, a composition scheduler, a transition service, `TrainingPlan` composition status, `TrainingWeek` `SourceSegment`, segment-provenance schema, or any database migration until those decisions are recorded; do not activate 21-52 week public behavior or claim these classifications as implemented until genuinely built and tested. See `PHASE4G_6A_1_TEN_K_HORIZON_POLICY_AND_DATE_AUTHORITY.md`. | No | Any candidate/distance family whose available full-week horizon could reach 21-52 weeks — not specific to a single candidate version; currently only theoretical for `TEN_K__4D__INTERMEDIATE` since no public path reaches 15+ weeks at all | **OPEN** |
| `TD-LONG-HORIZON-COMPOSITION-001` | **21-52 Week Product Composition and Allocation Formula Approved (Long-Horizon General Endurance / Preparation Runway / Core Split) — Formula Only, Runtime Activation Remains Blocked by `TD-GENERAL-ENDURANCE-STAGED-PLAN-001`.** Phase 4I.1 approved, for `21 <= AvailableFullWeeks <= 52`: `LongHorizonGeneralEnduranceWeeks = AvailableFullWeeks - 20`, `PreparationRunwayWeeks = 8` (fixed, unchanged from the already-approved 20-week maximum), `CoreWeeks = 12` (fixed, unchanged canonical value). This is the forced arithmetic consequence of two facts Phase 4G.6A.1 already approved — `MaximumRunwayWeeks=8` and `PreferredCoreWeeks=12` — combined with the required 20-to-21-week continuity invariant (one additional available week adds exactly one Long-Horizon General Endurance week; it must not shrink the already-maximum 8-week runway or the fixed 12-week core). Classification: `COMPOSITION_POLICY_DECISION`. Severity: `GOVERNS_FUTURE_RUNTIME_ACTIVATION_ONLY_NO_CURRENT_IMPACT`. **Closure note (Phase 4I.1):** 1-3 GE weeks classify `ShortExtension` (orientation/continuity role only, not a claim of complete physiological adaptation); 4-32 GE weeks classify `FullPhase`. Both readiness profiles (`CONSISTENCY_NEEDED`, `CORE_ENTRY_READY`) use identical GE/Runway/Core durations — profile affects content only (the already-approved 8-week runway allocations from Phase 4G.6A.2 are unchanged), never duration. 53+ remains rejected as a V1 product/system support boundary (not a safety claim), reusing the already-recorded `PLAN_HORIZON_EXCEEDS_SUPPORTED_WINDOW` reason name from Phase 4G.6A.1 rather than inventing a second name for the same unimplemented concept. This decision approves the composition envelope only — it explicitly does **not** approve mesocycle structure, recovery/deload cadence, progression caps, long-run progression, workout repetition limits, or any other runtime safety policy, all deferred to Phase 4I.2 under `TD-GENERAL-ENDURANCE-STAGED-PLAN-001`, which remains `OPEN` and unaffected in scope by this closure. See `PHASE4I_1_21_TO_52_WEEK_PRODUCT_COMPOSITION_AND_ALLOCATION_POLICY_RESOLUTION.md` for the full 20/21/22/23/24/28/32/40/48/52/53 fixture table and the typed decision-contract shape recorded for Phase 4I.3. | No | Any candidate/distance family whose available full-week horizon could reach 21-52 weeks; numerically instantiated using the already-approved `TEN_K__4D__INTERMEDIATE v10` pilot values (`PreferredCoreWeeks=12`, `MaximumRunwayWeeks=8`) per Phase 4G.6A.1 — not yet generalized to any other candidate | **CLOSED** |
| `TD-LONG-HORIZON-GE-SAFETY-001` | **Long-Horizon General Endurance (1-32 Week) Safety, Mesocycle, Weekly-Role, Repetition, Checkpoint and Structural Progression Policy Approved — Recovery-Week Numeric Reduction Magnitude Remains the One Unresolved Numeric Decision.** Phase 4I.2 approved the complete structural policy for `LONG_HORIZON_GENERAL_ENDURANCE`: four-week mesocycle cadence (3 development weeks + 1 recovery/consolidation week); stage families Entry/Base Development/Aerobic Durability/Consolidation/Pre-Runway Alignment; the existing 1 `KEY_SESSION` + 2 `EASY_SUPPORT` + 1 `LONG_RUN` weekly-role skeleton retained, with `KEY_SESSION` restricted to controlled-aerobic-support/easy-progression/relaxed-fartlek/technique-stride content only — never threshold, VO2max, goal-pace, or race-specific work; `ShortExtension` (1-3 GE weeks) structures per Phase 4I.1's own preferred Entry/Runway-Alignment semantics; terminal remainder placement for non-multiple-of-4 `FullPhase` durations; GE-to-Runway structural continuity requiring the approved 8-week Runway to remain structurally unchanged; workout-repetition constraints distinguishing identity/family/numeric-prescription reuse; advisory-only checkpoints (Continue / Recommend review / Recommend regeneration / Stop-and-seek-guidance) with zero plan-mutation capability; missing/conflicting baseline-data handling; and explicit safety non-claims. Classification: `PARTIAL_SAFETY_POLICY_DECISION`. Severity: `GOVERNS_FUTURE_RUNTIME_ACTIVATION_ONLY_NO_CURRENT_IMPACT`. **Closure note (Phase 4I.2):** the development-week NUMERIC volume-increase and long-run-share framework is also approved, by extending the already-real precedent that the Preparation Runway segment reuses Core's `VolumeSafetyPolicy.Default` numeric constants (`TenKPreparationRunwayNumericPolicyFactory`) rather than inventing new numbers — GE development weeks reuse the same `PreferredMaxWeeklyIncreaseRatio=0.07`/`HardMaxWeeklyIncreaseRatio=0.08`/`AbsoluteWeeklyIncrementCapKm=2.5`/long-run shares, explicitly not a universal 10-percent rule. The **one remaining unresolved numeric decision** is the recovery/consolidation-week volume-reduction magnitude — no canonical value exists anywhere in the repository for this specific concept (Core's own `TaperVolumeMultiplier=0.53` is a pre-race sharpening reduction, not a mid-phase deload, and reusing it without evidence was explicitly rejected as an invented number). Final classification: `LONG_HORIZON_GENERAL_ENDURANCE_STRUCTURAL_SAFETY_POLICY_APPROVED_BUT_NUMERIC_PROGRESSION_POLICY_REMAINS_BLOCKED` (recovery-week magnitude only — development-week numerics ARE approved). Runtime activation remains fully blocked, unchanged, under `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` (still `OPEN`) — this closure narrows that TD's scope further without closing it. See `PHASE4I_2_LONG_HORIZON_GENERAL_ENDURANCE_SAFETY_MESOCYCLE_AND_PROGRESSION_POLICY_RESOLUTION.md` for the full policy record. **Addendum (Phase 4I.2A):** the recovery-week numeric reduction magnitude this entry left unresolved is now `CLOSED` — see `TD-LONG-HORIZON-GE-RECOVERY-MAGNITUDE-001` below. | No | Any candidate/distance family whose available full-week horizon could reach 21-52 weeks; numerically instantiated using the already-approved `TEN_K__4D__INTERMEDIATE v10` pilot's `VolumeSafetyPolicy.Default` constants — not yet generalized to any other candidate | **CLOSED** |
| `TD-LONG-HORIZON-GE-RECOVERY-MAGNITUDE-001` | **Long-Horizon General Endurance Recovery-Week Numeric Reduction Magnitude Approved (15 Percent of Prior Development Peak, INTERMEDIATE Profile) — Closes Phase 4I.2's Single Remaining Numeric Gap Using a Real, Previously-Overlooked Canonical Cutback-Policy Document.** Phase 4I.2A found that `plan-catalog/docs/canonical/golden-fixture-v3/progression_rules_v2.yaml` — a real, precedence-level-2 canonical rule file already cited as an accepted source in an unrelated prior audit entry (AUD-044) — contains a `cutbackPolicy` section Phase 4I.2's own evidence review had not surfaced: `triggers.afterConsecutiveLoadingWeeks=3` (structurally identical to GE's own approved 3-development+1-recovery mesocycle trigger), `reductionRatioByProfile.INTERMEDIATE=[0.15,0.20]`, `postCutbackProgressionBaseline=PREVIOUS_NON_CUTBACK_VOLUME`. Classification: `NUMERIC_POLICY_DECISION`. Severity: `GOVERNS_FUTURE_RUNTIME_ACTIVATION_ONLY_NO_CURRENT_IMPACT`. **Closure note (Phase 4I.2A):** `RecoveryWeekTotalVolumeKm = Round_0.5km(PreviousDevelopmentPeakVolumeKm × 0.85)`, with a guaranteed minimum drop of one rounding increment (0.5km) if the raw 15% reduction would round to no visible change; `PreviousDevelopmentPeakVolumeKm` is the mesocycle's Week-3 (highest-load) volume. 0.15 is the conservative/minimum bound of the explicit canonical `[0.15,0.20]` range, selected per this phase's own decision standard. `RecoveryWeekLongRunKm = Round_0.5km(RecoveryWeekTotalVolumeKm × 0.33)` — reusing `VolumeSafetyPolicy.Default`'s existing `LongRunSelectionShare` unmodified, guaranteeing the long-run share never rises. Session-distance split reuses `FourDaySessionDistanceAllocationPolicy.Allocate` unmodified — no new session-level formula. Feasibility reuses the existing `CatalogSessionPrescriptionInfeasibleException` fail-closed mechanism (`MinimumKeySessionDistanceKm=3` + `2×MinimumEasySupportDistanceKm=1.5`) rather than inventing a new low-volume policy. Both `CONSISTENCY_NEEDED` and `CORE_ENTRY_READY` use the identical 0.15 ratio — `progression_rules_v2.yaml`'s profile axis is experience level (`NEW`/`INTERMEDIATE`/`ADVANCED`/`EXPERIENCED`), not GE's own readiness-profile axis, and both readiness profiles map to the single `INTERMEDIATE` experience level in the current pilot; profile differences remain confined to `KEY_SESSION` content selection, never numeric magnitude. Return-from-recovery reuses `postCutbackProgressionBaseline=PREVIOUS_NON_CUTBACK_VOLUME` verbatim: the next development week's 0.07/0.08/2.5km caps apply relative to the pre-recovery mesocycle peak, not the reduced recovery value — preventing sawtooth re-growth across up to 8 mesocycles. A `FullPhase` duration that is an exact multiple of 4 does end on a Recovery week immediately before Runway — accepted, not a defect, since Phase 4I.2's own approved GE-to-Runway continuity rule (final GE aligns upward, Runway Week 1 unchanged) already bridges this without a new terminal exception. Recovery is explicitly distinguished from Taper (`TaperVolumeMultiplier=0.53` is a single-occurrence, race-proximity, performance-peaking reduction; Recovery is a repeating, phase-interior, fitness-maintaining reduction) — the two magnitudes are not interchanged. Evidence classification: A/B (a real canonical rule file, self-declared `evidenceLevel=EVIDENCE_INFORMED` with unresolvable-within-repository source labels `S2`/`S6` — not upgraded to `EVIDENCE_BACKED`). Non-claims: this ratio does not guarantee injury prevention and is not laboratory-validated; it is a conservative, already-canonical, previously-unwired product default extended one segment via the same classification-D generalization already used repeatedly in Phase 4I.1/4I.2. Final classification: `LONG_HORIZON_GENERAL_ENDURANCE_NUMERIC_RECOVERY_POLICY_APPROVED`. Runtime activation remains fully blocked, unchanged, under `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` (still `OPEN` — this decision closes only the numeric-policy gap, not rolling materialization, segment-provenance persistence, transition orchestration, or catalog content). See `PHASE4I_2A_LONG_HORIZON_GENERAL_ENDURANCE_RECOVERY_WEEK_NUMERIC_REDUCTION_MAGNITUDE_RESOLUTION.md` for the full policy record and worked examples across 4/5/6/7/8/11/12/20/32 GE weeks. | No | Any candidate/distance family whose available full-week horizon could reach 21-52 weeks; numerically instantiated using the already-approved `TEN_K__4D__INTERMEDIATE v10` pilot's `INTERMEDIATE` experience-level cutback ratio — not yet generalized to any other candidate or experience level | **CLOSED** |
| `TD-LONG-HORIZON-TYPED-RUNTIME-DECISION-001` | **Typed 21-52 Week Runtime Eligibility and Composition Decision Contract Implemented (Dark, Unwired) — First Production Code for the Long-Horizon Line; Zero Live HTTP Behavior Changed.** Phase 4I.3 implemented `LongHorizonCompositionResolver.Resolve(CoreHorizonDecision, ReadinessProfile?)` — the single authoritative 8-to-52-week decision, consuming the existing, unchanged `CoreHorizonDecision`/`CoreHorizonMode` output (never re-deriving StartDate/RaceDate arithmetic) and adding exactly the 15-20/21-52/53+ split the existing classifier's single `PreparationRunwayPlusCore` bucket does not make. Classification: `TYPED_RUNTIME_DECISION_CONTRACT_IMPLEMENTED`. Severity: `GOVERNS_FUTURE_RUNTIME_ACTIVATION_ONLY_NO_CURRENT_IMPACT`. **Closure note (Phase 4I.3):** authority placement is a single resolver invoked using the existing horizon authority's output (not a new parallel authority, not a change to `CoreHorizonClassifier`/`CoreHorizonMode`/`RaceHorizonPolicy`). The Phase 4I.1 formula (`GE=AvailableFullWeeks-20`, `Runway=8`, `Core=12` for 21-52) is implemented exactly once; `ShortExtension`(1-3)/`FullPhase`(4-32) classification matches Phase 4I.1/4I.2; `PolicyId`/`PolicyVersion`/`DeferredSafetyPolicyId`/`RecoveryPolicyId` provenance carries `TD-LONG-HORIZON-COMPOSITION-001`/`TD-LONG-HORIZON-GE-SAFETY-001`/`TD-LONG-HORIZON-GE-RECOVERY-MAGNITUDE-001` verbatim on every eligible decision. 8-14 and 15-20 week ranges are represented but their own existing allocation authorities are not duplicated. 53+ carries the internal-only `ReasonCode=PLAN_HORIZON_EXCEEDS_SUPPORTED_WINDOW`; 21-52 carries `GenerationActivationStatus=EligibleButGenerationNotActivated`/`ReasonCode=LONG_HORIZON_GENERATION_NOT_ACTIVATED` — both distinctions internal-only, not yet exposed publicly (the live HTTP reason for every 15+ week request, including 21-52 and 53+, remains `PLAN_HORIZON_COMPOSITION_REQUIRED`, unchanged). Not integrated with the runtime-condition registry (documented reasoning: `CoreHorizonClassifier` itself isn't registered there either — still exactly one source of truth). Not DI-registered — a dependency-free static class, matching the established dark-component precedent (`PreparationRunwayDateAuthority`, `TenKPreparationRunwayDarkOrchestrator`). Tests: 41 new resolver tests (fixture matrix, sum invariant and monotonicity loops for the full 21-52 range, both readiness profiles, determinism, provenance, validator accept/reject) plus 4 new real E2E containment tests (Api host + Postgres, extending `Sw12LongHorizonFailClosedEndToEndTests` to 52/53 weeks) — all passing; full backend suite re-run green with zero regressions. Runtime activation remains fully blocked, unchanged, under `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` (still `OPEN`). See `PHASE4I_3_TYPED_21_TO_52_WEEK_RUNTIME_ELIGIBILITY_AND_COMPOSITION_DECISION_CONTRACT.md` for the full record. | No | Any candidate/distance family whose available full-week horizon could reach 21-52 weeks; numerically instantiated using the already-approved `TEN_K__4D__INTERMEDIATE v10` pilot's policy constants — not yet generalized to any other candidate | **CLOSED** |
| `TD-LONG-HORIZON-GE-CATALOG-CAPACITY-001` | **Long-Horizon General Endurance Catalog and Schema Capacity Implemented for 1-32 Weeks — Deterministic, Non-Exhausting, Profile-Specific Selection Proven; No Runtime/Public Activation.** Phase 4I.4 audited existing catalog capacity first and found `EASY_STANDARD`, `LONG_RUN_STANDARD`, `AEROBIC_STRENGTH_CONTROLLED_INTRO`/`_PROGRESSED` already express every approved semantic — so **zero new workout keys** were created, only four new `eligiblePhases`-extension versions (v6/v6/v2/v2). A new dark stage-family catalog document (`long-horizon-progressions/ten-k-long-horizon-ge-stage-families.v1.json`, schema `long-horizon-ge-stage-family.schema.json`) defines five stage families (Entry/BaseDevelopment/AerobicDurability/Consolidation/PreRunwayAlignment); a deterministic C# selector (`LongHorizonGeCatalogSelector`) combines a code-level mesocycle/remainder/ShortExtension placement rule with catalog-driven role/workout content — never 32 hand-authored week records. Classification: `CATALOG_CAPACITY_IMPLEMENTED`. Severity: `GOVERNS_FUTURE_RUNTIME_ACTIVATION_ONLY_NO_CURRENT_IMPACT`. **Closure note (Phase 4I.4):** mesocycle sequencing (mesocycle 1 = BaseDevelopment; final mesocycle = AerobicDurability; others alternate by parity) guarantees BaseDevelopment never repeats >1 consecutive mesocycle, AerobicDurability never >2, and eight identical mesocycles never occur — proven for every mesocycle count 1-8 (GE 4-32) and, standalone, for the mandatory 32-week exhaustion proof (both profiles). Both readiness profiles receive identical week counts/mesocycle positions/recovery positions/terminal positions for every GE duration 1-32 (proven by loop); they differ only in `KEY_SESSION` content (`CONSISTENCY_NEEDED`: `EASY_STANDARD` throughout; `CORE_ENTRY_READY`: `AEROBIC_STRENGTH_CONTROLLED_INTRO`/`_PROGRESSED` in development weeks only). Terminal remainder (1/2/3 leftover weeks) always follows complete mesocycles, is always Alignment (never Recovery), never precedes a full mesocycle. GE→Runway catalog compatibility: the final GE week's `KEY_SESSION` resolves to `EASY_STANDARD` (family `EASY`), structurally compatible with the Preparation Runway's own opening reference (also `EASY_STANDARD`) — numeric continuity explicitly deferred to a later phase. Containment: the new `long-horizon-progressions` folder is confirmed NOT iterated by the real catalog loader (same mechanism as the Preparation Runway's own dark folder); the four new workout versions are confirmed absent from the real published bundle and the live workout-progression reference; the approved Runway catalog documents are confirmed byte-for-byte unchanged. This phase touched plan-catalog only — no backend file was modified (TD-BACKEND-001 already establishes zero backend/plan-catalog integration exists). Final classification: `LONG_HORIZON_GENERAL_ENDURANCE_CATALOG_AND_SCHEMA_CAPACITY_COMPLETED_FOR_1_TO_32_WEEKS_WITH_PROFILE_SPECIFIC_DETERMINISTIC_SELECTION_AND_NO_PUBLIC_ACTIVATION`. Runtime activation remains fully blocked, unchanged, under `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` (still `OPEN`). See `PHASE4I_4_LONG_HORIZON_GENERAL_ENDURANCE_CATALOG_AND_SCHEMA_CAPACITY.md` for the full record, the complete 1-32 duration matrix, and the 32-week exhaustion proof. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **CLOSED** |
| `TD-LONG-HORIZON-STRUCTURAL-MATERIALIZATION-001` | **Dark 21-52 Week Structural Materialization Implemented — Contiguous Global Numbering, Authoritative GE/Runway/Core Segment Provenance, No Numeric or Public Activation.** Phase 4I.5 joined the Phase 4I.3 typed composition decision, a backend-mirrored Phase 4I.4 GE catalog selector (`LongHorizonGeStructuralSelector`, an independent mirror per `TD-BACKEND-001` since backend has no project reference onto `PlanCatalog.*`), and the EXISTING, UNCHANGED Preparation Runway (`PreparationRunwayWeekMaterializer`) and Core (`CatalogStageToWeekMaterializer`) structural materializers into one `LongHorizonStructuralMaterializer` producing a contiguous, globally-numbered 1..N week skeleton (segment order strictly GE → Runway → Core, no interleaving, no reset at boundaries). Classification: `STRUCTURAL_MATERIALIZATION_IMPLEMENTED`. Severity: `GOVERNS_FUTURE_RUNTIME_ACTIVATION_ONLY_NO_CURRENT_IMPACT`. **Closure note (Phase 4I.5):** every week carries exactly 4 ordered slots (1 `KEY_SESSION` + 2 `EASY_SUPPORT` + 1 `LONG_RUN`); GE weeks carry stage-family/mesocycle/recovery/terminal-alignment provenance with non-null, non-prohibited catalog workout references; Runway weeks reuse the existing allocation/progression/binding pipeline verbatim (profile block matrix confirmed unchanged); Core weeks reuse the existing pure structural materializer verbatim (`FOUNDATION`3/`BUILD`4/`RACE_SPECIFIC`4/`TAPER`1) but deliberately carry a null workout key/version per slot — an honest, disclosed scope boundary, since that existing materializer never selects one itself. 20→21 suffix continuity and N→N+1 monotonicity are proven directly; both readiness profiles proven to share identical durations/boundaries/recovery positions, differing only in approved content. Every horizon 21-52 for both profiles proven to materialize a valid, deterministic, fully-slotted (4×N) skeleton via a new fail-closed `LongHorizonStructuralValidator`, which rejects 20/53/GE-0/GE-33/Runway≠8/Core≠12/sum-mismatch/null-profile inputs with no partial skeleton ever returned. Dark integration level A — materializer invoked only by 126 new focused tests; zero new call sites into `PlanServices`/`CatalogPreviewGenerator` (confirmed by grep); the existing real E2E containment tests (`LongHorizonGenerationContainmentTests`, Phase 4I.3) remain valid unchanged evidence that 21/24/52/53-week public requests still return HTTP 422 `PLAN_HORIZON_COMPOSITION_REQUIRED` with zero persistence. No calendar date and no numeric volume/pace value appears anywhere in the output skeleton. Final classification: `LONG_HORIZON_21_TO_52_WEEK_DARK_STRUCTURAL_MATERIALIZATION_COMPLETED_WITH_CONTIGUOUS_GLOBAL_NUMBERING_AUTHORITATIVE_SEGMENT_PROVENANCE_AND_NO_NUMERIC_OR_PUBLIC_ACTIVATION`. Runtime activation remains fully blocked, unchanged, under `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` (still `OPEN`). See `PHASE4I_5_DARK_21_TO_52_WEEK_STRUCTURAL_MATERIALIZATION.md` for the full record. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **CLOSED** |
| `TD-LONG-HORIZON-NUMERIC-CALENDAR-EXECUTION-001` | **Long-Horizon GE Numeric Progression/Recovery Execution and Core Workout-Binding Gap Closed; Preparation Runway and Core Numeric (Volume/Pace) Execution Remains Unexecuted — Partial, Honestly Disclosed Closure.** Phase 4I.6 implemented `LongHorizonGeNumericExecutor` (real GE entry-baseline/development-progression/recovery/weekly-distribution/long-run execution, reusing `VolumeSafetyPolicy.Default` and `FourDaySessionDistanceAllocationPolicy.Allocate` verbatim), `LongHorizonCoreWorkoutBindingExecutor` (closes the Phase 4I.5 Core null-workout-reference gap by invoking the EXISTING `CatalogCandidateEligibilityGate`/`CatalogWorkoutProgressionLoader`/`CatalogStageToWeekMaterializer`/`CatalogWeekSkeletonCalendarMaterializer`/`ProgressionStageAllocator`/`CatalogWorkoutBinder` chain directly — the same real sequence `CatalogPreviewGenerator`'s own 8-14 week path uses, and the same isolated-invocation pattern `CatalogWorkoutBinderTests.cs` already proves safe), `LongHorizonCalendarAssigner` (numeric-independent weekday/date assignment), and `LongHorizonDarkExecutionOrchestrator`/`LongHorizonExecutionValidator` tying them together. Classification: `PARTIAL_EXECUTION_IMPLEMENTED`. Severity: `GOVERNS_FUTURE_RUNTIME_ACTIVATION_ONLY_NO_CURRENT_IMPACT`. **What was completed:** every GE week gets a full numeric prescription (weekly volume, long-run distance, per-slot distance) with development progression bound by the existing 0.07/0.08/2.5km caps, recovery weeks bound by the existing 0.85 ratio (never creating a new peak, always ≥0.5km reduction), post-recovery progression returning toward the prior non-cutback peak (no sawtooth drift, proven at GE32/8 recovery weeks); every GE, Runway, and Core slot across every horizon carries a real, non-null, catalog-resolved workout key/version (closing Phase 4I.5's disclosed Core gap); every week/slot across all three segments carries a real assigned calendar date/weekday, with the long-run slot always on the preferred long-run day. **What remains unexecuted (why this entry is `OPEN`, not `CLOSED`):** Preparation Runway numeric (volume/pace) execution for the 21-52 week path — the existing `PreparationRunwayNumericMaterializer`/`PreparationRunwayCalendarComposer`/`PreparationRunwayPaceMaterializer` stages are coupled to a Core-derived starting-evidence adapter that does not apply when GE precedes Runway; and Core numeric (volume/pace) execution — only Core workout identity was resolved this phase, not volume/pace, since the existing full Core numeric pipeline requires a live-request-shaped goal-feasibility/target-time context this dark skeleton does not naturally carry. Consequently GE→Runway/Runway→Core numeric transition and continuity requirements remain unresolved. Final classification: `LONG_HORIZON_NUMERIC_AND_CALENDAR_EXECUTION_REMAINS_BLOCKED_BY_EXPLICIT_WORKOUT_BINDING_BASELINE_PROGRESSION_TRANSITION_PACE_OR_DATE_ASSIGNMENT_GAP` (Runway/Core numeric gap specifically). `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4I_6_DARK_LONG_HORIZON_WORKOUT_BINDING_NUMERIC_PROGRESSIVE_LOADING_TRANSITION_AND_CALENDAR_ASSIGNMENT.md` for the full record. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **OPEN** |
| `TD-LONG-HORIZON-RUNWAY-CORE-NUMERIC-CONTEXT-INTEGRATION-001` | **Real Preparation Runway and Core Numeric/Pace Execution Integrated for Short-GE Long-Horizon Plans Using Real Runtime-Condition Resolution; Existing-Pipeline Compatibility Gap Discovered and Precisely Bounded for Longer GE Horizons.** Phase 4I.6A implemented `LongHorizonGeExitState` (bounded GE→Runway transition state, derived from the already-validated Phase 4I.6 GE numeric result, never recalculating GE progression), `LongHorizonFullNumericOrchestrator` (invokes the EXISTING, UNCHANGED `PreparationRunwayNumericMaterializer`/`PreparationRunwayCalendarComposer`/`PreparationRunwayPaceMaterializer` with GE-exit-derived entry evidence instead of the existing Core-derived adapter, and the EXISTING, UNCHANGED `DynamicCoreCalendarMaterializationOrchestrator`/`TenKPreparationRunwayCoreGenerator` for real Core numeric/pace/binding/dates — driven by REAL `RuntimeConditionResolutionService` resolution, never a fabricated stand-in), `LongHorizonFullExecutionValidator` (cross-segment continuity + full-completeness checks). Classification: `PARTIAL_EXECUTION_IMPLEMENTED`. Severity: `GOVERNS_FUTURE_RUNTIME_ACTIVATION_ONLY_NO_CURRENT_IMPACT`. **What was completed:** for 21- and 24-total-week Long-Horizon plans (both readiness profiles, typical baseline), the full three-segment pipeline — GE (Phase 4I.6, unchanged) → Preparation Runway (real numeric/calendar/pace, GE-exit-derived entry) → Core (real numeric/pace/binding/dates via the exact existing 8-14/15-20 week generation pipeline) — produces a fully numeric, workout-bound, paced, and dated internal schedule with every GE→Runway volume-continuity check inside approved caps and a clean, single Runway→Core transition (`PreSpecificTransition` → `FOUNDATION`, no duplicate week). **What remains unexecuted / the key finding:** for longer GE horizons (confirmed failing at 28/40/52 total weeks, typical baseline), the existing Runway numeric materializer throws its own existing, unmodified `"Starting weekly volume exceeds Core Week 1 and no approved non-taper runway reduction rule exists"` — GE's approved development-progression caps compounding over many weeks legitimately exceed Core's own independently-computed, fixed peak-volume-band ceiling, and the existing pipeline has no accepted rule to ramp a Runway entry DOWN into a lower Core target outside of Taper. This is a genuine existing-pipeline compatibility gap, not a defect introduced by this phase, and no new numeric rule/constant was invented to paper over it. Final classification: `LONG_HORIZON_FULL_NUMERIC_EXECUTION_REMAINS_BLOCKED_BY_EXPLICIT_GE_TO_RUNWAY_ENTRY_CONTEXT_CORE_RUNTIME_CONTEXT_OR_EXISTING_PIPELINE_COMPATIBILITY_GAP`. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4I_6A_LONG_HORIZON_RUNWAY_AND_CORE_NUMERIC_CONTEXT_INTEGRATION_AND_FULL_TRANSITION_CONTINUITY_RESOLUTION.md` for the full record. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **OPEN** |
| `TD-LONG-HORIZON-RUNWAY-ENTRY-ABOVE-CORE-TARGET-001` | **Runway Semantic Role and Structural Decisions Resolved for GE-Exit-Above-Core-Target State; Exact Numeric Convergence Formula Remains an Explicit, Evidence-Backed Open Gap — Not Invented.** Phase 4I.6B (pure governance, zero production code changed) resolved: Runway's semantic role in this state is **entry consolidation at the Core target** — distinct from GE recovery (single interior week, 0.85 policy), Taper (race-proximity, single occurrence, 0.53 policy), and normal upward progression. The **GE-side-ceiling** candidate (capping GE by the future Core target) is **REJECTED** — it would make an earlier general-development phase dependent on a later phase-specific target, contradicting Phase 4I.2's independent-development semantics. The **Core-target-lifting** candidate (raising Core Week 1 from incoming Runway load) is **REJECTED** — it would re-derive Core's own evidence-based decision, contradicting "do not change Core algorithms." Workout identity/pace unaffected either way; the policy is profile-neutral; strictly additive (never alters existing 15-20 or GE-at-or-below-target behavior). **What remains unresolved:** real diagnostic evidence (`LongHorizonRunwayEntryAboveCoreTargetPolicyDiagnosticTests.cs`, using the exact real, unchanged GE numeric executor and Core generation pipeline) shows the excess grows unboundedly with horizon — **~4km at 28 weeks, ~21.5km at 40 weeks, ~41km at 52 weeks** (GE's own peak reaches ~71.5km/week by week 52, over 3× the ~20km Core target). No canonical rule, approved default, generalizable production behavior, or domain evidence exists for a specific multi-week reduction magnitude able to safely bridge gaps this large within a fixed 8-week Runway. The existing 0.07/0.08/2.5km/0.85/0.53 constants are each evidenced for a *different* purpose and reusing any of them here without direct support is explicitly forbidden — none was found. An "immediate ceiling clamp" (zero new formula) was evaluated and rejected as a blanket default for large excesses — a single-step 65%+ volume drop is exactly the "hidden Taper"/"unnecessary detraining" risk the decision standard requires rejecting without real safety evidence. Final classification: `LONG_HORIZON_RUNWAY_ENTRY_ABOVE_CORE_TARGET_POLICY_REMAINS_BLOCKED_BY_EXPLICIT_CANONICAL_PRODUCT_OR_EVIDENCE_GAP`. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. A related but explicitly out-of-scope finding: GE's own uncapped compounding growth for very long horizons may itself be the more foundational issue worth a future governance review. See `PHASE4I_6B_LONG_HORIZON_RUNWAY_ENTRY_ABOVE_CORE_TARGET_NUMERIC_TRANSITION_POLICY_RESOLUTION.md` for the full record. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **OPEN** |
| `TD-LONG-HORIZON-NUMERIC-ACTIVATION-BOUNDARY-001` | **Long-Horizon Runtime Numeric Activation Narrowed to 21 Total Weeks Only — Real Full-Matrix Scan Proves Entry-Above-Core-Target Is the Norm, Not an Edge Case, for Every Horizon 22-52.** Phase 4I.6B.1 ran the real, unchanged production pipeline (real GE numeric executor, real horizon-invariant Core Week-1 target measurement, real `RuntimeConditionResolutionService`) across all 32 horizons (21-52) × 2 readiness profiles × 3 approved input classes (Low=15km/5km, Typical=20km/8km, High=30km/12km) — 192 combinations. **Key structural finding:** Core's Week-1 target equals the raw `RecentWeeklyVolumeKm` evidence directly (horizon-invariant, unclamped for all tested magnitudes) — which also equals GE's own Week-1 entry value by construction, meaning ANY GE progression beyond week 1 immediately creates excess. **Real results:** 21 weeks (GE=1, no progression) PASSES universally with zero excess; 22/23 weeks FAIL universally; 24 weeks (GE=4, lands exactly on a GE recovery week) PASSES again — a real, confirmed, non-monotonic "isolated later success"; every horizon 25-52 FAILS universally. Per this phase's own explicit instruction against activating isolated later successes, the correct answer is the longest contiguous universally-supported prefix starting at 21 — **MaxNumericallySupportedTotalWeeks = 21**, confirmed end-to-end (not just via comparison) using the real `LongHorizonFullNumericOrchestrator` for 21 (succeeds), 22 (throws the existing, unmodified Runway exception), and 24 (succeeds, confirming agreement). Classification: `RUNTIME_ACTIVATION_SCOPE_NARROWED`. Severity: `GOVERNS_FUTURE_RUNTIME_ACTIVATION_ONLY_NO_CURRENT_IMPACT`. The 21-52 week **composition** policy (Phase 4I.1), GE catalog capacity (Phase 4I.4), and structural materialization (Phase 4I.5) all remain fully preserved and unchanged — only numeric runtime activation is narrowed; 22-52 weeks are explicitly **not** reclassified as exceeding the supported composition window (that remains exclusively the 53+ reason, unchanged) — a distinct `LONG_HORIZON_NUMERIC_ENVELOPE_NOT_SUPPORTED` reason is defined for their composition-eligible-but-numerically-inactive state (production implementation deferred). No convergence percentage, GE ceiling, or Core-target lifting was introduced; zero production code changed. Final classification: `LONG_HORIZON_NUMERIC_ACTIVATION_BOUNDARY_APPROVED_WITH_STRUCTURAL_21_TO_52_SUPPORT_PRESERVED_AND_RUNTIME_NUMERIC_ACTIVATION_LIMITED_TO_21_THROUGH_21`. See `PHASE4I_6B_1_SUPPORTED_LONG_HORIZON_NUMERIC_BOUNDARY_RESOLUTION.md` and `PHASE4I_6B_1_HORIZON_SCAN_MATRIX.csv` (193-row full matrix) for the complete record. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **CLOSED** |
| `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` | **Path A (Deferred) — Long-Horizon Volume Envelope and Maintenance-Phase Redesign, Motivated by Phase 4I.6B.1's Finding That GE-Exit-Above-Core-Target Is the Norm for Every Horizon 22 Weeks and Above.** Created as an explicit forward-looking placeholder per Phase 4I.6B.1's own required deliverable; not investigated or resolved by this phase. Classification: `FUTURE_REDESIGN_DEFERRED`. A future dedicated phase must evaluate: long-duration GE progression redesign (a growth-to-maintenance transition instead of unbounded compounding); horizon-aware plateau/consolidation rules; whether the fixed 8-week Runway duration remains appropriate; an incoming-load-aware Core entry envelope; explicit multiweek long-run maintenance rules; and the detraining/excessive-reduction risk boundaries that caused Phase 4I.6B to reject an immediate ceiling clamp. Must not block activation of the validated 21-week lower range (`TD-LONG-HORIZON-NUMERIC-ACTIVATION-BOUNDARY-001`, `CLOSED`) — the two are independent. | No | `TEN_K__4D__INTERMEDIATE v10` specifically, though any future redesign would need re-evaluation for generalization | **OPEN / DEFERRED** |
| `TD-LONG-HORIZON-ROLLING-NUMERIC-ACTIVATION-001` | **Long-Horizon Rolling Numeric Activation and Reassessment Authority Policy Approved — Full Structural Roadmap Preserved, Numeric Prescription Limited to a Four-Week Rolling Activation Window, Detailed Maintenance/Checkpoint/JIT Policies Deferred.** Phase 4K.1 (pure governance, zero production code changed, zero diagnostics required) replaces the rejected full-upfront 21-52 week numeric model (and Path B's narrow one-horizon activation, `TD-LONG-HORIZON-NUMERIC-ACTIVATION-BOUNDARY-001`) with an approved rolling numeric activation policy. **Structural vs numeric split:** a confirmed plan always has a complete structural roadmap for all 21-52 weeks (unchanged Phase 4I.1/4I.4/4I.5); numeric prescription is limited to a bounded near-term **4-week activation window** — an explicit Appsel **product default** (not a universal rule) chosen because it matches the existing GE 3-development+1-recovery mesocycle exactly. Partial windows apply at segment boundaries (ShortExtension activates only its own remaining weeks; <4 remaining GE weeks may extend the window into the existing Runway pipeline rather than truncating). **Six lifecycle states** approved: `StructurallyPlanned`, `NumericPending`, `NumericActivated`, `Completed`, `Missed`, `NumericActivationBlocked` — pending weeks carry null (never fabricated zero) numeric fields; completed/missed history is immutable; only future unstarted weeks may activate. **Checkpoint authority:** evaluates evidence, returns a typed decision (progress/maintain/block) authoritative for the *next* window only, never rewriting history or the structural roadmap by default — detailed evidence fields deferred to Phase 4K.3. **Missing-evidence principle:** no new validated evidence ⇒ no upward progression; a maintenance-only window may be permitted if the last validated sustainable load remains available, otherwise fail closed — the maintenance formula itself is deferred to Phase 4K.2. **Structural immutability:** race date, horizon, GE/Runway/Core composition, segment order, global numbering, history, and catalog provenance all remain stable; future activation is not retroactive mutation. **Runway/Core just-in-time principle:** both remain structurally present from creation, but final numeric prescriptions resolve from CURRENT evidence near their own activation boundary (not frozen initial-creation evidence) — existing pipelines/pace/goal-feasibility policies fully unchanged, no Core-target lifting, no GE ceiling; exact JIT timing deferred to Phase 4K.4. **Public semantics** (conceptual only): roadmap + activated window + pending weeks + checkpoint info + lifecycle status — "Appsel shows the full journey; only the near-term prescription is finalized." **Non-claims** recorded verbatim per the phase's own list (product default, not injury guarantee, maintenance≠Taper, not medical diagnosis, etc.). Zero maintenance formula, checkpoint threshold, JIT timing rule, or code of any kind was introduced. Final classification: `LONG_HORIZON_ROLLING_NUMERIC_ACTIVATION_AND_REASSESSMENT_AUTHORITY_POLICY_APPROVED`. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4K_1_LONG_HORIZON_ROLLING_NUMERIC_ACTIVATION_AND_REASSESSMENT_AUTHORITY_POLICY_RESOLUTION.md` for the full record. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **CLOSED** |
| `TD-LONG-HORIZON-VALIDATED-LOAD-AND-MAINTENANCE-ENVELOPE-001` | **Validated Sustainable Load, Growth/Maintenance Eligibility and Maintenance Envelope Policy Approved — Reuses Existing TrainingDay/WorkoutLog Evidence and `AdherenceRatePercent` Terminology, No New Percentage Invented.** Phase 4K.2 (pure governance, zero production code) approves: **Evidence window** — the most recent activated window (≤4 weeks), sourced from `TrainingDay.ActualDistanceKm` for `Status=Completed` sessions only; Missed/Skipped/SoftMissed (including `NotTodayDecision`'s default `ResultingStatus=Missed`) count as **zero**, never excluded, never inferred from planned distance. **Validated weekly load** = mean ACTUAL weekly distance across the **non-recovery** weeks of the window (the recovery week is excluded from the average — its planned reduction is by design, not a completion failure — but its own completion still counts toward adherence; the pre-recovery peak is never assumed achieved without its own evidence), rounded via the existing 0.5km increment. **Validated long-run load** = mean completed `LONG_RUN` distance across the same weeks, bounded by the existing 0.40 hard-cap share — one anomalous run can never define capacity alone. **Adherence** reuses the existing `AdherenceRatePercent` term as a Growth-eligibility confidence gate (not folded into the load average, not weighted by role); no exact threshold is invented — explicitly deferred to Phase 4K.3. **GrowthEligible** requires validated load + long-run evidence + sufficient adherence + no safety conflict + 4-session feasibility + the EXISTING 0.07/0.08/2.5km progression authority remaining inside the peak-volume band — does not mean an increase is mandatory; no second progression formula created. **MaintenanceOnly** applies when evidence doesn't support Growth, or no new evidence exists but a prior validated load remains usable — never a fallback for invalid inputs, unresolved safety, infeasibility, or missing load (those fail closed instead). **Maintenance anchor** = the validated load directly — explicitly NOT a percentage of baseline/peak/Core-target/recovery-volume/Taper. **Four-week shape**: Weeks 1–3 flat at the anchor, Week 4 reuses the existing 0.85 recovery formula applied to the anchor itself; no maintenance week may create a new peak. **Long-run envelope**: the existing 0.33 share rule applied to the flat anchor algebraically cannot produce a new peak — zero new share invented. **Consecutive maintenance** windows are approved, with the anchor refreshed each window from real completed evidence (preventing both drift and recovery-averaging decline). **Partial windows**: the decision applies only to remaining GE weeks; Runway weeks always use the existing, unmodified Runway authority. **Blocked** reason families: `VALIDATED_LOAD_UNAVAILABLE`, `VALIDATED_LONG_RUN_EVIDENCE_UNAVAILABLE`, `NUMERIC_WINDOW_INFEASIBLE`, `SAFETY_REASSESSMENT_REQUIRED` — never collapsed into one generic reason. Both readiness profiles use an identical formula. Non-claims recorded verbatim (product metric not lab measurement; no injury-safety proof; maintenance ≠ Taper ≠ recovery; no HRV requirement; etc.). Final classification: `LONG_HORIZON_VALIDATED_LOAD_GROWTH_ELIGIBILITY_AND_MAINTENANCE_ENVELOPE_POLICY_APPROVED`. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4K_2_VALIDATED_LOAD_GROWTH_ELIGIBILITY_AND_MAINTENANCE_ENVELOPE_POLICY_RESOLUTION.md` for the full record. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **CLOSED** |
| `TD-LONG-HORIZON-CHECKPOINT-EVIDENCE-STATE-TRANSITION-001` | **Checkpoint Evidence Snapshot, Freshness, Terminal-Window, Adherence-Confidence, Safety and Deterministic State-Transition Policy Approved.** Phase 4K.3 (pure governance, zero production code changed) defines the checkpoint that decides `GrowthEligible` / `MaintenanceOnly` / `NumericActivationBlocked` for the next 4-week window. **Evidence snapshot:** a conceptual field set (checkpoint date, window boundaries, actual weekly volumes, completed long runs, `AdherenceRatePercent`, missed count, availability/safety signals, prior validated anchors, segment/weeks-to-Runway) that never admits future planned values. **Freshness:** weekly-volume and long-run evidence both use the same 4-week activation-window freshness default (an explicit product default — the repository's only existing age rule, `PaceSourceResolver`'s 30/60/90/180-day ladder, is scoped to race-pace evidence only and is not reused here); adherence must come from the most recent block only, never inherited indefinitely; unresolved safety signals never expire automatically. **Terminal-window rule:** a checkpoint decision fires only once the window's calendar period has ended and every session reached a terminal state (`Completed/Missed/Skipped/SoftMissed`) — unresolved sessions are never counted as zero or Completed; an incomplete window can still receive `MaintenanceOnly` using a prior anchor only, never blended with partial current-window data. **Partial completion:** no distinct status exists in `TrainingDayStatus`; actual (lower) distance already lowers the Phase 4K.2 average — no new percentage-complete threshold invented. **Missed/NotToday:** treated identically (Phase 4K.2), contribute zero distance; `Rescheduled` is confirmed reserved-but-never-assigned in the current repository and is documented as non-terminal for any future implementer. **Adherence confidence gate:** no accepted numeric threshold exists anywhere in the repository and none is invented — the approved deterministic Growth condition is "every non-recovery week of the window contains at least one Completed session with usable evidence"; Maintenance requires only a valid current-or-prior load anchor; Blocked is the fallback when neither holds. **Safety:** an unresolved safety-critical signal always produces `NumericActivationBlocked`/`SAFETY_REASSESSMENT_REQUIRED` with top evaluation priority — no medical-scoring system is invented (none exists in the repository beyond free-text `InjuryNotes`). **State-transition table:** eight mutually exclusive, priority-ordered cases (safety → window-terminal → load availability → long-run availability → feasibility → adherence confidence → Growth) guaranteeing exactly one output. **Reason taxonomy:** nine typed reasons (`CHECKPOINT_WINDOW_NOT_COMPLETE`, `CHECKPOINT_EVIDENCE_STALE`, `VALIDATED_LOAD_UNAVAILABLE`, `VALIDATED_LONG_RUN_EVIDENCE_UNAVAILABLE`, `ADHERENCE_CONFIDENCE_INSUFFICIENT_FOR_GROWTH`, `MAINTENANCE_ANCHOR_UNAVAILABLE`, `NUMERIC_WINDOW_INFEASIBLE`, `SAFETY_REASSESSMENT_REQUIRED`, `EVIDENCE_CONFLICT_UNRESOLVED`) — `GrowthEligible` never carries a failure reason, `Blocked` always carries exactly one. **Segment boundary:** the checkpoint governs only the remaining GE portion of a window; Runway/Core's own just-in-time authority (Phase 4K.1) takes over at its boundary; unavailable JIT context blocks activation, with exact timing deferred to Phase 4K.4. **Initial window** continues to use the existing Phase 4I.6 onboarding-evidence baseline, not this checkpoint machinery. Both profiles use an identical policy. Zero new adherence percentage, evidence-age duration beyond the 4-week default, medical-scoring system, or production code was introduced. Final classification: `LONG_HORIZON_CHECKPOINT_EVIDENCE_FRESHNESS_AND_NUMERIC_STATE_TRANSITION_POLICY_APPROVED`. `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` was updated accordingly, remains `OPEN`. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4K_3_CHECKPOINT_EVIDENCE_FRESHNESS_AND_NUMERIC_STATE_TRANSITION_POLICY_RESOLUTION.md` for the full record. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **CLOSED** |
| `TD-LONG-HORIZON-RUNWAY-CORE-JIT-CONTEXT-001` | **Runway and Core Just-in-Time Numeric Context Resolution Timing Approved.** Phase 4K.4 (pure governance, zero production code changed) performed real dependency tracing of the existing pipeline and found `PreparationRunwayNumericMaterializer`/`CalendarComposer`/`PaceMaterializer` all REQUIRE a Core Week-1 numeric/pace target as a required, validated, non-null input, with Runway's own weekly-volume/long-run values computed as a direct linear interpolation TOWARD that target — a real, existing architectural coupling, not invented. **Atomic resolution:** because of this hard dependency, Runway and Core-Week-1-target context are resolved ATOMICALLY at the checkpoint activating the first four-week window containing any Runway week — in exact week-boundary terms, the checkpoint whose activated window satisfies `EndWeek >= firstRunwayWeek` and `StartWeek <= firstRunwayWeek`. **Core's full internal schedule vs. activated Core weeks:** the existing pipeline unavoidably computes all of Core's weeks as a byproduct; only Core weeks within the currently activating window are activated/exposed — the rest remain `NumericPending` and are recomputed fresh at their own future checkpoint. **Core Week-1 target locking:** once used to activate any Runway week, the target is locked for those weeks (a required invariant) — satisfied by combining atomic locking (for already-activated weeks) with versioned future-only Core refresh (for not-yet-activated weeks), governed by the existing GE→Runway/Runway→Core validators on any incompatibility. **Evidence:** neither Runway's nor Core's existing evidence SOURCE is changed — Runway still uses GE exit state, Core's Week-1 target still uses original onboarding evidence (confirmed independent by design, to avoid circularity); this phase governs TIMING only, and discloses (without implementing) that the rolling runtime will eventually need Runway's evidence source swapped from GE's planned-progression exit state to Phase 4K.2's validated actual load. **GE exit vs. actual evidence:** Runway entry anchors to current validated actual evidence, never the last planned GE peak; no new convergence percentage; existing Phase 4I.6B build-toward/entry-consolidation policies already cover any magnitude relationship. **Mixed-window atomicity:** the entire four-week mixed window (GE+Runway or Runway+Core) succeeds or none activates — the only option the existing architecture can mechanically support. **Race date/time budget, target-time/pace-source, and availability** all reuse existing, unmodified mechanisms unchanged. **Ten new JIT-specific typed reasons** approved, additive to Phase 4K.3's nine, with `SAFETY_REASSESSMENT_REQUIRED` reused directly at highest priority. **Immutability:** already-activated numeric weeks, including their locked Core Week-1 target, never silently change; future recalculation is versioned, never retroactive. Both profiles use an identical policy. Zero new convergence percentage, evidence-freshness duration, algorithm change, or production code was introduced — every decision is a direct, disclosed consequence of the real pipeline's own dependencies. Final classification: `LONG_HORIZON_RUNWAY_AND_CORE_JUST_IN_TIME_NUMERIC_CONTEXT_POLICY_APPROVED`. `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` was updated accordingly, remains `OPEN`. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4K_4_RUNWAY_AND_CORE_JUST_IN_TIME_NUMERIC_CONTEXT_POLICY_RESOLUTION.md` for the full record. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **CLOSED** |
| `TD-LONG-HORIZON-ROLLING-TYPED-CONTRACTS-001` | **Production Typed Contracts for the Structural Roadmap, Rolling Numeric Lifecycle, Activation Window, Checkpoint Evidence/Decision, Evidence Authority, and Runway/Core JIT Context Implemented.** Phase 4K.5 implements Phases 4K.1–4K.4's approved policies as real, dark, unwired C# types (`backend/RunningApp.Application/RuntimeCatalog/Schedule/LongHorizon/RollingActivation/`, 14 files): structural roadmap (with validators enforcing `TotalWeeks=GE+8+12`, contiguous global weeks, segment order), the six-state numeric lifecycle (Completed/Missed fully terminal), the 4-week rolling activation window (mixed-window atomicity enforced), `ActivatedNumericWeek` (null-vs-complete distinction, session-sum invariant), checkpoint evidence snapshot and `ValidatedSustainableLoad`, `LongHorizonCheckpointDecision` (mutually-exclusive outcomes, exactly-one-reason rule), all 9 checkpoint + 10 JIT reason codes (a shared `LongHorizonReasonCode` abstraction reuses `SafetyReassessmentRequired` without duplication), `LongHorizonJitContextDecision` (enforces Runway-requires-Core-target + atomic-resolution-flag), `LongHorizonLockedCoreWeekOneTarget` (immutable, `Refresh()` rejects any overlap with an already-locked activated range), `LongHorizonContextVersion`, and an initial-vs-checkpoint activation distinction. **Critical authority guard:** `LongHorizonEvidenceAuthorityRecord.Create` structurally THROWS if any caller attempts to mark `OriginalOnboardingEvidence` as `Authoritative` — the exact silent-defaulting this phase's own governance explicitly forbids; Core's Week-1 rolling authority is recorded as `UnresolvedForRollingRuntime`, never upgraded. Zero production wiring: every type is `internal`, no controller/DI/orchestrator/database file was touched, confirmed by a clean build of the full backend. **52 new backend tests, all passing**; full backend suite **2506 passed, 0 failed, 0 skipped**. This TD does NOT close `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` — rolling numeric generation, checkpoint aggregation, runtime wiring, persistence, public preview, and Flutter remain entirely unimplemented. Final classification: `LONG_HORIZON_STRUCTURAL_ROADMAP_ROLLING_NUMERIC_ACTIVATION_AND_JIT_TYPED_CONTRACTS_COMPLETED`. See `PHASE4K_5_STRUCTURAL_ROADMAP_ROLLING_NUMERIC_ACTIVATION_AND_JIT_CONTEXT_TYPED_CONTRACTS.md` for the full record. | No | `TEN_K__4D__INTERMEDIATE v10` specifically — explicitly not generalized to 5K/Half Marathon/Marathon, other levels, other day-frequencies, habit plans, or 53+ horizons | **CLOSED** |
| `TD-LONG-HORIZON-CORE-TARGET-EVIDENCE-AUTHORITY-001` | **Core Week-1 Rolling JIT Evidence Authority Resolved (Phase 4K.5A).** OPTION A is approved: the latest fresh `ValidatedSustainableLoad` enters the existing unchanged Core generator through `RecentWeeklyVolumeKm` and `RecentLongestRunKm`. Completed history is the only permitted source for optional `RecentRunsPerWeek` readiness metadata; an exact validated integer may be carried, otherwise it remains absent because no rounding rule is invented. Declared four-day availability remains the separate feasibility authority. The generator—not a new direct formula—produces the final Week-1 prescription. Original onboarding evidence is rolling `ProvenanceOnly` and separately `LegacyCurrentProductionSource` for today's code; planned GE exit is `ProvenanceOnly`. Missing current/prior-still-valid weekly or required long-run evidence blocks JIT. Locked targets remain immutable for activated Runway weeks; future-only refresh is versioned and validator-gated. See `PHASE4K_5A_CORE_WEEK_ONE_ROLLING_JIT_EVIDENCE_AUTHORITY_RESOLUTION.md`. | No (authority blocker closed; runtime implementation remains deferred) | `TEN_K__4D__INTERMEDIATE v10` specifically | **CLOSED** |
| `TD-AEROBIC-STRENGTH-SEMANTICS-001` | **AEROBIC_STRENGTH Preparation Runway Block Semantics Resolved (Running-Based Aerobic-Power Stimulus, Not Gym-Based Resistance Training), But No Catalog-Executable Workout Exists Under Either Interpretation.** The dark, unwired `PreparationRunwayContracts.PreparationRunwayBlockType.AerobicStrength` block's semantics were never formally resolved between (A) gym-based resistance/plyometric strength training and (B) running-based controlled aerobic-power/economy stimulus (hills, strides, controlled fartlek, short aerobic intervals). This phase resolves the question in favor of (B): interpretation (A) is rejected because the repository contains zero gym/resistance/plyometric content anywhere (an exhaustive grep of all 18 workout-definition documents in `plan-catalog/catalog/workouts/` for hill/stride/plyometric/resistance/gym/strength/core/economy/power found zero matches). However, even under (B), no catalog-executable workout exists: the only running-based intensity-variation content in the catalog is FARTLEK's `SURGE_AND_FLOAT` pattern (`family=QUALITY`), restricted to `eligiblePhases=[BUILD]` only by the closed workout-definition schema enum — there is no runway-eligible phase value and no hill-repeat/stride/short-aerobic-interval workout definition exists at all. AEROBIC_STRENGTH is therefore catalog-`UNSUPPORTED` under either semantic interpretation. A second, independent finding: a local test-only coefficient simulation (`Phase4G6A2CoefficientSimulationTests.cs`, not wired to any runtime/DI path) proved the `CORE_ENTRY_READY` candidate profile's target matrix (`AerobicStrengthSecondWeekThreshold = RunwayWeeks >= 6`) is not mechanically reproducible by the described constrained-proportional/floor/largest-remainder allocator under any tested coefficient pair (including an extreme 0.85/0.15 weighting) — the block's second-week exposure emerges one horizon earlier (`RunwayWeeks=5`) under pure proportional weighting regardless of coefficient choice, a structural finding requiring an explicit threshold-gated rule rather than coefficient tuning. Classification: `CATALOG_CAPACITY_GAP`. Severity: `BLOCKS_COREENTRYREADY_PROFILE_APPROVAL`. Required resolution: before any future allocator schedules AEROBIC_STRENGTH weeks, add a validated catalog-executable workout for interpretation (B) and extend the workout-definition schema's `eligiblePhases` enum (or an equivalent mechanism) to permit runway use; or explicitly remove AEROBIC_STRENGTH from any V1 Preparation Runway profile; if ever implemented, revise the target matrix's threshold to match the mechanism's actual behavior (`RunwayWeeks>=5`) or design an explicit threshold-gated priority rule instead of proportional weighting; do not add hill/stride/resistance/plyometric catalog content to close this risk without an explicit product/coaching/schema decision. See `PHASE4G_6A_2_TEN_K_RUNWAY_COEFFICIENT_AND_ELIGIBILITY_DECISION.md`. **Addendum (Phase 4G.6A.2A — AerobicStrength Catalog Capacity, Second-Week Threshold and Transition-Bridge Resolution):** AEROBIC_STRENGTH is retained (KEEP, vs. an unevidenced `GeneralEndurance.MaximumWeeks=7` alternative); `AerobicStrengthSecondWeekThreshold` is formally replaced by `RunwayWeeks >= 5` (`PRODUCT_DEFAULT`, matching the mechanism's proven output); a full one-/two-week progression contract is specified (design-only, directly precedented by the existing `FARTLEK_INTRO` stage's `min1/max2` shape); the catalog audit confirms outcome D — a new workout definition AND an `eligiblePhases` schema enum extension are both required, since none of `FOUNDATION/BUILD/RACE_SPECIFIC/TAPER` correctly represents the pre-Foundation runway period — **explicitly deferred to Phase 4G.6A.4**, not implemented in 4G.6A.2A. `PreSpecificTransition` is upgraded from `PARTIALLY_SUPPORTED` to `SUPPORTED` (content-supported via `EASY_STANDARD`/`LONG_RUN_STANDARD`, distinguished by fixed position and a volume-settle trajectory, directly precedented by the existing `TAPER_SHARPEN` stage reusing `EASY_STANDARD` at the plan's opposite boundary without being a duplicate of `FOUNDATION_EASY_BASE`). Net effect: the allocation **policy/contract** is now fully specified and unblocks Phase 4G.6A.3's dark-allocator design work; this TD remained **OPEN** because the AerobicStrength catalog **content** itself was still not authored and continued to block runtime activation of that block specifically. See `PHASE4G_6A_2A_TEN_K_AEROBIC_STRENGTH_AND_TRANSITION_CAPACITY_DECISION.md`. **Closure (Phase 4G.6A.4A — 10K Preparation Runway AerobicStrength Workout Definition, Schema Eligibility and Two-Week Catalog Progression):** two new versioned workout definitions were authored — `AEROBIC_STRENGTH_CONTROLLED_INTRO v1` and `AEROBIC_STRENGTH_CONTROLLED_PROGRESSED v1` (`plan-catalog/catalog/workouts/`), both `family=QUALITY`, `eligiblePhases=[PREPARATION_RUNWAY]` only (never a Core phase, so no existing eligibility mechanism can select them for Core), running-based controlled-aerobic-power content, confirmed free of threshold/maximal/all-out/goal-pace/target-finish-time/race-specific content by direct test assertion. The workout-definition JSON schema's `eligiblePhases` enum and the corresponding production C# `PhaseKey` enum were both extended with `PREPARATION_RUNWAY` (the real loader deserializes this field strictly, so both had to change together); all 18 pre-existing plus 2 new workout definitions re-verified schema-valid, existing phase values re-verified still accepted, an unknown phase value re-verified still rejected. A new, separate, dark, deliberately-unregistered document type (`PREPARATION_RUNWAY_BLOCK_PROGRESSION`) records the canonical two-step progression (Step 1 = INTRO, Step 2 = PROGRESSED, Step 1 precedes Step 2, no third step, no `RunwayWeeks` inspection) — confirmed inert to the real catalog loader by direct source inspection (it only iterates 10 fixed subfolders). The two new workouts are confirmed, via construction of the real published bundle for `TEN_K__4D__INTERMEDIATE v10`, absent from that bundle; Core Week 1 (`FOUNDATION_EASY_BASE`) is confirmed to still candidate only `EASY_STANDARD`. This phase does **not** implement the runtime binding that would consume an allocator's `AllocatedWeeks` and select these steps — that remains Phase 4G.6A.4B's scope. **`TD-AEROBIC-STRENGTH-SEMANTICS-001` is now `CLOSED`.** | No | `TEN_K__4D__INTERMEDIATE v10` specifically; any descendant candidate retaining the same workout-progression/catalog content inherits the same gap | **CLOSED** |
| `TD-LONG-HORIZON-REMAINING-CONCURRENCY-IDEMPOTENCY-COMMIT-AMBIGUITY-001` | **Remaining dark Long-Horizon concurrency/idempotency/commit-ambiguity matrix completed against real PostgreSQL.** A reusable test-only barrier coordinates independent `AppDbContext` instances on distinct `pg_backend_pid()` connections immediately before the authoritative `SaveChangesAsync`. Natural mixed 1+3/2+2/3+1, Core-only same/different key, Core refresh same/different evidence, block same/different reason, retry same/different decision, activation-vs-block, activation-vs-retry, terminal and cross-plan races have one durable winner and no split/duplicate ownership. Exact replay, acknowledgement-loss recovery, stale-context rejection/fresh reload, next-operation distinction and natural V1→V2→V3 history preservation are proven. Checkpoints are atomically embedded in activation rather than a separate repository save. The Phase 4L.2B parent is now `CLOSED`; confirmation/public/API/Home/Calendar/completion/Flutter remain unwired. See `PHASE4L_2G_REMAINING_CONCURRENCY_IDEMPOTENCY_AND_COMMIT_AMBIGUITY_MATRIX.md`. | No | `TEN_K__4D__INTERMEDIATE v10` dark Long-Horizon persistence only | **CLOSED** |
| `TD-LONG-HORIZON-CONFIRMATION-AND-PUBLIC-PREVIEW-WIRING-001` | **Authenticated Long-Horizon public preview and atomic rolling confirmation wiring completed.** Dedicated authenticated 21-52-week Race/TenK/Intermediate/4-day routes reuse the real initial rolling lifecycle, Phase 4L.1 public mapper/validator and Phase 4L.2 persistence authority. `PlanPreview` owns the internal snapshot, candidate identity, expiry, versions and SHA-256 fingerprints; confirmation accepts only PreviewId/version. A real user-owned `TrainingPlan` persists `RollingLongHorizon` and links uniquely to the rolling aggregate in one explicit PostgreSQL transaction. Exact replay, same-preview concurrency, one-active-plan conflict, four pre-commit rollback points, post-commit acknowledgement loss and cross-user non-disclosure are proven. Every structural rolling week persists, only the initial window is executable, and no fake static week/day represents Pending work. Home/Calendar return a typed deferred-read-surface result; Flutter/background/broad read integration remain unwired. See `PHASE4L_3_LONG_HORIZON_CONFIRMATION_AND_PUBLIC_PREVIEW_WIRING.md`. | No | `TEN_K__4D__INTERMEDIATE v10` authenticated 21-52-week Race preview and confirmation only | **CLOSED** |
| `TD-LONG-HORIZON-ACTIVE-READ-MODEL-WORKOUT-MUTATION-001` | **Long-Horizon active read model and durable workout outcome integration completed.** Existing Home/Calendar routes now dispatch by persisted `PlanScheduleStrategy` inside the application layer: static payloads stay unchanged, while rolling plans return explicit V1 responses containing only activated/completed sessions. The durable rolling session ID is the public identity for dedicated detail, completion and direct not-today routes. Additive outcome fields and optimistic `OutcomeVersion` provide immutable planned values/date, exact replay, typed conflicting replay, one-winner completion/not-today concurrency, cancellation serialization, rollback and acknowledgement-loss recovery. A transient adapter feeds real durable rolling outcomes into the existing unchanged checkpoint evidence input shape; no checkpoint or next window is activated by any read/mutation. Active details expose structural-only Pending weeks. No fake `TrainingDay`, Flutter change, background scheduler, numeric/calendar formula or downward interpolation was added. See `PHASE4L_4_LONG_HORIZON_ACTIVE_READ_MODEL_WORKOUT_MUTATION_AND_FLUTTER_CONTRACT_READINESS.md`. | No | `TEN_K__4D__INTERMEDIATE v10` authenticated confirmed 21-52-week RollingLongHorizon plans only | **CLOSED** |
| `TD-LONG-HORIZON-EXPLICIT-NEXT-WINDOW-ACTIVATION-API-001` | **Long-Horizon Explicit Next-Window Activation, Reassessment and Public Continuation API.** Phase 4L.4A's explicit activation authority is implemented and its remaining lifecycle-shape evidence was closed by Phase 4L.4F. | No | `TEN_K__4D__INTERMEDIATE v10` authenticated confirmed 21-52-week RollingLongHorizon plans only | **CLOSED** |
| `TD-LONG-HORIZON-PUBLIC-RETRY-ACTIVATION-SHAPE-RACE-COMPLETION-001` | **Public Retry, Remaining Activation Shapes and Cross-Operation Race Completion.** Explicit retry stays distinct from activation; remaining reachable shapes and race evidence were completed by Phase 4L.4F. | No | `TEN_K__4D__INTERMEDIATE v10` authenticated confirmed 21-52-week RollingLongHorizon plans only | **CLOSED** |
| `TD-LONG-HORIZON-BLOCKED-RECOVERY-NEW-EVIDENCE-REASSESSMENT-001` | **Blocked Recovery, New-Evidence Reassessment and Meaningful Retry Policy.** Persisted block reasons now map to explicit retry, regenerate-preview or operational-support outcomes without automatic replacement. | No | `TEN_K__4D__INTERMEDIATE v10` authenticated confirmed 21-52-week RollingLongHorizon plans only | **CLOSED** |
| `TD-LONG-HORIZON-PUBLIC-ACTIVATION-SHAPE-JIT-RACE-COMPLETION-001` | **Remaining Public Activation Lifecycle Shapes, JIT Boundary Classification and Cross-Operation Race Completion.** The real JIT boundary block and reachable public activation/race matrix are classified and proven. | No | `TEN_K__4D__INTERMEDIATE v10` authenticated confirmed 21-52-week RollingLongHorizon plans only | **CLOSED** |
| `TD-LONG-HORIZON-RUNWAY-CORE-SESSION-ROLE-EVIDENCE-NORMALIZATION-001` | **Runway/Core Session-Role Evidence Normalization and Historical Compatibility.** Canonical uppercase session roles are written while the known legacy PascalCase representation remains read-compatible. | No | `TEN_K__4D__INTERMEDIATE v10` authenticated confirmed 21-52-week RollingLongHorizon plans only | **CLOSED** |
| `TD-LONG-HORIZON-PUBLIC-LIFECYCLE-SHAPE-REPLAY-TERMINAL-CONCURRENCY-MATRIX-001` | **Remaining Public Lifecycle Shape, Replay, Terminal and Concurrency Matrix Closure.** Every structurally reachable lifecycle shape is proven through terminal completion; impossible mixed/partial shapes are documented as structurally unreachable. | No | `TEN_K__4D__INTERMEDIATE v10` authenticated confirmed 21-52-week RollingLongHorizon plans only | **CLOSED** |
| `TD-LONG-HORIZON-FLUTTER-READ-OUTCOME-RETRY-CONTINUATION-INTEGRATION-001` | **Long-Horizon Flutter Read, Workout Outcome, Retry, Continuation and Rolling-State Integration.** The original Flutter integration gaps are closed by Phases 4L.5A-4L.5C. | No | Flutter Long-Horizon pilot surfaces; static and Habit flows remain separate | **CLOSED** |
| `TD-LONG-HORIZON-FLUTTER-CALENDAR-ERROR-MAPPING-ACCESSIBILITY-TEST-CLOSURE-001` | **Flutter Calendar, Error Mapping, Accessibility and Integration Test Closure.** Calendar, error mapping, accessibility and the remaining connected-flow gaps are closed by Phase 4L.5C. | No | Flutter Long-Horizon pilot surfaces; static and Habit flows remain separate | **CLOSED** |
| `TD-LONG-HORIZON-FLUTTER-END-TO-END-FLOW-NAVIGATION-TEST-CLOSURE-001` | **Flutter End-to-End Flow, Navigation and Production-Wiring Test Closure.** Navigation, duplicate-mutation protection, regenerate/cancellation and authentication redirect evidence are closed by Phase 4L.5C. | No | Flutter Long-Horizon pilot surfaces; static and Habit flows remain separate | **CLOSED** |
| `TD-LONG-HORIZON-FLUTTER-REGENERATE-CANCELLATION-AUTH-AMBIGUITY-CLOSURE-001` | **Flutter Regenerate/Cancellation, Authentication Redirect and Mutation Ambiguity Closure.** Explicit destructive cancellation, authoritative read-after-write verification and protected-route behavior are implemented and tested. | No | Authenticated Flutter Long-Horizon pilot surfaces | **CLOSED** |
| `TD-LONG-HORIZON-END-TO-END-RELEASE-ACCEPTANCE-PRODUCTION-READINESS-001` | **Long-Horizon End-to-End Release Acceptance and Production Readiness.** Local automated evidence cannot substitute for production-like migration rehearsal, authenticated real-device E2E, deployment/configuration, observability, rollout ownership and rollback-drill evidence. See `PHASE4L_6_END_TO_END_RELEASE_ACCEPTANCE_GOVERNANCE_TEST_DEBT_CLOSURE_AND_PRODUCTION_READINESS.md`. | **Yes — P1 release blocker** | `TEN_K__4D__INTERMEDIATE v10` Long-Horizon 21-52-week production pilot | **OPEN** |
| `TD-LONG-HORIZON-MOBILE-RELEASE-ARTIFACT-BACKEND-CATALOG-PACKAGING-001` | **Mobile Release Artifact and Backend Catalog Packaging Closure.** The backend Release artifact now packages and validates all 71 canonical runtime JSON files and published Long-Horizon smoke succeeds. Mobile remains blocked by absent approved non-example application identity, matching Firebase registration, production signing ownership and fresh APK/AAB proof. See `PHASE4L_6A_MOBILE_RELEASE_ARTIFACT_AND_BACKEND_CATALOG_PACKAGING_CLOSURE.md`. | **Yes — P1 release blocker, backend portion locally proven** | Flutter Android and published backend artifacts for `TEN_K__4D__INTERMEDIATE v10` Long-Horizon 21-52-week pilot | **OPEN** |
| `TD-LONG-HORIZON-PRODUCTION-CONFIGURATION-SECURITY-MIGRATION-ROLLBACK-001` | **Production Configuration, Security, Migration and Forward-Schema Rollback Closure.** Production configuration is fail-fast and secure (no localhost DB fallback, Mock auth rejected, restrictive CORS, explicit proxy/HTTPS trust); a real migration bundle artifact is proven against empty, pre-Long-Horizon and current databases with real seeded data and semantic preservation. Application rollback was proven `ROLLBACK_SAFE_WITH_EXPLICIT_MUTATION_GUARD` in Phase 4L.6B.2 after the cancel write-safety gap was closed with a PostgreSQL-level guard. See `PHASE4L_6B_PRODUCTION_CONFIGURATION_SECURITY_MIGRATION_AND_FORWARD_SCHEMA_ROLLBACK_CLOSURE.md` and `PHASE4L_6B_2_ROLLING_PLAN_ROLLBACK_MUTATION_GUARD_CLOSURE.md`. | No | Backend production deployment for `TEN_K__4D__INTERMEDIATE v10` Long-Horizon 21-52-week pilot | **CLOSED** |
| `TD-LONG-HORIZON-ROLLBACK-MUTATION-GUARD-001` | **Rolling-Plan Rollback Mutation Guard Closure.** A PostgreSQL trigger on `TrainingPlans`, gated by a client-unreachable `RollbackCompatibilityMode` control row that fails closed by default, blocks any write to a `RollingLongHorizon` plan while enabled — enforced independent of which application binary is connected. Proven against the real committed-HEAD binary (the previously-successful cancel defect now fails safely), a direct raw-SQL bypass attempt, repeated/concurrent attempts, and full rolling-state reconstruction after redeploying the current application. See `PHASE4L_6B_2_ROLLING_PLAN_ROLLBACK_MUTATION_GUARD_CLOSURE.md`. | No | Backend production application rollback for `TEN_K__4D__INTERMEDIATE v10` Long-Horizon 21-52-week pilot; static and Habit rollback unaffected | **CLOSED** |
| `TD-LONG-HORIZON-CI-CD-KILL-SWITCH-OBSERVABILITY-STAGING-UAT-001` | **CI/CD, Generation Kill Switch, Observability and Production-Equivalent Staging UAT Closure.** A generation-only kill switch (`LongHorizon:GenerationEnabled`) and structured operational observability are closed with real evidence (full drill: generate/confirm → disable → existing plan fully usable → re-enable → new generation resumes). A GitHub Actions CI authority now exists, config-validated with every command independently proven locally, but no hosted run was executed. No production-equivalent staging environment exists, so the required 44-scenario authenticated UAT matrix is entirely NOT RUN. See `PHASE4L_6C_CI_CD_GENERATION_KILL_SWITCH_OBSERVABILITY_AND_STAGING_UAT_CLOSURE.md`. | **Yes — P1 release blocker, kill-switch/observability portions closed, CI run and staging UAT unproven** | Backend release pipeline, generation rollout control and observability for `TEN_K__4D__INTERMEDIATE v10` Long-Horizon 21-52-week pilot; static and Habit unaffected | **OPEN** |
| `TD-LONG-HORIZON-EXTERNAL-RELEASE-ENVIRONMENT-FINAL-REACCEPTANCE-001` | **External Release Environment Activation and Final End-to-End Re-Acceptance.** Verified directly that ten of twelve required external release authorities (CI execution access, staging deployment target, production Android identity, matching Firebase client, production signing, HTTPS/domain, observability platform, named owners) are genuinely missing from this environment — no `gh`/token, no `APPSEL_*` variables set, Firebase client still `com.example.antigravity_app`, only a mechanical-testing keystore present, no staging domain anywhere in the repository. No code changed; no evidence fabricated. See `PHASE4L_6D_EXTERNAL_RELEASE_ENVIRONMENT_ACTIVATION_AND_FINAL_RE_ACCEPTANCE.md`. | **Yes — P1 release blocker, terminal for this release cycle within this environment** | Final production release of `TEN_K__4D__INTERMEDIATE v10` Long-Horizon 21-52-week pilot | **OPEN** |

### Current-state clarifications (Phase 4G.3B.10A)

- `TD-FOUNDATION-COMPRESSION-001`: the original Phase 4G.3B.9 closure scan found 16 production files. The current repository contains 17 matching files after the later addition of the dark Preparation Runway contract. The additional reference is the neutral `PreparationNeedProfile.CoreEntryReadiness` field in `PreparationRunwayContracts.cs`; it is not consumed by `CatalogPhaseAllocationResolver` or any live race-core allocation path. The runtime non-reachability proof and `CLOSED_AS_NON_REACHABLE_IN_CURRENT_SUPPORTED_SCOPE` classification remain unchanged.
- `TD-VOLUME-CAP-UNENFORCED-001`: `CatalogVolumeAndLongRunPlanner` does not clamp, reject, or alter its live reachable-peak result using `HardMaxWeeklyIncreaseRatio` or `AbsoluteWeeklyIncrementCapKm`. The dark `VolumeProgressionVerifier` reads both fields and can report violations, but it is not wired into `CatalogVolumeAndLongRunPlanner` or any public generation path. “Not enforced by the live planner” does not mean “not referenced anywhere in the repository”; the scoped closure remains `CLOSED`.

### Allocation-priority verifier clarification (Phase 4G.5B.0)

For `TD-ALLOCATION-PRIORITY-001`, the 13-week allocation remains theoretically order-dependent, but it is executable and deterministic under the approved Foundation-first extension priority. Order dependence alone is no longer a `DecisionRequired` outcome once an explicit approved policy is supplied; missing or invalid priority approval still fails closed. The verifier remains dark and public horizon support is unchanged.

### Wiring-gap addition (Phase 4G.5C)

`TD-COREHORIZON-ALLOCATOR-UNWIRED-001` was added `OPEN` during Phase 4G.5C's Thirteen-Week Core Extension Decision Resolution pass. It does not affect `TD-ALLOCATION-PRIORITY-001`'s `CLOSED` status or its Foundation-first extension-priority evidence — the two risks are unrelated: this new entry tracks the absence of mechanical wiring between `CoreHorizonClassifier` and `CatalogPhaseAllocationResolver`, not the allocation-order/priority question itself. See `activation-readiness-risks.json`'s `currentAppendOnlyStatus` field for the authoritative running count.

### Volume-cap algebraic proof re-verification at scale (Phase 4G.5F)

For `TD-VOLUME-CAP-UNENFORCED-001`, its closing algebraic proof (Phase 4G.3B.7) was re-verified beyond its original scope: previously confirmed only for `TEN_K_MASTER v6`'s 12-week candidate with one fixed runner-readiness fixture, this pass re-ran the same real, unmodified `CatalogVolumeAndLongRunPlanner`/`VolumeProgressionVerifier` pipeline across all 7 feasible target week counts (8-14) **and** six distinct runner-readiness profiles (low-volume intermediate, the existing pilot profile, higher-volume intermediate, missing recent long run, missing recent volume, explicit zero evidence) — 42 combinations total, zero `HardMaxWeeklyIncreaseRatio`/`AbsoluteWeeklyIncrementCapKm` violations found in any of them. This is confirming evidence, not a new finding: the TD remains `CLOSED`, its scope note ("verified safe only for the current `TEN_K__4D__INTERMEDIATE` 8-14-week range") is now empirically broader than when it was written. See `PHASE4G_5F_DYNAMIC_CORE_VOLUME_AND_LONG_RUN.md` for the full re-verification.

Future passes should append new risk entries here rather than creating a new aggregator file.

### Core-horizon wiring closure (Phase 4G.5J/4G.5K)

`TD-COREHORIZON-ALLOCATOR-UNWIRED-001` is now `CLOSED`. The recorded architecture decision is that `CoreHorizonDecision` owns upstream, day-accurate eligibility and supplies `AvailableFullWeeks` as the target count; `CatalogPhaseAllocationResolver` owns only phase distribution. `CatalogPreviewGenerator` is the single live composition point. Partial days are never rounded into an extra allocated week. Reopen this TD if another live caller derives the target count independently or allocation begins interpreting partial days.

### AEROBIC_STRENGTH semantic resolution and capacity gap (Phase 4G.6A.2)

`TD-AEROBIC-STRENGTH-SEMANTICS-001` was added `OPEN` during Phase 4G.6A.2's 10K Preparation Runway Block Semantics, Eligibility, Target-Behavior Matrix, Capacity and Coefficient Decision Resolution pass. It does not affect `TD-GENERAL-ENDURANCE-STAGED-PLAN-001`'s scope (21-52 week staged General Endurance architecture) — the two risks are unrelated: this new entry tracks the 3-8-week Preparation Runway `AEROBIC_STRENGTH` block specifically. See `PHASE4G_6A_2_TEN_K_RUNWAY_COEFFICIENT_AND_ELIGIBILITY_DECISION.md` for the full decision record, including the resolved `CONSISTENCY_NEEDED` profile (approved) and the blocked `CORE_ENTRY_READY` profile (blocked by this TD).

### AEROBIC_STRENGTH catalog capacity closure (Phase 4G.6A.4A)

`TD-AEROBIC-STRENGTH-SEMANTICS-001` is now `CLOSED` — see its own row above for the full closure evidence. Its closure does not affect `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` (unrelated, still `OPEN`, tracks the 21-52 week staged architecture). This phase implemented catalog capacity only (two workout definitions, one schema/enum extension, one dark progression-mapping document) — no allocator, runtime binding, workout generation, or public/persistence behavior changed. See `PHASE4G_6A_4A_TEN_K_PREPARATION_RUNWAY_AEROBIC_STRENGTH_CATALOG_CAPACITY.md` for the full decision record.

### 21-52 week composition/allocation formula closure (Phase 4I.1)

`TD-LONG-HORIZON-COMPOSITION-001` was added `CLOSED` during Phase 4I.1's 21-52 Week Product Composition and Allocation Policy Resolution pass. It narrows only the composition/allocation-formula half of `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` (approved architecture direction from Phase 4G.6A.1) into one authoritative numeric formula (`GE = AvailableFullWeeks - 20`, `Runway = 8`, `Core = 12`, for `21..52`). `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` itself remains `OPEN` and unaffected in scope — General Endurance safety policy (mesocycle structure, recovery/deload, progression caps, long-run progression, workout repetition, GE-to-Runway transition loading), rolling-materialization mechanics, segment-provenance persistence shape, and transition orchestration are all still undecided and still block runtime activation. This phase is decision/governance only: no production runtime code, resolver, materializer, catalog workout, schema, migration, preview, confirmation, persistence, or Flutter code was changed. See `PHASE4I_1_21_TO_52_WEEK_PRODUCT_COMPOSITION_AND_ALLOCATION_POLICY_RESOLUTION.md` for the full decision record.

### Long-Horizon General Endurance safety/mesocycle/progression policy closure (Phase 4I.2)

`TD-LONG-HORIZON-GE-SAFETY-001` was added `CLOSED` during Phase 4I.2's Long-Horizon General Endurance Safety, Mesocycle and Progression Policy Resolution pass. It resolves the safety/mesocycle/progression half of `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` that Phase 4I.1 explicitly deferred — complete structural policy (mesocycle cadence, stage families, weekly roles, intensity boundary, `ShortExtension`/`FullPhase` structures, remainder placement, GE-to-Runway continuity, repetition constraints, advisory checkpoints, missing-data handling, safety non-claims) plus the development-week numeric progression framework (reusing `VolumeSafetyPolicy.Default`, not a universal 10-percent rule) are approved; the recovery-week numeric reduction magnitude alone remains unresolved and was explicitly not invented. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN` — rolling-materialization mechanics, segment-provenance persistence shape, transition orchestration, and actual catalog/workout content are all still undecided and still block runtime activation. This phase is decision/governance only: no production runtime code, resolver, materializer, catalog workout, schema, migration, preview, confirmation, persistence, or Flutter code was changed. See `PHASE4I_2_LONG_HORIZON_GENERAL_ENDURANCE_SAFETY_MESOCYCLE_AND_PROGRESSION_POLICY_RESOLUTION.md` for the full decision record.

### Long-Horizon General Endurance recovery-week numeric magnitude closure (Phase 4I.2A)

`TD-LONG-HORIZON-GE-RECOVERY-MAGNITUDE-001` was added `CLOSED` during Phase 4I.2A's narrow, single-question follow-up pass. It closes the exact numeric gap `TD-LONG-HORIZON-GE-SAFETY-001` left open: a 15-percent recovery-week volume reduction from the mesocycle's prior development peak (the conservative bound of `progression_rules_v2.yaml`'s canonical `INTERMEDIATE` cutback-policy range `[0.15,0.20]` — a real, precedence-level-2 governance document Phase 4I.2's own evidence review had not surfaced), with long run recomputed via the existing 0.33 `LongRunSelectionShare`, both readiness profiles using the identical ratio, and return-from-recovery reusing the document's own `PREVIOUS_NON_CUTBACK_VOLUME` baseline rule. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN` — this decision closes only the numeric-policy gap, not rolling materialization, segment-provenance persistence, transition orchestration, or catalog content, all of which still block runtime activation. This phase is decision/governance only: no production runtime code, resolver, materializer, catalog workout, schema, migration, preview, confirmation, persistence, or Flutter code was changed. See `PHASE4I_2A_LONG_HORIZON_GENERAL_ENDURANCE_RECOVERY_WEEK_NUMERIC_REDUCTION_MAGNITUDE_RESOLUTION.md` for the full decision record.

### Typed 21-52 week runtime decision contract implemented (Phase 4I.3)

`TD-LONG-HORIZON-TYPED-RUNTIME-DECISION-001` was added `CLOSED` during Phase 4I.3 — the first real production code for the Long-Horizon line. `LongHorizonCompositionResolver` (dark, unwired, not DI-registered, not called from any live request path) converts the three approved governance policies (`TD-LONG-HORIZON-COMPOSITION-001`/`TD-LONG-HORIZON-GE-SAFETY-001`/`TD-LONG-HORIZON-GE-RECOVERY-MAGNITUDE-001`) into one authoritative 8-to-52-week typed decision, consuming the existing unchanged `CoreHorizonDecision` output rather than re-deriving anything. Zero live HTTP behavior changed — proven by new real E2E tests (Api host + Postgres) showing 21/24/52/53-week requests all still return HTTP 422 `PLAN_HORIZON_COMPOSITION_REQUIRED` with zero persistence, unchanged from before this phase. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN` — catalog/schema, materialization, numeric execution, calendar assignment, public preview, confirmation, and persistence all remain entirely unimplemented. See `PHASE4I_3_TYPED_21_TO_52_WEEK_RUNTIME_ELIGIBILITY_AND_COMPOSITION_DECISION_CONTRACT.md` for the full decision record.

### Long-Horizon General Endurance catalog and schema capacity implemented (Phase 4I.4)

`TD-LONG-HORIZON-GE-CATALOG-CAPACITY-001` was added `CLOSED` during Phase 4I.4. Real catalog/schema capacity now exists for all 1-32 GE weeks: four `eligiblePhases`-extension workout versions (no new workout keys — existing `EASY_STANDARD`/`LONG_RUN_STANDARD`/`AEROBIC_STRENGTH_CONTROLLED_INTRO`/`_PROGRESSED` already covered every approved semantic), a new dark stage-family catalog document, and a deterministic C# selector proving non-exhausting, profile-specific selection (including the mandatory standalone 32-week exhaustion proof). Zero live/public behavior changed — the new catalog folder is confirmed not iterated by the real loader and the new workout versions are confirmed absent from the real published bundle, mirroring the exact containment mechanism already proven for the Preparation Runway's own dark progression documents. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN` — structural materialization, numeric execution, calendar assignment, dark pipeline wiring, public preview, confirmation, and persistence all remain entirely unimplemented. See `PHASE4I_4_LONG_HORIZON_GENERAL_ENDURANCE_CATALOG_AND_SCHEMA_CAPACITY.md` for the full decision record.

### Dark 21-52 week structural materialization implemented (Phase 4I.5)

`TD-LONG-HORIZON-STRUCTURAL-MATERIALIZATION-001` was added `CLOSED` during Phase 4I.5. A dark, unwired `LongHorizonStructuralMaterializer` now joins the typed composition decision (Phase 4I.3), a backend-mirrored GE catalog selector (Phase 4I.4's algorithm, independently mirrored per `TD-BACKEND-001`), and the existing, unchanged Preparation Runway and Core structural materializers into one contiguous, globally-numbered 21-52 week skeleton with authoritative per-segment provenance. Zero numeric volume/pace, zero calendar dates, zero new public call sites — confirmed by grep and by the pre-existing real E2E containment suite continuing to pass unchanged. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN` — numeric execution, calendar assignment, dark pipeline wiring, public preview, confirmation, and persistence all remain entirely unimplemented. See `PHASE4I_5_DARK_21_TO_52_WEEK_STRUCTURAL_MATERIALIZATION.md` for the full decision record.

### Long-Horizon GE numeric execution and Core workout-binding gap closed; Runway/Core numeric remains unexecuted (Phase 4I.6)

`TD-LONG-HORIZON-NUMERIC-CALENDAR-EXECUTION-001` was added `OPEN` during Phase 4I.6 — a deliberate, honestly disclosed **partial** closure, not a full one. GE numeric execution (development progression, recovery formula, weekly distribution, long-run calculation) is real and fully tested; the Phase 4I.5 Core null-workout-reference gap is closed via the existing, unchanged Core workout-binding authority; every slot across all three segments now carries a real assigned calendar date. However, Preparation Runway and Core numeric (volume/pace) execution for the 21-52 week path were not implemented this phase — both require deeper integration with existing numeric/pace pipelines that are structurally coupled to assumptions (Core-derived Runway entry evidence; live goal-feasibility/target-time context) that do not cleanly apply to the joined Long-Horizon skeleton. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4I_6_DARK_LONG_HORIZON_WORKOUT_BINDING_NUMERIC_PROGRESSIVE_LOADING_TRANSITION_AND_CALENDAR_ASSIGNMENT.md` for the full decision record.

### Real Runway/Core numeric execution for short-GE horizons; existing-pipeline gap bounded for longer GE horizons (Phase 4I.6A)

`TD-LONG-HORIZON-RUNWAY-CORE-NUMERIC-CONTEXT-INTEGRATION-001` was added `OPEN` during Phase 4I.6A. Real, end-to-end Preparation Runway and Core numeric/pace/calendar execution now works for 21/24-total-week Long-Horizon plans (both profiles), reusing the existing Runway numeric/calendar/pace stages (fed a GE-exit-derived entry evidence instead of the existing Core-derived one) and the existing Core generation pipeline, driven by real `RuntimeConditionResolutionService` resolution rather than a fabricated stand-in. A genuine, precisely-bounded existing-pipeline compatibility gap was discovered: longer GE horizons (28-52 weeks) fail closed because the existing Runway numeric materializer has no accepted rule for the case where GE's compounding progression exceeds Core's independently-computed peak-volume-band ceiling outside of Taper — this is not resolved and no new rule was invented to force a false success. `TD-LONG-HORIZON-NUMERIC-CALENDAR-EXECUTION-001` was updated accordingly (still `OPEN`). `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4I_6A_LONG_HORIZON_RUNWAY_AND_CORE_NUMERIC_CONTEXT_INTEGRATION_AND_FULL_TRANSITION_CONTINUITY_RESOLUTION.md` for the full decision record.

### Runway entry-above-Core-target transition: structural decisions resolved, numeric magnitude remains an open evidence gap (Phase 4I.6B)

`TD-LONG-HORIZON-RUNWAY-ENTRY-ABOVE-CORE-TARGET-001` was added `OPEN` during Phase 4I.6B — a pure governance/policy-decision pass, zero production code changed. Resolved: Runway's semantic role (entry consolidation at the Core target), rejection of a GE-side ceiling, rejection of Core-target lifting, profile-neutral applicability, workout/pace unaffected. Unresolved: real diagnostic evidence shows the GE-exit-vs-Core-target excess grows unboundedly with horizon (~4km at 28 weeks to ~41km at 52 weeks, GE peaking near 71.5km/week by week 52) with no canonical rule, approved default, generalizable production behavior, or domain evidence for a safe multi-week reduction formula of that magnitude — an "immediate ceiling clamp" was evaluated and rejected for large excesses as an unevidenced abrupt-drop risk. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4I_6B_LONG_HORIZON_RUNWAY_ENTRY_ABOVE_CORE_TARGET_NUMERIC_TRANSITION_POLICY_RESOLUTION.md` for the full decision record.

### Long-Horizon numeric activation narrowed to 21 weeks; Path A deferred (Phase 4I.6B.1)

`TD-LONG-HORIZON-NUMERIC-ACTIVATION-BOUNDARY-001` was added `CLOSED` during Phase 4I.6B.1 — a real, 192-combination full-matrix scan of the unchanged production pipeline proved `MaxNumericallySupportedTotalWeeks = 21`: the only horizon where GE involves no progression at all. Every horizon 22-52 fails for every tested input class (24 weeks is a real but non-contiguous "isolated later success," correctly excluded per this phase's own instruction). The 21-52 week composition policy remains fully preserved; only runtime numeric activation is narrowed. `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` was added `OPEN`/`DEFERRED` as the explicit Path A placeholder this finding motivates. `TD-LONG-HORIZON-RUNWAY-ENTRY-ABOVE-CORE-TARGET-001` was updated: non-blocking for the 21-week activated range, fully blocking for 22-52. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4I_6B_1_SUPPORTED_LONG_HORIZON_NUMERIC_BOUNDARY_RESOLUTION.md` for the full decision record.

### Rolling numeric activation policy approved, replacing the rejected full-upfront model (Phase 4K.1)

`TD-LONG-HORIZON-ROLLING-NUMERIC-ACTIVATION-001` was added `CLOSED` during Phase 4K.1 — a pure governance/policy-decision pass, zero production code changed, zero diagnostics required. Approved: a full 21-52 week structural roadmap combined with a 4-week rolling numeric activation window (an Appsel product default matching the existing GE mesocycle), six lifecycle states, checkpoint authority scoped to the next window only, a fail-closed missing-evidence principle, structural immutability, and a Runway/Core just-in-time numeric-resolution principle. This formally replaces Path B's narrow one-horizon (21-week-only) activation model, which must not be implemented. Detailed maintenance formula (Phase 4K.2), checkpoint evidence thresholds (Phase 4K.3), and exact Runway/Core JIT timing (Phase 4K.4) all remain explicitly deferred — this phase approved only the shape of the solution. `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` was updated to reflect the now-approved direction. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4K_1_LONG_HORIZON_ROLLING_NUMERIC_ACTIVATION_AND_REASSESSMENT_AUTHORITY_POLICY_RESOLUTION.md` for the full decision record.

### Validated load, Growth/Maintenance eligibility and maintenance envelope approved (Phase 4K.2)

`TD-LONG-HORIZON-VALIDATED-LOAD-AND-MAINTENANCE-ENVELOPE-001` was added `CLOSED` during Phase 4K.2 — a pure governance/policy-decision pass, zero production code changed. Approved: a validated-weekly-load formula reusing real `TrainingDay` completion evidence (never planned distance), an evidence-conservative recovery-week exclusion, a validated long-run formula bounded by the existing 0.40 hard-cap share, Growth/Maintenance eligibility conditions, and a maintenance anchor/envelope defined structurally as the validated load itself — explicitly not a new percentage of anything. Zero new numeric constants were invented; the existing 0.85/0.33/0.5km/0.07/0.08/2.5km values and the existing `AdherenceRatePercent` term are reused verbatim. Adherence threshold and evidence-freshness rules remain deferred to Phase 4K.3; Runway/Core JIT timing remains deferred to Phase 4K.4. `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` was updated accordingly. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4K_2_VALIDATED_LOAD_GROWTH_ELIGIBILITY_AND_MAINTENANCE_ENVELOPE_POLICY_RESOLUTION.md` for the full decision record.

### Checkpoint evidence, freshness and deterministic state-transition policy approved (Phase 4K.3)

`TD-LONG-HORIZON-CHECKPOINT-EVIDENCE-STATE-TRANSITION-001` was added `CLOSED` during Phase 4K.3 — a pure governance/policy-decision pass, zero production code changed. Approved: a conceptual checkpoint evidence snapshot sourced exclusively from real `TrainingDay` history; a 4-week activation-window freshness default for weekly-volume/long-run evidence (the existing race-pace recency ladder is explicitly not reused for this different evidence type); a terminal-window rule requiring every session to reach a terminal state before a next-window decision fires; explicit partial/missed/NotToday treatment reusing Phase 4K.2's zero-counting rules; a deterministic, non-invented adherence confidence gate (no accepted percentage exists in the repository and none was invented); a fail-closed safety rule with top evaluation priority; an eight-case, priority-ordered, mutually exclusive state-transition table; and a nine-reason typed taxonomy. `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` was updated accordingly. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4K_3_CHECKPOINT_EVIDENCE_FRESHNESS_AND_NUMERIC_STATE_TRANSITION_POLICY_RESOLUTION.md` for the full decision record.

### Runway and Core just-in-time context resolution timing approved (Phase 4K.4)

`TD-LONG-HORIZON-RUNWAY-CORE-JIT-CONTEXT-001` was added `CLOSED` during Phase 4K.4 — a pure governance/policy-decision pass, zero production code changed, but real dependency tracing of the existing pipeline was performed (not simulated). Found: Runway's numeric materializer structurally requires Core's Week-1 target as a validated, non-null input, and computes Runway's own values via direct interpolation toward it — a real, existing architectural coupling. Approved: atomic Runway+Core-target resolution at the checkpoint activating the first window containing a Runway week; only in-window Core weeks activate, later Core weeks refresh at their own future checkpoint; the Core Week-1 target is locked for already-activated Runway weeks while remaining versioned/refreshable for not-yet-activated Core weeks; neither segment's existing evidence source is changed (only resolution timing); mixed-window activation is all-or-nothing; ten new JIT-specific reason codes were approved. `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` was updated accordingly. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4K_4_RUNWAY_AND_CORE_JUST_IN_TIME_NUMERIC_CONTEXT_POLICY_RESOLUTION.md` for the full decision record.

### Rolling-activation typed contracts implemented; Core Week-1 rolling evidence authority left explicitly open (Phase 4K.5)

`TD-LONG-HORIZON-ROLLING-TYPED-CONTRACTS-001` was added `CLOSED` during Phase 4K.5 — dark, unwired production C# contracts implementing Phases 4K.1–4K.4's approved policy shapes (structural roadmap, numeric lifecycle, activation window, numeric week, checkpoint evidence/validated-load/decision, evidence-authority, JIT context, Core target lock, versioning, initial activation), zero production wiring, 52 new tests, full backend suite 2506 passed/0 failed. `TD-LONG-HORIZON-CORE-TARGET-EVIDENCE-AUTHORITY-001` was added `OPEN` (blocking) — the rolling runtime's final Core Week-1 evidence authority was deliberately left unresolved rather than assumed, per this phase's own explicit instruction. `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` was updated accordingly, remains `OPEN`. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4K_5_STRUCTURAL_ROADMAP_ROLLING_NUMERIC_ACTIVATION_AND_JIT_CONTEXT_TYPED_CONTRACTS.md` for the full decision record.

Historical Phase 4K.5A aggregate: 36 risks, 14 `OPEN`, 22 `CLOSED`.

### Core Week-1 rolling JIT evidence authority resolved (Phase 4K.5A)

`TD-LONG-HORIZON-CORE-TARGET-EVIDENCE-AUTHORITY-001` is now `CLOSED`. The selected authority is Option A: fresh current validated checkpoint evidence is mapped into the existing Core input seam, and the existing unchanged Core generator remains the prescription authority. Weekly load maps to `RecentWeeklyVolumeKm`; validated long run maps to `RecentLongestRunKm`. Completed history is the only permitted source for optional `RecentRunsPerWeek` readiness metadata; an exact validated integer may be carried, otherwise it remains absent because no rounding rule is invented. Current declared four-day availability remains the separate structural-feasibility authority. A prior still-valid validated checkpoint is allowed only where Phase 4K.3 permits it; otherwise missing weekly or long-run evidence blocks JIT. Original onboarding evidence is rolling provenance only and remains separately described as today's legacy production source. Planned GE exit remains provenance only. No runtime, persistence, API, Flutter or public-preview wiring was implemented. `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` remains `OPEN` for the deferred runtime work. See `PHASE4K_5A_CORE_WEEK_ONE_ROLLING_JIT_EVIDENCE_AUTHORITY_RESOLUTION.md`.

### Dark rolling General Endurance initial numeric runtime implemented (Phase 4K.6)

`TD-LONG-HORIZON-ROLLING-GE-INITIAL-RUNTIME-001` was added `CLOSED`. Applicability is restricted to Race / exact 10K / Intermediate / four days per week / 21–52 structural weeks / valid readiness and catalog inputs. Internal entry point: `ILongHorizonRollingInitialActivationRuntime.BuildInitialActivationAsync`, implemented by `LongHorizonRollingInitialActivationRuntime` with no public DI registration. It reuses `LongHorizonStructuralMaterializer` for the complete `GE=TotalWeeks-20`, `Runway=8`, `Core=12` roadmap and numerically materializes only GE weeks `1..min(4, GE weeks)` through the existing `LongHorizonGeNumericExecutor`, `VolumeSafetyPolicy.Default`, `FourDaySessionDistanceAllocationPolicy`, catalog workout references, and `LongHorizonCalendarAssigner`. The requested size remains four; 21/22/23-week plans activate only 1/2/3 GE weeks, borrow no Runway week, and invent no recovery; a real GE Week 4 uses the unchanged 0.85 recovery rule. Evidence is typed `InitialOnboardingActivation` / `OriginalOnboardingEvidence` / `LegacyCurrentProductionSource`, never `CompletedTrainingHistory`. Every later GE, Runway, and Core week remains `NumericPending` with null executable fields. Activation is atomic and blocked results preserve exactly one typed reason plus the original failure type/message. Production validator order is input/eligibility → roadmap → context → window → GE numeric → numeric weeks → atomicity → final result. Integration level is an independently invokable dark service exercised by tests only. Public preview, persistence, API, DI, Flutter, checkpoint aggregation, next-window activation, and Runway/Core JIT remain unimplemented. `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` remains `OPEN`, narrowed by this initial-window implementation but not closed. See `PHASE4K_6_ROLLING_GENERAL_ENDURANCE_NUMERIC_RUNTIME_AND_INITIAL_WINDOW_IMPLEMENTATION.md`.

### Dark checkpoint and next-GE-window runtime implemented (Phase 4K.7)

`TD-LONG-HORIZON-CHECKPOINT-NEXT-WINDOW-RUNTIME-001` was added `CLOSED`. The internal runtime consumes supplied real `TrainingDay` rows for the most recently activated window, validates completed-history weekly and long-run load, evaluates the approved safety-first checkpoint transition table, then atomically activates at most one bounded next GE window. Planned distance and evidence outside the current window never enter aggregation. A fresh prior validated anchor can authorize Maintenance only; it never authorizes Growth. Growth reuses the existing GE executor. Maintenance introduces no new multiplier and preserves the existing 0.85 recovery behavior. Reaching Runway returns a non-error boundary result with Runway/Core still `NumericPending`; no JIT context or Core target lock is created. The runtime remains dark, supplied-collection-only, unpersisted and absent from public preview/API/DI/Flutter. `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` remains `OPEN` for Runway/Core JIT, full lifecycle validation/retry, persistence and product wiring.

### Runway direction and bounded-exposure authorities resolved (Phase 4K.8A)

`TD-LONG-HORIZON-RUNWAY-DOWNWARD-CONSOLIDATION-AUTHORITY-001` and `TD-LONG-HORIZON-BOUNDED-RUNWAY-MATERIALIZATION-001` were added `CLOSED`. Append-only correction of `TD-LONG-HORIZON-RUNWAY-CORE-JIT-CONTEXT-001` records that existing Runway interpolation does not support negative weekly movement. The supported weekly relations are below→capped upward and equal→flat; above remains fail-closed. Long-run direction is evaluated separately after existing band/cap normalization, with all existing continuity/share/rounding validators retained. The selected bounded strategy keeps the existing complete 3–8-week output as the sole numeric authority, locks one target across the complete Runway, and exposes exact immutable slices without slice-local interpolation. Phase 4K.8B must implement this contract; Phase 4K.8 runtime, full lifecycle validation, persistence, public/API and Flutter remain incomplete.

### Runway direction guard and bounded prescription production contracts implemented (Phase 4K.8B)

`TD-LONG-HORIZON-RUNWAY-BOUNDED-PRESCRIPTION-CONTRACTS-001` was added `CLOSED`. Dark, unwired production contracts implement Phase 4K.8A's approved direction authority and bounded-materialization strategy: a typed direction relation/comparer/policy/guard mapping unsupported (above-target) relations to the existing `JIT_SEGMENT_TRANSITION_INFEASIBLE` reason; an immutable full Runway prescription contract wrapping the unchanged numeric materializer's own output with deterministic SHA-256 identity; a week-reference contract that reads values directly from the real production object rather than recomputing them; a one-lock-per-full-range target-lock scope with a mid-Runway refresh guard; and a bounded 1–4-week slice contract whose factory structurally cannot invoke the numeric materializer and whose exact-equivalence validator runs in production code, not only tests. 70 new tests pass, built from real materializer results via the existing test fixtures. Phase 4K.8's JIT runtime, mixed-window activation, persistence, public preview, API, and Flutter all remain entirely unimplemented. `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` remains `OPEN`. See `PHASE4K_8B_PREPARATION_RUNWAY_DIRECTION_GUARD_AND_BOUNDED_PRESCRIPTION_CONTRACT_IMPLEMENTATION.md`.

### Runway and Core JIT numeric runtime and mixed-segment window activation implemented (Phase 4K.8)

`TD-LONG-HORIZON-RUNWAY-CORE-JIT-RUNTIME-001` was added `CLOSED` — a dark, unwired orchestration runtime consuming (never redesigning) the Phase 4K.5/4K.8B typed contracts, resolving and atomically activating one next rolling window across GE+Runway/Runway-only/Runway+Core/Core-only combinations. The unchanged Runway materializer runs exactly once per Runway segment (reused across continuation windows); Core generation and condition resolution remain the caller's own real, unchanged responsibility, consumed as inputs. 41 new tests pass; zero numeric-formula change; zero public/persistence/API/Flutter wiring. `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` was updated accordingly, remains `OPEN`. `TD-LONG-HORIZON-RUNWAY-CORE-JIT-CONTEXT-001` and `TD-LONG-HORIZON-RUNWAY-BOUNDED-PRESCRIPTION-CONTRACTS-001` each received an append-only update. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4K_8_RUNWAY_AND_CORE_JIT_NUMERIC_RUNTIME_AND_MIXED_SEGMENT_WINDOW_ACTIVATION.md` for the full decision record.

### Real Core target, runtime condition and session-level calendar composition adapter implemented (Phase 4K.8C)

`TD-LONG-HORIZON-JIT-REAL-CORE-CONDITION-CALENDAR-COMPOSITION-001` was added `CLOSED` — a dark, unwired composition orchestrator (`LongHorizonRollingJitCompositionOrchestrator`) that closes Phase 4K.8's own disclosed gap: the Core Week-1 target, available Core weeks, and condition results Phase 4K.8's request previously accepted as caller-fabricated inputs are now produced for real, via the real unmodified `RuntimeConditionResolutionService` and the real, unmodified `TenKPreparationRunwayDarkOrchestrator` (reached through its own existing production factory), then handed unchanged to Phase 4K.8's own runtime for atomic activation. Phase 4K.8's own window-selection/atomicity/direction-guard/target-lock/bounded-slice authorities are never duplicated or redesigned. 18 new tests pass, all exercising the real on-disk catalog and real production authorities end-to-end; zero numeric-formula change; zero public/persistence/API/Flutter wiring. Honestly disclosed scope gap: real per-session calendar dates are computed and available as provenance, but Phase 4K.8's own final calendar-date assignment still uses the simpler, unmodified week-level `LongHorizonCalendarAssigner` — not redesigned by this phase. `TD-LONG-HORIZON-RUNWAY-CORE-JIT-CONTEXT-001`, `TD-LONG-HORIZON-RUNWAY-BOUNDED-PRESCRIPTION-CONTRACTS-001`, and `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` each received an append-only update. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4K_8C_REAL_CORE_TARGET_RUNTIME_CONDITION_AND_SESSION_CALENDAR_COMPOSITION_ADAPTER.md` for the full decision record.

### Real session-level calendar projection aligned with activated numeric weeks (Phase 4K.8D)

`TD-LONG-HORIZON-ACTIVATED-SESSION-CALENDAR-PROJECTION-001` was added `CLOSED`. The final 4K.8C gap is closed inside the same dark atomic composition boundary: selected Runway/Core session references now expose exact `SessionDate` values from the existing real `CalendarComposition`, while `CalendarDates` remains a separate structural week boundary. The adapter selects/maps only and never invokes a calendar composer, weekday algorithm, long-run placement algorithm, spacing formula or `WeekStartDate`. A stable structural slot ordinal resolves duplicate-role sessions without random identity. The immutable full Runway calendar projection is reused across continuation slices; only selected current Core weeks receive executable dates. Identity, preferred/long-run day, uniqueness, week bounds and mixed chronology validate before success. Existing Phase 4K.8/4K.8C window, numeric, target-lock, direction and lifecycle authorities remain unchanged. Append-only updates were added to the 4K.8C composition, 4K.8 runtime and volume-redesign records; the redesign remains `OPEN` for Phase 4K.9 lifecycle/retry, persistence, public preview/API and Flutter.

Current aggregate: 44 risks, 14 `OPEN`, 30 `CLOSED`.

### Full 21-52 rolling dark lifecycle validation and explicit retry (Phase 4K.9)

`TD-LONG-HORIZON-FULL-DARK-LIFECYCLE-VALIDATION-001` was added `CLOSED`. The diagnostic harness composes, rather than replaces, the existing production runtimes. All 32 supported horizons complete through initial GE, repeated Growth/Maintenance checkpoints, real Runway/Core composition, immutable bounded Runway exposure, repeated Core windows, authoritative session dates and final accumulated validation. Safety and non-terminal-session blocks prove explicit later changed-evidence retry through `Blocked -> Pending -> normal activation`; unchanged/same-date retry and direct `Blocked -> Activated` remain illegal. The canonical eight-week Runway produces every naturally reachable greedy mixed-boundary shape; standalone 3-7-week mechanics remain at the narrower 4K.8B contract seam because the 21-52 roadmap fixes Runway at eight weeks. The volume-envelope redesign stays `OPEN`: its append-only definition still names persistence, public preview/API activation and Flutter, none of which Phase 4K.9 implements.

Current aggregate: 45 risks, 14 `OPEN`, 31 `CLOSED`.

### Long-Horizon public preview contract approved, dark (Phase 4L.1)

`TD-LONG-HORIZON-PUBLIC-PREVIEW-CONTRACT-READINESS-001` was added `CLOSED`. `LongHorizonPublicPreviewMapper` produces a public-safe contract from real Phase 4K.9 dark lifecycle snapshots: a complete structural roadmap, the currently activated numeric window, and explicit non-leaking `Pending`/`Blocked` representation for every future week. Direct repository inspection resolved every readiness question the phase required: confirmation remains `NotReadyForConfirmation` (the existing flow requires a complete static schedule), persistence requires new schema (no week/day `Pending`/`Blocked` status exists today), restart/resume has nothing to reconstruct from, and the recommended API strategy is a dedicated Long-Horizon response discriminator (the existing `GeneratePreviewResponse` DTOs are fully non-nullable). Two genuine mapper bugs were found and fixed via this phase's own real-snapshot testing before landing at 37/37 tests passing. `TD-LONG-HORIZON-FULL-DARK-LIFECYCLE-VALIDATION-001` and `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` each received an append-only update; the redesign remains `OPEN` (public live activation, confirmation, persistence, restart/resume and Flutter integration all remain unimplemented). `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4L_1_LONG_HORIZON_PUBLIC_PREVIEW_CONTRACT_AND_DARK_TO_PUBLIC_READINESS_REVIEW.md` for the full decision record.

Current aggregate: 46 risks, 14 `OPEN`, 32 `CLOSED`.

### Long-Horizon rolling persistence and restart-safe state contract implemented, dark (Phase 4L.2)

`TD-LONG-HORIZON-ROLLING-PERSISTENCE-RESTART-SAFETY-001` was added `CLOSED`. Eight new durable tables close the persistence and restart-resume blockers Phase 4L.1 identified: structural week/session ownership, activation-window/checkpoint/block-retry audit records, and immutable bounded Runway/Core snapshots, all deliberately isolated from the real `TrainingPlans`/`TrainingDays` tables and every live read path. Every one of 30 new tests runs against the real configured PostgreSQL database, proving restart continuation across independent connections, optimistic concurrency, idempotent replay, and fail-closed corruption detection for real — not approximated with in-memory fixtures. Direct `Blocked→Activated` has no code path; retry requires a strictly later checkpoint date and changed evidence. Migration is additive-only (zero changes to existing tables). `TD-LONG-HORIZON-PUBLIC-PREVIEW-CONTRACT-READINESS-001` and `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` each received an append-only update; the redesign remains `OPEN` (public preview wiring, confirmation wiring, home/calendar integration, API endpoint, and Flutter integration all remain unimplemented). `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4L_2_LONG_HORIZON_ROLLING_PERSISTENCE_AND_RESTART_SAFE_STATE_CONTRACT.md` for the full decision record.

Current aggregate: 47 risks, 14 `OPEN`, 33 `CLOSED`.

### Runway/Core PostgreSQL restart, recovery and transaction boundary matrix — narrow, honest scope (Phase 4L.2A)

`TD-LONG-HORIZON-RUNWAY-CORE-POSTGRESQL-RESTART-RECOVERY-MATRIX-001` was added `CLOSED`. Closes Phase 4L.2's own "smoke-tested only" disclosure for the pure first-Runway-entry, continuation, and blocked/retry paths, proving each against the real configured PostgreSQL database (never in-memory), with a brand-new `AppDbContext`/connection per restart. Two real defects were found and corrected: the Runway target lock/prescription/calendar projection were not persisted with full fidelity at all (fixed via two new bounded jsonb columns and full-object JSON round-trip); and `LongHorizonCoreContextRecord`'s primary key collided across different plans (fixed via a per-plan-scoped deterministic identity). 16 new tests pass, all against real Postgres, covering first-entry reconstruction, continuation reuse, blocked/retry survival, no-regeneration proof, a corruption subset, and one concurrency race. This TD explicitly does not close the pure GE+Runway mixed-window matrix, Core-only/refresh restart, the full failure-injection matrix, or the full concurrency matrix — all honestly disclosed as open. `TD-LONG-HORIZON-ROLLING-PERSISTENCE-RESTART-SAFETY-001`, `TD-LONG-HORIZON-FULL-DARK-LIFECYCLE-VALIDATION-001`, `TD-LONG-HORIZON-PUBLIC-PREVIEW-CONTRACT-READINESS-001`, and `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` each received an append-only update; the redesign remains `OPEN`. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4L_2A_RUNWAY_CORE_POSTGRESQL_RESTART_RECOVERY_AND_TRANSACTION_BOUNDARY_MATRIX.md` for the full decision record.

Current aggregate: 48 risks, 14 `OPEN`, 34 `CLOSED`.

### Mixed-window/Core-refresh/failure-injection/concurrency completion matrix — remains OPEN; real Runway continuation defect discovered (Phase 4L.2B)

`TD-LONG-HORIZON-MIXED-CORE-REFRESH-POSTGRESQL-COMPLETION-MATRIX-001` was added `OPEN`, kept open exactly per the phase prompt's own governance instruction since none of the four required capability groups (mixed-window restart; Core-only/future-refresh restart; failure-injection/rollback matrix; full concurrency/idempotency matrix) close this phase. The phase's real contribution: real Postgres testing discovered that Phase 4L.2A's "Runway continuation restart" claim proved prescription/target-lock identity reuse only, never window advancement — and when directly tested, the activation window does not appear to advance on a second continuation call, a defect only exposed now that 4L.2A's own fix made `ExistingRunwayPrescription` real (previously always null, which masked this by forcing full regeneration every call). Real GE→Runway mixed-window restart at horizons 25/26/27 was also attempted and did not activate as expected; the investigative test file was removed rather than left failing or misleadingly passing. No production code was changed this phase. `TD-LONG-HORIZON-RUNWAY-CORE-POSTGRESQL-RESTART-RECOVERY-MATRIX-001`, `TD-LONG-HORIZON-ROLLING-PERSISTENCE-RESTART-SAFETY-001`, `TD-LONG-HORIZON-PUBLIC-PREVIEW-CONTRACT-READINESS-001`, `TD-LONG-HORIZON-FULL-DARK-LIFECYCLE-VALIDATION-001`, and `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` each received an append-only update; the redesign remains `OPEN`. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4L_2B_MIXED_WINDOW_CORE_REFRESH_FAILURE_INJECTION_AND_CONCURRENCY_COMPLETION_MATRIX.md` for the full decision record.

Current aggregate: 49 risks, 15 `OPEN`, 34 `CLOSED`.

### Runway continuation window advancement root-cause resolution (Phase 4L.2C)

`TD-LONG-HORIZON-RUNWAY-CONTINUATION-WINDOW-ADVANCEMENT-001` was added `CLOSED`. Root-caused the exact defect Phase 4L.2B discovered but did not fix: `LongHorizonLockedCoreWeekOneTarget.LockedForActivatedRunwayWeekRange` is a `(int, int)` ValueTuple, and `System.Text.Json`'s default options do not serialize/deserialize public fields (only properties) — ValueTuple exposes `Item1`/`Item2` as fields, so the tuple silently round-tripped as `(0,0)` after restart, tripping the reuse-scope validator on every second continuation call, masked behind the generic `JitSegmentTransitionInfeasible` reason. Fixed via a shared `JsonSerializerOptions { IncludeFields = true }` applied symmetrically to every serialize/deserialize call site — no numeric, calendar, direction, evidence, target-lock, or slice formula touched, no regeneration workaround. A new fail-closed `LongHorizonRunwayContinuationAdvancementValidator` now guards the invariant in the real continuation path. Proven against real PostgreSQL: three-call progression through GE→Runway→Core with a restart between every call; the Phase 4L.2A continuation test strengthened to assert genuine advancement instead of identity reuse alone; replay idempotency and fail-closed corruption behavior both re-confirmed. The Phase 4L.2B GE→Runway mixed-window finding (25/26/27-week horizons) was re-diagnosed and found to be an *independent*, still-open defect (a different exception, thrown before Runway composition is reached) — not fixed by this update, honestly disclosed rather than folded in to inflate scope. Full existing 751-test LongHorizon suite re-confirmed passing. `TD-LONG-HORIZON-MIXED-CORE-REFRESH-POSTGRESQL-COMPLETION-MATRIX-001` remains `OPEN` (mixed-window, Core-only/refresh, failure-injection, and concurrency matrices are all still unresolved). `TD-LONG-HORIZON-RUNWAY-CORE-POSTGRESQL-RESTART-RECOVERY-MATRIX-001`, `TD-LONG-HORIZON-ROLLING-PERSISTENCE-RESTART-SAFETY-001`, and `TD-LONG-HORIZON-PUBLIC-PREVIEW-CONTRACT-READINESS-001` each received an append-only update. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4L_2C_RUNWAY_CONTINUATION_WINDOW_ADVANCEMENT_ROOT_CAUSE_RESOLUTION.md` for the full decision record.

Current aggregate: 50 risks, 15 `OPEN`, 35 `CLOSED`.

### GE-to-Runway partial-boundary checkpoint handoff root-cause resolution — reported production defect was a test-harness bug (Phase 4L.2D)

`TD-LONG-HORIZON-GE-RUNWAY-PARTIAL-BOUNDARY-HANDOFF-001` was added `CLOSED`. Investigated the Phase 4L.2B/4L.2C finding that 25/26/27-week horizons fail a second continuation call with `LongHorizonCheckpointDecisionInvalidException: Next GE week 5 must be NumericPending`. Direct evidence (isolated checkpoint-runtime calls, direct composition-orchestrator calls, stage tracing) proved the GE checkpoint runtime, the boundary-handoff routing, and the JIT composition orchestrator were never defective: a partial terminal GE window is and always was a valid checkpoint window, and the handoff to Runway composition already worked correctly. The real root cause: `LongHorizonRunwayCoreRestartFixture.AdvanceOneWindowAsync` (shared PostgreSQL test infrastructure) hardcoded `RaceDate = StartDate + 21*7` regardless of the plan's actual horizon, making the race appear too soon for non-21-week horizons and causing the real Core-generation pipeline to silently block the first mixed-window composition — a downstream symptom later misread as a GE checkpoint defect. Fixed with a one-line test-infrastructure change (`RaceDate` now derived from `StructuralRoadmap.TotalWeeks`); zero production code modified. Proven against real PostgreSQL for horizons 25/26/27, restart between every call: natural GE+Runway mixed shape persists and reconstructs, Runway continuation advances correctly, no regeneration. No new production contract/validator was built, since none was needed — an explicit, evidence-based rejection of the phase prompt's assumed defect narrative rather than a fabricated fix. `TD-LONG-HORIZON-MIXED-CORE-REFRESH-POSTGRESQL-COMPLETION-MATRIX-001` remains `OPEN` (Runway→Core mixed restart, Core-only/refresh, failure-injection, and concurrency matrices are all still unresolved). `TD-LONG-HORIZON-RUNWAY-CONTINUATION-WINDOW-ADVANCEMENT-001`, `TD-LONG-HORIZON-ROLLING-PERSISTENCE-RESTART-SAFETY-001`, and `TD-LONG-HORIZON-FULL-DARK-LIFECYCLE-VALIDATION-001` each received an append-only update. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4L_2D_GE_RUNWAY_PARTIAL_BOUNDARY_CHECKPOINT_HANDOFF_ROOT_CAUSE_RESOLUTION.md` for the full decision record.

Current aggregate: 51 risks, 15 `OPEN`, 36 `CLOSED`.

### Future-only Core context refresh capability implemented (Phase 4L.2E)

`TD-LONG-HORIZON-FUTURE-ONLY-CORE-CONTEXT-REFRESH-001` was added `CLOSED`. Implemented the missing production capability the parent Phase 4L.2B TD identified as unresolved: an explicit eligibility gate (`LongHorizonCoreRefreshEligibility`), typed request/result contracts, and an orchestrator (`LongHorizonFutureCoreRefreshOrchestrator`) that reuses the existing real condition-resolution/Core-generation/persistence chain to produce a new versioned Core context (V2) applying only to future `NumericPending` Core weeks, while historical activated weeks remain owned by the prior context (V1). Fixed a real, previously-unpopulated `SupersededByContextId` linkage gap in the existing repository code (narrow correction, not a redesign). One additive nullable-column migration (`EvidenceFingerprint`) supports the "unchanged evidence" eligibility rule. Proven against real PostgreSQL: V1 immutability (historical volume/calendar values byte-identical across refresh and restart), V2 future-only ownership (correct boundary derivation), both contexts reconstructing from a fresh connection, normal continuation using V2 through to terminal completion, stale-version replay rejection without duplication, and one focused concurrency race with exactly one winner. Two scope narrowings disclosed: refresh uses the existing combined refresh-plus-activation authority (per the phase's own fallback) rather than a separate context-only persistence path, and idempotency is proven via version-staleness rejection. `TD-LONG-HORIZON-MIXED-CORE-REFRESH-POSTGRESQL-COMPLETION-MATRIX-001` remains `OPEN` — this closes exactly one of its two remaining blockers (future-only Core refresh); the failure-injection matrix and the remaining concurrency/idempotency scenarios are still unresolved. `TD-LONG-HORIZON-RUNWAY-CONTINUATION-WINDOW-ADVANCEMENT-001`, `TD-LONG-HORIZON-GE-RUNWAY-PARTIAL-BOUNDARY-HANDOFF-001`, `TD-LONG-HORIZON-ROLLING-PERSISTENCE-RESTART-SAFETY-001`, `TD-LONG-HORIZON-PUBLIC-PREVIEW-CONTRACT-READINESS-001`, and `TD-LONG-HORIZON-FULL-DARK-LIFECYCLE-VALIDATION-001` each received an append-only update. `TD-GENERAL-ENDURANCE-STAGED-PLAN-001` remains `OPEN`. See `PHASE4L_2E_FUTURE_ONLY_CORE_CONTEXT_REFRESH_CAPABILITY.md` for the full decision record.

Current aggregate: 52 risks, 15 `OPEN`, 37 `CLOSED`.

### Transactional failure-injection and rollback matrix — representative coverage proven, full matrix remains open (Phase 4L.2F)

`TD-LONG-HORIZON-TRANSACTIONAL-FAILURE-INJECTION-ROLLBACK-MATRIX-001` was added `OPEN`. Confirmed every authoritative Long-Horizon persistence method commits via exactly one EF `SaveChangesAsync` call (no split-transaction defect exists). Built a production-inert, test-controlled failure-injection seam wired via an optional repository constructor parameter (defaults to `NoOp`, zero behavioral change to existing call sites). Proved against real PostgreSQL, for all five required operation groups (mixed Runway→Core, Core-only, future-only Core refresh, block, retry): an injected pre-commit failure leaves the exact prior durable state with no orphan/split ownership, and a subsequent replay succeeds exactly once; a post-commit-acknowledgement failure is correctly distinguished from rollback (committed state survives, replay deduplicates via the real `IdempotencyKey`). Remains OPEN per the phase's own explicit closure criteria: real PostgreSQL constraint-exception rollback was not tested, and the full suggested stage matrix was covered only representatively. `TD-LONG-HORIZON-MIXED-CORE-REFRESH-POSTGRESQL-COMPLETION-MATRIX-001` (the parent Phase 4L.2B TD) remains `OPEN` — the failureInjectionCoverage blocker is now partially, not fully, addressed. `TD-LONG-HORIZON-FUTURE-ONLY-CORE-CONTEXT-REFRESH-001`, `TD-LONG-HORIZON-ROLLING-PERSISTENCE-RESTART-SAFETY-001`, `TD-LONG-HORIZON-RUNWAY-CORE-POSTGRESQL-RESTART-RECOVERY-MATRIX-001`, `TD-LONG-HORIZON-FULL-DARK-LIFECYCLE-VALIDATION-001`, and `TD-LONG-HORIZON-PUBLIC-PREVIEW-CONTRACT-READINESS-001` each received an append-only update. See `PHASE4L_2F_TRANSACTIONAL_FAILURE_INJECTION_AND_ROLLBACK_MATRIX.md` for the full decision record.

Current aggregate: 53 risks, 16 `OPEN`, 37 `CLOSED`.

### Real PostgreSQL constraint-exception rollback completion (Phase 4L.2F-A)

`TD-LONG-HORIZON-POSTGRESQL-CONSTRAINT-EXCEPTION-ROLLBACK-001` was added `CLOSED`, and `TD-LONG-HORIZON-TRANSACTIONAL-FAILURE-INJECTION-ROLLBACK-MATRIX-001` moved from `OPEN` to `CLOSED` at complete mutation-category coverage. The real configured Npgsql/PostgreSQL provider now proves that 23505 failures for duplicate activation idempotency ownership, executable-session `(WeekStateId, SessionOrdinal)` ownership and plan-scoped Core-context version ownership; 23503 failures during both block and retry persistence; and an honestly reachable 23502 required `IdempotencyKey` failure roll back the complete existing single-`SaveChangesAsync` unit of work. Every case discards the failed context, compares a canonical raw snapshot including `xmin`, reconstructs through production services in a fresh context, runs the integrity validator, and performs a corrected replay exactly once. V1 remains Active and unsuperseded after the failed Core refresh; no V2, partial window, session, block or retry survives. The activation idempotency unique index is the database-level idempotency authority; no separate idempotency table exists. Long-Horizon has no database check constraint, so no 23514 test or convenience migration was fabricated. The new mutation seam is internal, optional, defaults to NoOp, and has no DI/API/configuration exposure. Phase 4L.2B remains `OPEN` for Phase 4L.2G's remaining concurrency, idempotency and commit-ambiguity matrix. Public preview, confirmation, API, Home/Calendar, completion handlers and Flutter remain unwired. See `PHASE4L_2F_A_POSTGRESQL_CONSTRAINT_EXCEPTION_AND_ROLLBACK_COMPLETION.md` for the full decision record.

Current aggregate: 54 risks, 15 `OPEN`, 39 `CLOSED`.

### Remaining concurrency, idempotency and commit-ambiguity completion (Phase 4L.2G)


Append-only updates were recorded for `TD-LONG-HORIZON-MIXED-CORE-REFRESH-POSTGRESQL-COMPLETION-MATRIX-001`, `TD-LONG-HORIZON-TRANSACTIONAL-FAILURE-INJECTION-ROLLBACK-MATRIX-001`, `TD-LONG-HORIZON-POSTGRESQL-CONSTRAINT-EXCEPTION-ROLLBACK-001`, `TD-LONG-HORIZON-FUTURE-ONLY-CORE-CONTEXT-REFRESH-001`, `TD-LONG-HORIZON-ROLLING-PERSISTENCE-RESTART-SAFETY-001`, `TD-LONG-HORIZON-RUNWAY-CORE-POSTGRESQL-RESTART-RECOVERY-MATRIX-001`, `TD-LONG-HORIZON-FULL-DARK-LIFECYCLE-VALIDATION-001`, and `TD-LONG-HORIZON-PUBLIC-PREVIEW-CONTRACT-READINESS-001`. The first parent moves to `CLOSED`; confirmation and public-wiring readiness remain separate and unchanged.

Current aggregate: 55 risks, 14 `OPEN`, 41 `CLOSED`.

### Authenticated Long-Horizon public preview and rolling confirmation wiring (Phase 4L.3)

`TD-LONG-HORIZON-CONFIRMATION-AND-PUBLIC-PREVIEW-WIRING-001` was added `CLOSED`.

- Classification: `LONG_HORIZON_CONFIRMATION_AND_PUBLIC_PREVIEW_WIRING_COMPLETED`.
- Severity: `NON_BLOCKING_COMPLETED_PUBLIC_PILOT_CAPABILITY`.
- Routes: authenticated `POST /api/v1/plans/generate-preview/race/long-horizon` and `POST /api/v1/plans/confirm/long-horizon`.
- Eligibility: Race/TenK/Intermediate/4-day, candidate `TEN_K__4D__INTERMEDIATE v10`, 21–52 complete weeks. The dedicated route returns `PLAN_HORIZON_EXCEEDS_SUPPORTED_WINDOW` for 53+; the existing 8–14, 15–20 and Habit routes are unchanged.
- Preview authority: the public-safe Phase 4L.1 contract is mapped from the real initial rolling lifecycle. `PlanPreview` owns the internal initialization snapshot, candidate identity, 30-minute expiry, independent contract versions and SHA-256 input/roadmap/executable-window fingerprints. Confirmation accepts only `PreviewId` and contract version; it never accepts generated sessions, structural state or user identity.
- Ownership and transaction: a real user-owned `TrainingPlan` persists `PlanScheduleStrategy.RollingLongHorizon` and a unique relationship to `LongHorizonRollingPlanState`. One explicit PostgreSQL transaction rechecks preview/active-plan authority, calls the existing `InitializeStructuralStateAsync`, creates plan ownership and marks the preview confirmed. All structural rolling weeks persist; only the current window has executable session rows. No fake `TrainingWeek`/`TrainingDay` rows represent future Pending weeks.
- Replay/concurrency/rollback: exact replay returns the existing PlanId; same-preview concurrent HTTP scopes leave one plan/aggregate/roadmap; two previews for one user obey the existing one-active-plan rule. Four pre-commit failpoints prove complete rollback and preview reuse. Post-commit acknowledgement loss is recovered as `AlreadyConfirmed` without duplication.
- Authorization/errors: preview lookup is authenticated-user scoped; cross-user lookup is indistinguishable from missing. Typed mappings cover missing, expired, stale/corrupt, active conflict, unsupported pilot, infeasible initialization, 53+, and deferred rolling read surfaces. Provider detail is not public.
- Read-surface safety: interim policy A is explicit. Existing Home and Calendar return `LONG_HORIZON_READ_SURFACE_NOT_YET_SUPPORTED` for rolling plans rather than interpreting them as empty static schedules. Flutter, background activation and broad rolling Home/Calendar projection remain unwired.
- Migration: additive `20260804142858_Phase4L3LongHorizonPublicConfirmation` was applied to local PostgreSQL. Existing TrainingPlan rows default to `StaticComplete`.
- Tests: 23 focused Phase 4L.3 integration cases cover boundaries, server authority, real confirmation, no fake static rows, replay, concurrency, rollback, acknowledgement loss, authorization, Home/Calendar safety, typed errors and Swagger. Full-suite counts are recorded in `PHASE4L_3_LONG_HORIZON_CONFIRMATION_AND_PUBLIC_PREVIEW_WIRING.md`.

Append-only Phase 4L.3 updates were added to the public-preview, rolling-persistence, full-dark-lifecycle, mixed-completion, rollback, remaining-concurrency and volume-redesign records. `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` remains `OPEN` because full Home/Calendar rolling integration and Flutter remain outside this phase; no numeric or calendar formula changed.

Current aggregate: 56 risks, 14 `OPEN`, 42 `CLOSED`.

### Long-Horizon active reads, outcomes and Flutter contract readiness (Phase 4L.4)

`TD-LONG-HORIZON-ACTIVE-READ-MODEL-WORKOUT-MUTATION-001` was added `CLOSED`.

- Architecture: existing Home and Calendar application services dispatch to a dedicated rolling provider only when persisted strategy is `RollingLongHorizon`. Existing static Home objects and Calendar lists retain their prior wire shapes. Rolling Home/Calendar use explicit V1 contracts.
- Identity and authorization: `LongHorizonRollingSessionState.Id` is the stable public session ID. Every query/mutation traverses the authenticated user's active `TrainingPlan` and rolling-state link; cross-user and cancelled-plan access is non-disclosing not-found.
- Reads: Home returns today when an activated session is assigned, otherwise no today workout plus the next activated Planned session. Calendar returns only activated/completed sessions in the requested month. Detail returns planned and actual outcome data for one executable session. Active details expose all structural weeks, but Pending weeks remain structural-only with `NumericDetailsAvailable=false`.
- Outcomes: direct durable `Completed` and `NotToday` outcomes are mutually exclusive. Exact replay is idempotent, materially different replay conflicts, optimistic `OutcomeVersion` resolves concurrent writers, and the TrainingPlan ownership-row lock serializes cancellation. Planned distance and AssignedDate never change.
- Evidence and activation: when a week becomes terminal, durable rolling outcomes are adapted in memory to the existing checkpoint-evidence row input. Existing aggregation, validated-load and checkpoint formulas are unchanged. No read or mutation persists a checkpoint, activates a next window, reschedules a session or creates a fake static row. Readiness is public and side-effect free.
- Migration: additive `20260805081427_Phase4L4RollingSessionOutcomes` adds actual/not-today/version fields plus an AssignedDate index, preserves existing `CompletionStatus` storage with safe `Planned` default, and was applied to local PostgreSQL.
- Contracts: rolling Home, Calendar, detail, completion, not-today and active-details fields, enum/nullability/date/unit/error/idempotency semantics and public examples form the backend readiness record for Phase 4L.5. Flutter remains unchanged.
- Append-only updates were added to the Phase 4L.3 confirmation, public-preview readiness, rolling-persistence, mixed-completion, full-dark-lifecycle and volume-redesign records. The volume-redesign TD remains `OPEN`; Flutter and explicit next-window activation remain separate future work.

### Explicit next-window activation and public continuation API (Phase 4L.4A)

`TD-LONG-HORIZON-EXPLICIT-NEXT-WINDOW-ACTIVATION-API-001` was added `PARTIALLY_CLOSED`.

- Route and runtime reuse: `POST /api/v1/plans/active/long-horizon/activate-next-window` reuses the existing, unmodified Phase 4L.2 reconstruct-checkpoint-compose-persist chain (`LongHorizonRollingCheckpointRuntime`, `LongHorizonRollingRestartContinuationService`, `LongHorizonRollingStateRepository`) via a new Application-layer orchestrator. No second continuation engine was created; both pre-existing Phase 4L.2A guard tests forbidding controller/DI references to the internal chain by name still pass unmodified.
- Eligibility and terminality: a fresh, `FOR UPDATE`-locked read of the aggregate never trusts a client-cached Home readiness value. `TerminalPlanComplete`, `CurrentWindowInProgress`, `ReassessmentRequired`/`RetryRequired`, and `NextWindowActivationReady` are each mapped to a deterministic, typed HTTP result.
- Proven end-to-end against real PostgreSQL: one real pure-GE activation; atomic persistence; one-winner concurrent activation; two real pre-commit rollback failpoints with a working corrected retry; Home reflecting the newly activated window immediately; a reflection-based public leakage guard; Swagger route/enum presence.
- Documented scoping decision: the endpoint's minimal request contract (`{"contract_version":1}`, no client idempotency token) means a POST retry immediately after a successful activation legitimately reports `CurrentWindowInProgress` rather than a magic idempotent replay of the activation itself — recovery from a lost acknowledgement is via re-reading Home/Calendar, which already reflects the committed window.
- Known gaps carried forward: no public retry endpoint; Runway/mixed/Core-only/final-partial/terminal lifecycle shapes not independently driven end-to-end by this phase's own tests; dedicated activation-vs-completion/not-today/cancellation race tests not added (the shared row-lock mechanism is proven by Phase 4L.4's own equivalent tests); only 2 of 7 failure-injection stages exercised.

Current aggregate: 58 risks, 15 `OPEN`, 43 `CLOSED`. (This record is `PARTIALLY_CLOSED`, counted with `OPEN` here since it is not fully closed.)

### Public retry, remaining activation shapes and cross-operation race completion (Phase 4L.4B)

`TD-LONG-HORIZON-PUBLIC-RETRY-ACTIVATION-SHAPE-RACE-COMPLETION-001` was added `OPEN`.

- Retry authority reuse: `POST /api/v1/plans/active/long-horizon/retry` reuses the existing, unmodified Phase 4L.2 `SaveRetryRestorationAsync` (via `LongHorizonRollingRetryPersistenceAdapter`) — no parallel retry persistence model. The server derives all ownership (owning plan, current Blocked boundary, last block record) from durable state; the client supplies only `contract_version`.
- Naturally reachable Blocked boundary: every `LONG_RUN` session in the current window is left `NotToday` through the real public completion/not-today endpoints, triggering the real checkpoint runtime's own `ValidatedLongRunEvidenceUnavailable` classification — not a fabricated fixture row. A 45-week horizon plan was used so the triggering checkpoint stays inside pure-GE range rather than crossing into the Runway/Core JIT-composition boundary, where a missing-evidence outcome is classified differently.
- Proven end-to-end against real PostgreSQL: Blocked→Pending restoration; original block record remains historical; retry creates no sessions and no activation; activation-while-Blocked returns `RetryRequired` and creates no sessions; concurrent retry has exactly one winner with no partial mutation; two real pre-commit rollback failpoints with a working corrected retry; Home readiness reflects the restoration.
- Architectural finding, documented rather than worked around: `SaveRetryRestorationAsync` requires a strictly-later checkpoint date and a changed evidence fingerprint versus the block. For a block whose evidence comes from an already-fully-terminal window (this endpoint's only reachable trigger), no public operation can subsequently change that window's terminal session outcomes — so a literal "retry, then the same evidence gap resolves itself" path is not reachable through the current public surface without a not-yet-existing evidence-correction endpoint. A fresh re-evaluation after retry correctly re-blocks with the same reason, proving retry restores real eligibility without bypassing checkpoint authority — it does not and structurally cannot fabricate a more favorable outcome.
- Known gaps: Runway/mixed/Core-only/final-partial/terminal activation shapes not independently driven end-to-end through the public activation endpoint in this phase; dedicated activation-vs-completion, activation-vs-not-today, and retry-vs-activation race tests not added (the shared row-lock mechanism is proven by Phase 4L.4's and Phase 4L.4A's own equivalent tests); a genuine "retry then successful activation" end-to-end path was not demonstrated for the architectural reason above; only one Swagger example added.

### Blocked recovery, new-evidence reassessment and meaningful retry policy (Phase 4L.4C)

`TD-LONG-HORIZON-BLOCKED-RECOVERY-NEW-EVIDENCE-REASSESSMENT-001` was added `CLOSED` under Outcome B (no new recovery authority invented; public retry no longer implies recoverability).

- Empirical correction: Phase 4L.4B's own retry test scenario was never verified against the actually-persisted internal reason code. Direct querying in this phase found the real reason is `CheckpointWindowNotComplete` (a real-calendar-time gate that fires before any evidence-completeness check, regardless of session outcomes — confirmed even with every session, including the long run, fully `Completed`), not `ValidatedLongRunEvidenceUnavailable` as previously assumed from source-reading alone.
- Investigation (Part 3/6): no evidence-update, activity-import, profile-refresh or recovery-window capability exists anywhere in the repository. Candidates A/B/C were each explicitly rejected with the missing authority named, not silently skipped.
- Fix: `LongHorizonRollingRetryContinuationService` now classifies the last `Block` record's real reason code (`LongHorizonBlockRecoveryClassification`, all 19 existing Phase 4K.3/4K.4 reason codes mapped deterministically) before attempting restoration. Only the genuinely time-recoverable `CheckpointWindowNotComplete` class proceeds to the existing `SaveRetryRestorationAsync`; every other class returns a typed, zero-mutation `LONG_HORIZON_REGENERATE_PREVIEW_REQUIRED` or `LONG_HORIZON_OPERATIONAL_SUPPORT_REQUIRED` result instead of the previous misleading always-eventually-succeeds behavior.
- Approved recovery path for an evidence-immutable block: cancel the blocked plan, then generate and confirm a new preview — 100% existing, unmodified capability, proven end-to-end against real PostgreSQL in this phase. No new atomic-replace capability was built.
- Home now exposes `recovery_requirement` and `blocked_public_reason_category` (both computed from existing, unmodified persisted fields) so a blocked plan's public state is honest and consistent, with the internal reason code never leaked.
- Two residual gaps surfaced, not fixed here: (a) the Runway/Core JIT boundary-handoff path's null-`ValidatedLoad` case currently maps to a generic corrupt-state error rather than a classifiable Block record; (b) evidence-content-based blocks (`ValidatedLongRunEvidenceUnavailable` and siblings) remain independently unreachable through the public activation eligibility gate given realistic future-dated plan horizons — classification correctness for them is proven only by direct seeding, matching Phase 4L.2's own established test technique.

### Remaining public activation lifecycle shapes, JIT boundary classification and cross-operation race completion (Phase 4L.4D)

`TD-LONG-HORIZON-PUBLIC-ACTIVATION-SHAPE-JIT-RACE-COMPLETION-001` was added `OPEN`.

- JIT-boundary fix: the Runway/Core JIT-composition boundary's null-evidence case previously threw an unclassified `LongHorizonReadStateCorruptException` with no durable `Block` record. It now persists a real, classifiable `Block` via the existing `PersistBlockAsync` authority with `JitValidatedLoadUnavailable` (existing Phase 4K.4 reason code), which the existing, unmodified `LongHorizonBlockRecoveryClassification` already maps to `RequiresRegeneratePreview`. Validated against a real, naturally-occurring block, not a seeded one.
- Lifecycle-shape finding: only 1 of 10 requested public-activation shapes (GE→Runway crossing) is naturally reachable through the public endpoint today. The other 9 are structurally blocked by two real, separate root causes: (1) `CheckpointWindowNotComplete`'s real-calendar-date gate (Phase 4L.4C's own finding); and (2) a newly-discovered session-role-naming mismatch — Runway/Core session roles are assigned as `SlotRole.ToString()` (e.g. `"LongRun"`) while the evidence adapter's long-run detection matches only `"LONG_RUN"` (GE's convention) — so every Runway/Core continuation attempt computes zero completed long runs and always blocks, regardless of real completion. Neither cause was fabricated or worked around; both are explicit follow-up.
- Six cross-operation races proven coherent against real PostgreSQL: activation-vs-completion, activation-vs-not-today, activation-vs-cancellation (against the one reachable shape), and retry-vs-activation, retry-vs-cancellation (against the now-real JIT-boundary block).
- Unrelated pre-existing finding: 42 of 43 plan-catalog test failures observed during this phase's regression run were proven pre-existing (present even when the ledger was truncated back to its 57-entry pre-session state) — roughly twenty historical Long-Horizon governance test files hardcode a risks-count/aggregate-sentence snapshot at authoring time and were never designed to tolerate later ledger growth. Not fixed here (a large, unrelated undertaking); documented so it isn't mistaken for a regression this phase caused. One additional failure was attributable to this session's own governance-ledger growth and is accounted for by this record.

### Runway/Core session-role evidence normalization and historical compatibility (Phase 4L.4E)

`TD-LONG-HORIZON-RUNWAY-CORE-SESSION-ROLE-EVIDENCE-NORMALIZATION-001` was added `CLOSED`.

- Defect fixed: `LongHorizonRollingJitActivationRuntime.MapRunwayWeeks` and `LongHorizonRealCalendarProjectionAdapter.BuildFullRunwayProjection` derived Runway session roles via the raw `PreparationRunwaySlotRole.ToString()` enum default (`"LongRun"`) instead of the canonical uppercase convention (`"LONG_RUN"`) an identical, unmodified switch expression already used in four other files. Core was never affected. Fixed by routing both call sites through one new centralized `LongHorizonSessionRoleCodec` instead of inventing a new representation.
- Canonical decision: the pre-existing uppercase-with-underscore convention (`KEY_SESSION`/`EASY_SUPPORT`/`LONG_RUN`), not a new invention. One known legacy set (the exact PascalCase enum-default strings this defect could have produced). No migration — Option A (read-compatible, write-canonical); no real production data exists to migrate in this pre-launch pilot.
- Proven against real PostgreSQL: a genuinely completed Runway long run in each week of a real activated window now contributes exactly once to checkpoint evidence, distinguished explicitly from the separate, untouched real-calendar `CheckpointWindowNotComplete` gate. A real evidence gap (long run left `NotToday`) still produces the exact same `JitValidatedLoadUnavailable` typed Block, still maps to `RegeneratePreviewRequired`, with no regression to the old unclassified corrupt-state error.
- Incidental finding, reported not overclaimed: this phase's own real testing observed a *second* real Runway/Core continuation cycle activate successfully once the role fix was in place — the JIT composition path apparently doesn't share GE's real-calendar gate. Supports Phase 4L.4F's next attempt at the full lifecycle-shape matrix; not itself claimed as that proof.
- Governance-test-debt verification: ran the full plan-catalog suite before and after this phase's code changes — identical 1206 passed / 43 failed both times. Zero failures attributable to this phase.

### Remaining public lifecycle shape, replay, terminal and concurrency matrix closure (Phase 4L.4F)

`TD-LONG-HORIZON-PUBLIC-LIFECYCLE-SHAPE-REPLAY-TERMINAL-CONCURRENCY-MATRIX-001` was added `CLOSED`.

- Empirical walk: direct real-HTTP testing against real PostgreSQL (performed before writing any test assertions) proved Phase 4L.4E's role fix unblocked the *entire* remaining lifecycle — a real 21-week plan walks GE(window 1) → Runway(2-5) → Runway(6-9) → Core(10-13) → Core(14-17) → Core(18-21, final) → `TerminalPlanComplete` end-to-end through the real public activation endpoint in exactly 6 real activation cycles, with no real-calendar gate beyond the very first GE checkpoint. Confirmed with a second horizon (23 weeks).
- Structural unreachability finding: mixed Runway/Core windows and a partial final window are proven **structurally unreachable**, not merely untested, under the existing approved composition formula (GE consumed entirely by window 1; Runway always exactly 8 weeks = 2 full windows; Core always exactly 12 weeks = 3 full windows) — an architectural fact about the approved formula, not a defect. Not fabricated.
- Proven: exact selected ranges/segments/canonical roles per shape; exact next-operation distinction (6 distinct, ordered, non-overlapping activation windows); terminal idempotent replay with zero mutation; Core-boundary concurrency (one durable winner); 2 real-Postgres rollback failpoints at the Core boundary (first endpoint-level proof for `CoreOnlyActivation`); historical Runway session-identity immutability across a real segment transition; Home/Calendar/detail consistency at a shape reached only after two full Runway cycles.
- Explicit remaining gap, disclosed not hidden: Core continuation with future-only context refresh was not naturally triggered by a straightforward complete-on-time walk and was not artificially forced, per this phase's own instruction. Left open for a future narrow phase.
- Given the above, `TD-LONG-HORIZON-EXPLICIT-NEXT-WINDOW-ACTIVATION-API-001` (4L.4A), `TD-LONG-HORIZON-PUBLIC-RETRY-ACTIVATION-SHAPE-RACE-COMPLETION-001` (4L.4B) and `TD-LONG-HORIZON-PUBLIC-ACTIVATION-SHAPE-JIT-RACE-COMPLETION-001` (4L.4D) are each append-only updated to `CLOSED` — their own remaining itemized lifecycle-shape/race gaps are now proven closed to the architectural limit.
- Governance-test-debt verification: ran the full plan-catalog suite before and after this phase's changes (test-only, no production code modified) — identical 1206 passed / 43 failed both times. Zero new failures.

### Long-Horizon Flutter read, workout outcome, retry, continuation and rolling-state integration (Phase 4L.5)

`TD-LONG-HORIZON-FLUTTER-READ-OUTCOME-RETRY-CONTINUATION-INTEGRATION-001` was added `OPEN`.

- First Flutter integration of the Long-Horizon lifecycle: an additive parallel vertical slice (new DTOs, repository, providers, preview/Home/session-detail screens, a route-level Home dispatcher) with zero backend files modified and zero changes to the existing static/Habit Flutter screens (verified by full existing test-suite regression pass).
- Built: Long-Horizon DTOs verified field-for-field against real backend C# source; a thin repository wrapping every real endpoint (preview/confirm/active-home/active-calendar/rolling-detail/complete/not-today/activate-next-window/retry); client-side >20-week preview routing (routing decision only -- backend remains sole planning authority); a preview screen rendering only backend-returned data (locked summaries for Pending weeks, no fabricated sessions); a Home screen surfacing explicit Activate-next-block/Retry actions only when the backend's own readiness fields allow them, never automatically; a rolling session detail screen with single-step completion/not-today mutation (six backend-approved reason tokens verified against `LongHorizonRollingSessionMutationService.AllowedNotTodayReasons`).
- Regression found and fixed by this phase itself: `onboardingProvider` gained a new `longHorizonRepositoryProvider` dependency, which six existing test files had not overridden, causing a Firebase-not-initialized crash in the test sandbox. Fixed via a shared `NoopLongHorizonRepository` test double. Full `flutter test` after the fix: 235 passed, 0 failed, zero regressions in static/Habit/Preparation-Runway tests.
- Explicitly deferred, disclosed not hidden: dedicated Long-Horizon Calendar UI (decoding support only); Long-Horizon-specific error-code mapping additions; accessibility/analytics passes; the full 109-item test matrix (20 targeted tests were written instead); a shared JSON fixtures module; widget-level tests for the new screens.
- Left `OPEN` per this phase's own instruction not to overclose -- closure requires the deferred items above. Backend Long-Horizon governance records remain `CLOSED` and unchanged; this is a new, additive, append-only entry.

### Long-Horizon Flutter Calendar, error mapping, accessibility and integration test closure (Phase 4L.5A)

`TD-LONG-HORIZON-FLUTTER-CALENDAR-ERROR-MAPPING-ACCESSIBILITY-TEST-CLOSURE-001` was added `OPEN`.

- Critical defect found and fixed: `LongHorizonPublicLifecycleStatus`, `LongHorizonPublicPhase`, `LongHorizonConfirmationReadiness`, and `LongHorizonConfirmationOutcome` are real C# enums under the API-wide `JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower)` policy, so their wire values are lowercase snake_case, not the PascalCase C# member names Phase 4L.5's `fromWire` implementations used. This would have made every real preview/confirmation response silently fail-closed to `unknown`, permanently disabling the confirm CTA -- never caught because Phase 4L.5's own test fixtures used the same incorrect PascalCase values. Fixed and pinned with explicit casing-regression tests.
- Built: rolling Calendar UI with a route-level dispatcher mirroring Home's; a centralized `LongHorizonUiErrorMapper` covering all 24 real backend Long-Horizon error codes (verified exhaustively against `GlobalExceptionHandler.cs` -- several codes the originating prompt suggested do not exist in backend source and were correctly omitted); exact-month (not family-wide) Calendar invalidation for completion/not-today/activation, Home-only invalidation for retry; an accessibility pass that found and fixed a real app-wide gap in the shared `AppPrimaryButton` (a loading button announced nothing to screen readers).
- Type-level Pending-week leakage protection: the Calendar response type literally cannot carry a structural roadmap week (no such field/type exists on the Calendar DTOs), so "Pending leaking into Calendar" is a compile-time impossibility, not a convention.
- Testing: 48 net new tests this phase (24 DTO/decoder incl. 8 casing-regression, 20 error-mapper, 7 Calendar widget, 6 accessibility, 4 provider-isolation). Full `flutter test`: 283 passed, 0 failed (up from 235 at the end of Phase 4L.5). `flutter analyze`: 0 errors. `dart format --set-exit-if-changed` fails project-wide as a pre-existing, unrelated condition (73/105 files differ, including many never touched by this phase; no format-enforcing gate exists in the repo).
- `TD-LONG-HORIZON-FLUTTER-READ-OUTCOME-RETRY-CONTINUATION-INTEGRATION-001` (Phase 4L.5) is closed by this phase: every one of its own original closure conditions is now genuinely satisfied.
- Left `OPEN`: end-to-end flow/integration tests and most navigation tests beyond rolling-Calendar-entry-to-detail remain a real, disclosed, material gap. Recommend a narrow follow-up phase (4L.5B) to close this record outright before Phase 4L.6.

### Long-Horizon Flutter end-to-end flow, navigation and production-wiring test closure (Phase 4L.5B)

`TD-LONG-HORIZON-FLUTTER-END-TO-END-FLOW-NAVIGATION-TEST-CLOSURE-001` was added `OPEN`.

- Two real production bugs found and fixed while writing connected-system flow tests (neither was caught by Phase 4L.5A's isolated unit/decoder tests): (1) `RollingSessionDetailPage._complete()`/`_notToday()` had no duplicate-tap guard at all, unlike the preview page's `_onConfirm` -- a rapid double tap fired two real mutation requests; (2) the not-today reason bottom sheet overflowed its `RenderFlex` on a height-constrained viewport because it used a bare `Column` instead of a scrollable one. Both fixed.
- Built: 21 new flow/navigation tests using a real widget tree, a real (minimal, Firebase-free) `GoRouter` matching the pre-existing `active_plan_test_harness.dart` precedent, and real production screens wired together -- preview-ready/not-ready/duplicate-tap/PascalCase-regression; Home-to-Calendar/detail, Calendar-to-detail, invalid-deep-link-safety, terminal-state-Calendar-access; completion/not-today exact-request and duplicate-tap prevention; activation visibility/exactly-once; retry visibility/exactly-once and, critically, proof that retry never also calls activation in the same action.
- Testing: full `flutter test`: 304 passed, 0 failed (up from 283). `flutter analyze`: 0 errors. `dart format` applied to the 5 files touched this phase; project-wide format debt unchanged (73/105 files, pre-existing, zero new unformatted files). Backend suites not re-run -- zero backend files touched.
- Left `OPEN`: regenerate/cancellation flow cannot be tested because no such screen exists yet (a structural gap, not a missing test); authentication-redirect proof is blocked on missing Firebase test-mocking infrastructure (the real router's redirect callback touches FirebaseAuth.instance directly); most timeout/concurrency ambiguity paths and 8 of 9 error-mapper wiring flows remain unproven at the live-screen level.
- `TD-LONG-HORIZON-FLUTTER-CALENDAR-ERROR-MAPPING-ACCESSIBILITY-TEST-CLOSURE-001` (Phase 4L.5A) remains `OPEN` -- append-only updated to note this phase's real progress without closing it. `TD-LONG-HORIZON-FLUTTER-READ-OUTCOME-RETRY-CONTINUATION-INTEGRATION-001` (Phase 4L.5) remains `CLOSED`, no regression found.

Current aggregate: 66 risks, 16 `OPEN`, 50 `CLOSED`.

### Long-Horizon Flutter regenerate/cancellation, auth redirect and mutation ambiguity closure (Phase 4L.5C)

`TD-LONG-HORIZON-FLUTTER-REGENERATE-CANCELLATION-AUTH-AMBIGUITY-CLOSURE-001` was added `CLOSED`.

- `RegeneratePreviewRequired` now opens an explicit explanation and a second destructive confirmation. It reuses the existing authenticated `POST /plans/{planId}/cancel` contract, then always reads authoritative active-plan state before clearing onboarding state or navigating. Opening the flow never cancels; cancellation never generates or confirms a replacement preview.
- Cancellation success, replay/404, lost response, same-plan-still-active and verification-read-failure paths are covered. Duplicate taps issue one request, committed transition runs once, and `go()` removes cancelled Home/regenerate routes from the stack.
- The real `AppRouter` now consumes a narrow app-owned `AuthNavigationState`; production delegates to the same Firebase user/auth-state stream, while tests inject loading/authenticated/unauthenticated states without Firebase initialization. Real-router tests prove protected rolling Home, Calendar, detail and regenerate routes, plus static detail and Habit routes, redirect before protected content; authenticated routes proceed; loading remains at bootstrap; no redirect loop occurs.
- Confirmation, completion, NotToday, activation, retry and cancellation timeouts/lost responses now perform endpoint-specific authoritative reads. Matching committed state succeeds once; unchanged state offers explicit retry; conflicting state renders the server-authored result; no path sends an automatic second mutation, activates from retry, or fabricates sessions.
- Accessibility coverage found and fixed one real small-screen issue: a long shared primary-button label could overflow horizontally. The label now flexes safely; destructive semantics, loading state, scrollability and focus-safe route replacement are covered.
- Focused regenerate/auth/ambiguity/accessibility files: 56 passed, 0 failed. Full Flutter suite: 339 passed, 0 failed, including static, Habit and Preparation Runway regressions. Touched-file analysis and formatting are clean. The project-wide analyzer/formatter still exposes inherited debt outside the touched set; no unrelated broad formatting was performed.
- `TD-LONG-HORIZON-FLUTTER-CALENDAR-ERROR-MAPPING-ACCESSIBILITY-TEST-CLOSURE-001` and `TD-LONG-HORIZON-FLUTTER-END-TO-END-FLOW-NAVIGATION-TEST-CLOSURE-001` are append-only updated to `CLOSED`. `TD-LONG-HORIZON-FLUTTER-READ-OUTCOME-RETRY-CONTINUATION-INTEGRATION-001` remains `CLOSED` with no regression.
- This closes the material Flutter integration gaps only. It does not declare release readiness and does not implement Phase 4L.6.

Current aggregate: 67 risks, 14 `OPEN`, 53 `CLOSED`.

### End-to-end release acceptance reconciliation (Phase 4L.6)

`TD-GENERAL-ENDURANCE-STAGED-PLAN-001`, `TD-LONG-HORIZON-NUMERIC-CALENDAR-EXECUTION-001`, `TD-LONG-HORIZON-RUNWAY-CORE-NUMERIC-CONTEXT-INTEGRATION-001`, `TD-LONG-HORIZON-RUNWAY-ENTRY-ABOVE-CORE-TARGET-001`, and `TD-LONG-HORIZON-VOLUME-ENVELOPE-REDESIGN-001` appear superseded by the implemented bounded rolling architecture, but remain `OPEN`. A trial closure caused 38 phase-local status guards to fail, proving these are meaningful historical closure contracts rather than the stale global-count test debt fixed in this phase. Closing them requires a separate evidence-backed governance migration that reconciles each original closure criterion; Phase 4L.6 does not weaken or bypass those guards.

`TD-LONG-HORIZON-END-TO-END-RELEASE-ACCEPTANCE-PRODUCTION-READINESS-001` was added `OPEN` with severity `P1_RELEASE_BLOCKER`. Local automated suites cannot establish production-like clean/existing-database migration, authenticated real-device Flutter-to-backend-to-PostgreSQL UAT, deployment/configuration, observability, rollout ownership, or rollback-drill acceptance. The release decision therefore remains NO-GO until that evidence is captured and approved.

Current aggregate: 68 risks, 15 `OPEN`, 53 `CLOSED`.

### Mobile release artifact and backend catalog packaging closure (Phase 4L.6A)

`TD-LONG-HORIZON-MOBILE-RELEASE-ARTIFACT-BACKEND-CATALOG-PACKAGING-001` was added `OPEN` with classification `BACKEND_CATALOG_PACKAGING_PROVEN_MOBILE_PRODUCTION_ARTIFACT_IDENTITY_AND_SIGNING_UNPROVEN`.

- Backend packaging is locally proven: Release output contains 71 canonical runtime JSON files in 12 exact-case directories; resolver precedence is explicit override, packaged content-root fallback and Development-only repository fallback; missing/invalid packages fail fast; seven focused tests pass; and the published API preserves the supported Long-Horizon, boundary, unsupported-input, static and Habit behavior matrix.
- Android minSdk is explicitly 23 and release signing no longer falls back to debug keys. Production release signing requires external `APPSEL_RELEASE_*` values, and key material remains ignored.
- Mobile production acceptance remains blocked: the only repository/Firebase identity is `com.example.antigravity_app`; no authority approves a replacement ID; no matching production Firebase client or production signing ownership is available; and restricted Flutter SDK cache access prevented a fresh dependency/test/APK/AAB run. No mobile binary or install/launch evidence is claimed.
- The parent `TD-LONG-HORIZON-END-TO-END-RELEASE-ACCEPTANCE-PRODUCTION-READINESS-001` remains `OPEN`. Phase 4L.6A narrows the backend artifact blocker but does not close migration, configuration/security, authenticated device UAT, observability, rollout or rollback requirements.

Current aggregate: 69 risks, 16 `OPEN`, 53 `CLOSED`.

### Production configuration, security, migration and forward-schema rollback closure (Phase 4L.6B)

`TD-LONG-HORIZON-PRODUCTION-CONFIGURATION-SECURITY-MIGRATION-ROLLBACK-001` was added `OPEN` with classification `CONFIGURATION_SECURITY_AND_MIGRATION_UPGRADE_PROVEN_APPLICATION_ROLLBACK_LIMITED_BY_UNMITIGATED_WRITE_SAFETY_GAP`.

- The checked-in `localhost/postgres/postgres` database fallback was removed from the Production-reachable `appsettings.json`; Development kept its own local default in `appsettings.Development.json`. A new `ProductionConfigurationValidator` fails startup, before any traffic is served, if `ConnectionStrings:DefaultConnection` is missing or matches a known local/development fragment, or if `Auth:Provider` is `Mock` — with no secret value in the resulting error. CORS is now environment-aware: Development keeps `AllowAnyOrigin`; Production defaults to zero allowed origins (fully restrictive) unless `Cors:AllowedOrigins` is explicitly configured. Forwarded-header trust (`Deployment:TrustedProxies`/`TrustedNetworks`) and HTTPS enforcement (`Deployment:EnforceHttpsRedirection`) are both explicit opt-in outside Development — nothing is silently assumed. 22 focused tests (11 new) prove all of this against the real host and real PostgreSQL; the full backend suite remained 3148/3148 (3137 + the 11 new tests, zero regressions) and plan-catalog remained 1250/1250.
- A real, reproducible EF Core migration bundle (`dotnet ef migrations bundle`, SHA-256 `f68173c808f1e28f5341f609977aee4be40a275815efd5b137d09b372419f729`) was generated and scanned clean of the known dev credential. It was proven against three real PostgreSQL databases: an empty database (all 21 migrations apply, idempotent rerun, clean non-zero exit on an invalid connection), a database rolled back to the exact pre-Long-Horizon migration and seeded through the real committed-HEAD application with a confirmed 12-week static plan and one real Completed day (migration preserved the plan's identity/status/outcome exactly, created zero fabricated rolling rows, and the current application then generated and confirmed a real 22-week Long-Horizon plan on the same upgraded database), and that same database once it also held a confirmed rolling plan with a real Completed and a real NotToday session (rerunning the bundle was a clean no-op with zero duplicate migration-history rows and the two outcome rows unchanged byte-for-byte).
- Forward-schema application rollback was tested with the actual previous application: committed HEAD `3549a8a1eeef18ca96794fa1056043142d13bc78` (isolated via a separate `git worktree`, since all five Long-Horizon migrations are currently uncommitted relative to that commit — confirmed via `git status`), Release-built and run against the latest schema while the real confirmed rolling plan from above still existed. Reads degraded safely (empty/generic view, zero fabricated `TrainingWeeks`/`TrainingDays`, zero mutation of the rolling session rows). A real write-safety gap was found and is not yet mitigated: the old binary's ordinary, schedule-strategy-blind `POST /api/v1/plans/{planId}/cancel` endpoint successfully cancelled the active rolling plan (`TrainingPlans.Status` flipped `active` → `cancelled`, verified in Postgres), even though the two `LongHorizonRollingSessionStates` outcome rows themselves were untouched. Redeploying the current application afterward reconstructed the session-level identity/outcomes/actual-values byte-for-byte, but the plan-level cancellation from the old binary was a genuine, real consequence, not reversible by redeploying.
- Rollback is classified `ROLLBACK_LIMITED`: static/Habit rollback is fully safe in both directions (proven with the same seeded fixture). Rolling-plan rollback is unsafe for writes until a gateway-level mitigation (blocking mutating Long-Horizon endpoints from the old binary, or equivalent) is implemented and re-verified against the same rehearsal. See `PHASE4L_6B_PRODUCTION_CONFIGURATION_SECURITY_MIGRATION_AND_FORWARD_SCHEMA_ROLLBACK_CLOSURE.md` for the full rehearsal transcript.
- The parent `TD-LONG-HORIZON-END-TO-END-RELEASE-ACCEPTANCE-PRODUCTION-READINESS-001` remains `OPEN`. Phase 4L.6B closes configuration/security and migration-upgrade acceptance with real evidence but does not close rollback, mobile artifact, CI, observability, rollout/kill-switch or staging UAT requirements.

Current aggregate: 70 risks, 17 `OPEN`, 53 `CLOSED`.

### Rolling-plan rollback mutation guard closure (Phase 4L.6B.2)

`TD-LONG-HORIZON-ROLLBACK-MUTATION-GUARD-001` was added `CLOSED` with classification `ROLLBACK_SAFE_WITH_EXPLICIT_MUTATION_GUARD`. `TD-LONG-HORIZON-PRODUCTION-CONFIGURATION-SECURITY-MIGRATION-ROLLBACK-001` (Phase 4L.6B) is closed as a result.

- Every mutating HTTP surface in committed HEAD (`3549a8a1eeef18ca96794fa1056043142d13bc78`) was inventoried directly from that worktree's controllers, not inferred from current source. Exactly one was unsafe: `POST /api/v1/plans/{planId}/cancel`, which the earlier phase proved could cancel an active `RollingLongHorizon` plan. Every other mutating endpoint (preview generation/confirmation, `TrainingDay` complete/not-today, pending-confirmation resolve, not-today-decision confirm) is either structurally unreachable for a rolling plan (no `TrainingDay`/`PendingConfirmation`/`NotTodayDecision` row is ever created for one — confirmed by real 404 responses against arbitrary IDs) or already protected by the pre-existing `IX_TrainingPlans_InternalUserId_ActiveOnly` unique database index, which was proven in this phase (not merely assumed) to reject a second active plan regardless of `ScheduleStrategy` — a real old-binary preview+confirm attempt against the active rolling plan returned the rolling plan's own ID with `already_active: true` and created zero new rows.
- Because a guard implemented only in the current application's own code cannot protect against rollback (the current application is, by definition, not running during a rollback incident), and this repository has no reverse proxy, gateway, or sidecar process in its deployment architecture, the guard was implemented as a PostgreSQL `BEFORE UPDATE OR DELETE` trigger on `TrainingPlans` (`fn_guard_rolling_plan_mutation`, migration `20260807093631_Phase4L6B2RollbackCompatibilityGuard`), gated by a singleton `RollbackCompatibilityMode` control row with **no HTTP route in either application able to toggle it** (toggling is a direct SQL statement in the rollback runbook) and a fail-closed default (missing/unreadable control row blocks, never permits).
- Re-running the exact Phase 4L.6B.1 rollback rehearsal — same committed-HEAD binary, same real active rolling plan with a Completed and a NotToday session — with the guard enabled: the previously-successful `cancel` call now returns a sanitized `500` and `TrainingPlans.Status` remains `active`; a direct raw-SQL `UPDATE` issued with no application involved at all is independently rejected by the trigger; three repeated and five concurrent `cancel` attempts are all blocked with zero partial mutation; the two session outcome rows remain byte-for-byte identical throughout. A second fixture proved the guard does not over-block: with the guard enabled from before the plan even existed, the old binary generated, confirmed, completed, and cancelled a real **static** plan with all four operations succeeding normally.
- After the rehearsal, the guard was disabled (direct SQL) and the current application redeployed against the unchanged database: `Home`/`Calendar` reconstructed the exact plan ID, window, and session set; a genuinely untouched third session was completed successfully, proving the rolling aggregate did not become subtly unusable. Five new automated tests (`RollbackCompatibilityGuardTests.cs`) exercise the trigger mechanism itself (direct-SQL block, static-plan non-interference, current-app error mapping, disabled-by-default sanity check, fail-closed-on-missing-row) against the real shared test Postgres and pass in the full backend suite.
- The parent `TD-LONG-HORIZON-END-TO-END-RELEASE-ACCEPTANCE-PRODUCTION-READINESS-001` remains `OPEN`. Phase 4L.6B.2 closes the rollback-safety gap specifically; mobile artifact identity/signing, CI/CD, observability, generation kill switch/rollout, and staging UAT are unrelated and remain unresolved.

Current aggregate: 71 risks, 16 `OPEN`, 55 `CLOSED`.

### CI/CD, generation kill switch, observability and staging UAT closure (Phase 4L.6C)

`TD-LONG-HORIZON-CI-CD-KILL-SWITCH-OBSERVABILITY-STAGING-UAT-001` was added `OPEN` with classification `KILL_SWITCH_AND_OBSERVABILITY_CLOSED_CI_CONFIG_VALIDATED_NOT_RUN_PROVEN_STAGING_UAT_NOT_RUN`.

- A server-authoritative, generation-only kill switch (`LongHorizon:GenerationEnabled`, default `true`) now gates only the dedicated Long-Horizon public preview endpoint. A full automated drill against real PostgreSQL over real HTTP proved: enabled → generate/confirm a real 22-week rolling plan; disabled → a new preview attempt returns `503 LONG_HORIZON_GENERATION_TEMPORARILY_DISABLED` while the same plan's Home, Calendar and a real session Complete all continue to work unchanged; re-enabled → new generation resumes and the existing plan's identity/status were untouched throughout. Confirmation of an already-issued preview, all reads, Complete, NotToday, activation, retry and cancellation are structurally untouched by the switch. Static and Habit routes use different code paths entirely and are unaffected — proven directly, not merely reasoned. A client-supplied bypass attempt (spoofed request-body and header fields claiming to enable generation) was rejected identically to a normal disabled-state request. A live smoke test against the actual published Release artifact (not just the Debug test host), configured only via an external environment variable, returned the exact same sanitized `503` contract.
- No prior CI/CD authority existed in this repository. A GitHub Actions PR-gate workflow (backend/plan-catalog/PostgreSQL/security/rollback-guard on every commit, Flutter analyze/test) and a release-candidate workflow (migration bundle generation/secret-scan/checksum/empty-DB rehearsal/idempotency/invalid-connection, backend publish with a hard catalog-count and no-secret assertion, and a signing-gated Android job that explicitly reports rather than fabricates the still-open mobile blocker) were created. YAML syntax was validated locally and every backend/migration/publish command inside both workflows was independently proven by direct execution in this session — but no actual hosted GitHub Actions run was performed, since doing so would require pushing to the real remote, outside this phase's authorized scope. This is recorded honestly as `CONFIG_VALIDATED`, not `CI_RUN_PROVEN`.
- Structured operational observability was found already extensive before this phase (preview request/generation/confirmation/retry/activation/completion/not-today/block-recovery-classification events all pre-existed with correlation IDs and no sensitive payloads). Two real, previously-unlogged gaps were found and closed: unsupported-horizon/eligibility preview rejections, and plan cancellation had no structured `ILogger` event (only a DB-level audit row); and the Phase 4L.6B.2 rollback-guard-blocked path was logged only for the generic-500 case, not its own typed `409`. Operational queries, minimal P1/P2 alert criteria (numeric thresholds explicitly marked `NEEDS_THRESHOLD_AUTHORITY` rather than invented), and a runbook are documented; owner roles are placeholders (`NEEDS_ASSIGNMENT`) since no named individuals exist anywhere in this repository's governance history.
- No production-equivalent staging environment (published artifact, packaged catalog, Production configuration, real Firebase authentication, HTTPS/reverse-proxy path, deployed migration artifact) exists in or reachable from this environment. Per this phase's own explicit instruction not to fabricate one, the required 44-scenario authenticated staging UAT matrix is recorded as entirely `NOT RUN` — no inferred `PASS`. Each row cites the strongest available local, non-staging test-suite evidence instead, so the exact gap is visible rather than a blanket unknown.
- Full backend suite: 3161/3161 (3153 baseline + 8 new kill-switch tests, zero regressions). Plan-catalog: 1250/1250. Flutter was not executed this phase (environment-blocked, consistent with every prior phase in this engagement).
- The parent `TD-LONG-HORIZON-END-TO-END-RELEASE-ACCEPTANCE-PRODUCTION-READINESS-001` remains `OPEN`. This record cannot close until an actual hosted CI run and real staging UAT evidence both exist; mobile artifact identity/signing (Phase 4L.6A) is unrelated and remains open.

Current aggregate: 72 risks, 17 `OPEN`, 55 `CLOSED`.

### External release environment activation and final re-acceptance (Phase 4L.6D)

`TD-LONG-HORIZON-EXTERNAL-RELEASE-ENVIRONMENT-FINAL-REACCEPTANCE-001` was added `OPEN` with classification `EXTERNAL_RELEASE_AUTHORITIES_GENUINELY_UNAVAILABLE_IN_THIS_ENVIRONMENT_NO_EVIDENCE_FABRICATED`.

- Every external release authority this phase's own instructions require was checked directly, not assumed: GitHub Actions execution access (no `gh` CLI, no `GITHUB_TOKEN`/`GH_TOKEN`), a staging hosting/deployment target (none found anywhere in the repository), a non-example Android production `applicationId` (`APPSEL_ANDROID_APPLICATION_ID` unset; no approved value recorded anywhere), a matching Firebase production client (the only registered Android client remains `com.example.antigravity_app` under real project `runner-29739`), Android production signing (only the Phase 4L.6A mechanical-testing-only keystore exists; no `key.properties`, no `APPSEL_RELEASE_*` variables), HTTPS/domain/reverse-proxy authority (none exists), a deployed observability platform (none exists beyond local structured logging), and named release/incident owners (none recorded anywhere in this repository's governance history). Ten of twelve required authorities are genuinely `MISSING`; the eleventh (Firebase project) exists but is registered to an example package, not a production one.
- No code was changed in this phase — there was nothing safe or honest to build without the missing authorities, and this phase's own instructions explicitly forbid fabricating hosted CI, Firebase, staging, signing, TLS, or observability evidence. No hosted CI run, signed Android artifact, real Firebase authentication, staging deployment, or UAT scenario was attempted or claimed. All 44 staging UAT scenarios remain exactly as Phase 4L.6C left them: NOT RUN.
- Every prior phase's proven local evidence (backend 3161/3161, plan-catalog 1250/1250, migration/rollback guard/kill-switch/observability closures) remains valid, unchanged, and not reopened by this finding.
- The parent `TD-LONG-HORIZON-END-TO-END-RELEASE-ACCEPTANCE-PRODUCTION-READINESS-001` remains `OPEN`. Final release decision: **NO-GO**. The phase document's own Section 53 records the exact recommended sequencing once real external authorities become available; no further phase should re-attempt end-to-end release re-acceptance until at least CI execution access and a real staging deployment target exist.

Current aggregate: 73 risks, 18 `OPEN`, 55 `CLOSED`.
